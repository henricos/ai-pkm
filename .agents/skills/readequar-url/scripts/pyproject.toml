[project]
name = "readequar-url-skill"
version = "0.1.0"
description = "Ambiente Python local da skill readequar-url"
requires-python = ">=3.12"
dependencies = [
  "pyyaml>=6.0",
]

[tool.uv]
package = false
