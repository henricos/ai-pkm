[project]
name = "criar-grupo-skill"
version = "0.1.0"
description = "Ambiente Python local da skill criar-grupo"
requires-python = ">=3.12"
dependencies = [
  "pyyaml>=6.0",
]

[tool.uv]
package = false
