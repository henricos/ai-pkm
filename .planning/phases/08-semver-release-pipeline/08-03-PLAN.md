---
phase: 08-semver-release-pipeline
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - README.md
  - docs/release-semver-ghcr.md
autonomous: false
requirements: [VER-02, VER-03, PUB-01, PUB-03, PUB-04]

must_haves:
  truths:
    - "O operador tem um checklist curto e explícito para fechar a release sem wrapper opaco"
    - "O fluxo oficial documentado exige working tree limpa, branch `main`, `npm version` e `git push --follow-tags`"
    - "O operador sabe como conferir a rastreabilidade entre `package.json`, tag Git, UI e imagem publicada"
    - "O repositório deixa explícito que qualquer automação guiada futura deve apenas orquestrar os comandos canônicos, preferencialmente via skill"
  artifacts:
    - path: "docs/release-semver-ghcr.md"
      provides: "checklist operacional curto da release SemVer para GHCR"
    - path: "README.md"
      provides: "ponte a partir da narrativa principal do projeto para o fluxo oficial de release"
  key_links:
    - from: "docs/release-semver-ghcr.md"
      to: ".github/workflows/release-ghcr.yml"
      via: "documentação ensina o operador a disparar exatamente o workflow criado na fase"
    - from: "docs/release-semver-ghcr.md"
      to: "package.json"
      via: "`npm version patch|minor|major` continua sendo a fonte oficial da release"
    - from: "README.md"
      to: "docs/release-semver-ghcr.md"
      via: "repositório passa a apontar para o procedimento canônico de release"
---

<objective>
Fechar a superfície operacional humana da fase: o operador precisa conseguir executar a release SemVer de forma curta, auditável e sem esconder o mecanismo real por trás de scripts proprietários.

Purpose: cumprir VER-02 e consolidar a verificabilidade operacional de VER-03/PUB-01/PUB-03/PUB-04.
Output: documentação canônica de release + checkpoint humano para validar o fluxo externo ao ambiente local.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-semver-release-pipeline/08-CONTEXT.md
@.planning/phases/08-semver-release-pipeline/08-RESEARCH.md
@.planning/phases/08-semver-release-pipeline/08-VALIDATION.md
@.github/workflows/release-ghcr.yml
@README.md
@docs/docker-validation.md
@docs/dev-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentar o checklist canônico de release SemVer</name>
  <files>docs/release-semver-ghcr.md, README.md</files>
  <read_first>
    - .planning/phases/08-semver-release-pipeline/08-CONTEXT.md
    - .planning/phases/08-semver-release-pipeline/08-RESEARCH.md
    - .github/workflows/release-ghcr.yml
    - README.md
    - docs/docker-validation.md
    - docs/dev-setup.md
  </read_first>
  <action>Criar `docs/release-semver-ghcr.md` como o procedimento canônico da fase, com um checklist curto e explícito. O documento deve incluir, em ordem: `git fetch origin`; `git checkout main`; `git pull --ff-only origin main`; validação de working tree limpa com `git diff --quiet && git diff --cached --quiet`; validação local com `npm test && npm run typecheck && npm run build`; execução de `npm version patch|minor|major`; publicação com `git push origin main --follow-tags`; e conferência do workflow/tag/imagem em `ghcr.io/henricos/ai-pkm`. Incluir uma seção curta de rastreabilidade mandando conferir `package.json`, a tag Git `vX.Y.Z`, o footer da tela de login e a imagem no GHCR. Esta task implementa explicitamente D-02, D-03, D-04, D-10 e D-11 ao documentar precondições operacionais, o mecanismo oficial de bump, o push do commit+tag correspondente, o fluxo nativo e o checklist curto obrigatório. Registrar explicitamente que qualquer automação guiada futura deve apenas orquestrar esses comandos, preferencialmente via skill per D-12/D-13. Atualizar `README.md` para apontar para esse guia na seção operacional apropriada. Não criar script wrapper, alias ou Make target.</action>
  <acceptance_criteria>
    - `test -f docs/release-semver-ghcr.md` retorna exit code 0
    - `rg -n 'git checkout main|git diff --quiet && git diff --cached --quiet|npm version patch\\|minor\\|major|git push origin main --follow-tags|ghcr.io/henricos/ai-pkm|preferencialmente via skill' docs/release-semver-ghcr.md` encontra todos os comandos e a regra de automação guiada
    - `rg -n 'release-semver-ghcr\\.md' README.md` encontra o link para o novo guia
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'git checkout main|git diff --quiet && git diff --cached --quiet|npm version patch\\|minor\\|major|git push origin main --follow-tags|ghcr.io/henricos/ai-pkm|preferencialmente via skill' docs/release-semver-ghcr.md && rg -n 'release-semver-ghcr\\.md' README.md</automated>
  </verify>
  <done>O repositório passa a documentar um fluxo canônico de release curto, nativo e auditável, sem wrappers opacos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validar uma release real ponta a ponta</name>
  <files>docs/release-semver-ghcr.md, .github/workflows/release-ghcr.yml, package.json, src/app/(auth)/login/page.tsx</files>
  <read_first>
    - docs/release-semver-ghcr.md
    - .github/workflows/release-ghcr.yml
    - package.json
    - src/app/(auth)/login/page.tsx
  </read_first>
  <action>Executar uma release real usando exatamente o checklist documentado na Task 1, sem atalhos nem wrappers: partir de `main`, confirmar working tree limpa, rodar `npm version patch|minor|major`, publicar com `git push origin main --follow-tags` e então conferir o workflow `Release GHCR`, as tags `vX.Y.Z` e `latest` em `ghcr.io/henricos/ai-pkm`, além do footer da app mostrando a mesma versão e o hash curto do commit. Esta task fecha operacionalmente D-02, D-03, D-04, D-10 e D-11, provando que o fluxo oficial da release continua curto, nativo, auditável e baseado em `npm version` + push da tag correspondente.</action>
  <what_built>Fluxo operacional de release SemVer por `npm version` com publicação por tag no GHCR e rastreabilidade entre app, Git e imagem.</what_built>
  <how-to-verify>
    1. Na sua máquina com Git remoto configurado, execute os comandos de `docs/release-semver-ghcr.md` a partir de `main` com working tree limpa.
    2. Escolha um bump real: `npm version patch`, `npm version minor` ou `npm version major`.
    3. Rode `git push origin main --follow-tags`.
    4. No GitHub Actions, confirme que o workflow `Release GHCR` iniciou por causa da tag `vX.Y.Z` e que o job rodou em `ubuntu-latest`.
    5. No GHCR, confirme que `ghcr.io/henricos/ai-pkm` recebeu as tags `vX.Y.Z` e `latest`.
    6. Abra a app da release publicada ou rode a imagem localmente e confirme no footer do login o formato `vX.Y.Z · abc1234`.
    7. Se o pacote tiver nascido privado, ajuste a visibilidade para `public` uma vez e registre isso no SUMMARY da execução.
  </how-to-verify>
  <acceptance_criteria>
    - Existe uma tag Git `vX.Y.Z` nova apontando para o commit de release gerado por `npm version`
    - O workflow `Release GHCR` termina com status `success`
    - O pacote `ghcr.io/henricos/ai-pkm` exibe `vX.Y.Z` e `latest`
    - O footer da tela de login mostra a mesma versão `X.Y.Z` e um hash curto correspondente ao commit da release
  </acceptance_criteria>
  <resume-signal>Type "approved" se a release externa passou, ou descreva exatamente onde a cadeia quebrou.</resume-signal>
  <verify>
    <automated>test -f docs/release-semver-ghcr.md && test -f .github/workflows/release-ghcr.yml && rg -n 'git checkout main|git diff --quiet && git diff --cached --quiet|npm version patch\\|minor\\|major|git push origin main --follow-tags|ghcr.io/henricos/ai-pkm|preferencialmente via skill' docs/release-semver-ghcr.md && rg -n 'Release GHCR|ghcr.io/henricos/ai-pkm|docker/build-push-action|docker/login-action' .github/workflows/release-ghcr.yml && rg -n 'NEXT_PUBLIC_APP_VERSION|NEXT_PUBLIC_GIT_HASH|npm_package_version' src/app/'(auth)'/login/page.tsx package.json</automated>
  </verify>
  <done>A fase fica operacionalmente comprovada fora do ambiente local, com evidência de tag Git, workflow e imagem publicada coerentes entre si.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator -> release commands | Erro humano pode gerar tag fora da política oficial |
| Repository docs -> real operational flow | A documentação precisa refletir exatamente o workflow versionado |
| GitHub/GHCR external systems -> project audit trail | Parte da evidência vive fora do repositório e precisa ser verificada explicitamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Repudiation | release procedure | mitigate | Documentar checklist curto com comandos exatos e prova de rastreabilidade |
| T-08-09 | Tampering | operator workflow | mitigate | Exigir `main`, working tree limpa e `npm version` como mecanismo oficial |
| T-08-10 | Information Disclosure | package visibility | accept | A imagem precisa ser pública por requisito; limitar a mudança manual apenas à visibilidade do pacote |
</threat_model>

<verification>
- `rg -n 'git checkout main|git diff --quiet && git diff --cached --quiet|npm version patch\\|minor\\|major|git push origin main --follow-tags|ghcr.io/henricos/ai-pkm' docs/release-semver-ghcr.md`
- `rg -n 'release-semver-ghcr\\.md' README.md`
- Checkpoint humano: release real executada e aprovada
</verification>

<success_criteria>
- O operador consegue fechar a release seguindo um guia curto, sem script wrapper.
- A documentação ensina como provar a relação entre `package.json`, tag Git, UI e GHCR.
- O checkpoint humano confirma uma publicação real ou identifica precisamente o ponto de quebra.
</success_criteria>

<output>
After completion, create `.planning/phases/08-semver-release-pipeline/08-03-SUMMARY.md`
</output>
