---
phase: 07-container-packaging-foundation
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - README.md
  - docs/dev-setup.md
  - docs/docker-validation.md
autonomous: false
requirements: [PKG-03]

must_haves:
  truths:
    - "Um operador consegue validar a imagem localmente sem adivinhar mounts, env vars ou paths"
    - "A documentacao separa claramente dev local, runtime container e refresh externo futuro"
    - "O repositório registra explicitamente que `pkm` e `index` precisam evoluir juntos enquanto `index` for dinamico"
    - "O plano documenta o contrato futuro de refresh externo sem implementa-lo na UI web"
    - "O checkpoint final prova login e leitura coerente do acervo montado"
  artifacts:
    - path: "docs/docker-validation.md"
      provides: "guia canonico da validacao Docker e do contrato de refresh externo"
    - path: "README.md"
      provides: "entrada principal para descobrir o fluxo Docker correto"
    - path: "docs/dev-setup.md"
      provides: "separacao entre setup de desenvolvimento e validacao do artefato distribuivel"
  key_links:
    - from: "README.md"
      to: "docs/docker-validation.md"
      via: "operador encontra o fluxo sem cair em docs antigas"
    - from: "docs/docker-validation.md"
      to: "compose.yaml"
      via: "guia usa exatamente o contrato canonico da fase"
    - from: "docs/docker-validation.md"
      to: "07-CONTEXT.md"
      via: "documenta o racional do refresh externo e da separacao de artefatos"
---

<objective>
Fechar PKG-03 com documentacao e checkpoint operacional alinhados ao replanejamento, registrando claramente o modelo de validacao local e o contrato futuro de refresh externo para `pkm` + `index`.

Purpose: transformar o novo contexto da Phase 7 em operacao compreensivel e verificavel pelo operador, sem misturar com release pipeline ou redeploy.
Output: docs objetivas e checkpoint manual final da fase.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/07-container-packaging-foundation/07-CONTEXT.md
@.planning/phases/07-container-packaging-foundation/07-RESEARCH.md
@.planning/phases/07-container-packaging-foundation/07-VALIDATION.md
@AGENTS.md
@README.md
@docs/dev-setup.md
@compose.yaml
@.env.compose.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentar o fluxo oficial de validacao Docker da fase</name>
  <files>README.md, docs/dev-setup.md, docs/docker-validation.md</files>
  <action>Adicionar `docs/docker-validation.md` com o fluxo oficial da fase: preparar env file local, ajustar `PKM_HOST_PATH` e `INDEX_HOST_PATH`, executar `docker compose config`, `docker compose build` e `docker compose up`, autenticar na aplicacao e confirmar leitura coerente do acervo montado. Atualizar `README.md` para apontar para esse guia e corrigir a secao de producao para o contrato novo da fase. Ajustar `docs/dev-setup.md` para distinguir claramente desenvolvimento via `npm run dev` do runtime container validado via compose. Se a fase ficar sem healthcheck, documentar que isso e uma decisao consciente enquanto nao houver endpoint seguro e desacoplado de auth/proxy.</action>
  <verify>
    <automated>rg -n 'docker compose|docs/docker-validation|INDEX_HOST_PATH|PKM_HOST_PATH|npm run dev|healthcheck' README.md docs/dev-setup.md docs/docker-validation.md</automated>
  </verify>
  <done>O operador passa a ter um fluxo unico, descobrivel e coerente para validar localmente a imagem replanejada.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar o contrato de refresh externo futuro e o impacto para agentes</name>
  <files>docs/docker-validation.md</files>
  <action>Registrar em `docs/docker-validation.md` um resumo tecnico objetivo sobre o que precisa ser externo ao container nesta fase (`pkm` e `index`), o que fica versionado na release (`models`, `reference`, `.agents/skills`, `AGENTS.md`) e como isso conversa com a possibilidade futura de agentes no mesmo runtime. Explicitar que o primeiro refresh operacional deve acontecer fora da UI web, por mecanismo externo ao processo principal, e que enquanto `index` for dinamico ele deve ser atualizado em conjunto com `pkm`. Nao implementar script nem endpoint agora; apenas deixar o contrato inequívoco.</action>
  <verify>
    <automated>rg -n 'refresh externo|index|pkm|models|reference|\\.agents/skills|AGENTS\\.md' docs/docker-validation.md</automated>
  </verify>
  <done>O repositorio registra com clareza o contrato operacional futuro sem empurrar essa responsabilidade para a app nesta fase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Executar o checkpoint operacional final do runtime replanejado</name>
  <files>docs/docker-validation.md, .planning/phases/07-container-packaging-foundation/07-VALIDATION.md</files>
  <action>Usar `docs/docker-validation.md` e `07-VALIDATION.md` como roteiro oficial da prova operacional. O checkpoint precisa confirmar que a imagem builda, o compose monta `pkm` e `index` externamente, a autenticacao continua obrigatoria e a navegacao le o acervo sem depender de artefatos cristalizados na imagem.</action>
  <verify>
    <automated>test -f docs/docker-validation.md && test -f .planning/phases/07-container-packaging-foundation/07-VALIDATION.md && rg -n 'docker compose config|docker compose build|docker compose up|/data/pkm|/data/index' docs/docker-validation.md .planning/phases/07-container-packaging-foundation/07-VALIDATION.md</automated>
  </verify>
  <done>O operador executou o roteiro manual e confirmou `approved` ou listou desvios concretos do contrato novo.</done>
  <how-to-verify>
    1. Copie `.env.compose.example` para um arquivo local de trabalho e preencha valores reais, incluindo paths absolutos para `PKM_HOST_PATH` e `INDEX_HOST_PATH`.
    2. Execute `docker compose config` e confirme que o serviço monta ambos os diretórios externos e injeta `PKM_PATH` e o path de `index` correto no container.
    3. Execute `docker compose build` e `docker compose up`.
    4. Abra `http://localhost:3000`, faça login com as credenciais configuradas e confirme que a aplicação sobe normalmente.
    5. Valide que a árvore e as leituras usam o acervo montado, sem erro de índice ausente nem dependência de `cwd` antigo.
    6. Pare o stack e registre qualquer divergencia.
  </how-to-verify>
  <resume-signal>Responder com `approved` se o fluxo bateu com a documentacao, ou listar os desvios encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> operator actions | O operador depende das docs para montar corretamente dados dinamicos e nao validar o fluxo errado |
| External refresh -> running container | O contrato precisa deixar claro que a UI nao e responsavel pelo refresh do acervo |
| Browser login -> mounted runtime data | O checkpoint final precisa provar auth obrigatoria e leitura coerente dos mounts externos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Repudiation | operator validation | mitigate | Roteiro manual objetivo com passos e resultados esperados |
| T-07-09 | Spoofing | docs/runtime expectations | mitigate | Separar claramente dev, container e refresh externo |
| T-07-10 | Information Disclosure | operational guidance | mitigate | Nao documentar segredos reais nem instrucoes de release/deploy fora do escopo |
</threat_model>

<verification>
- `rg -n 'docker compose|docs/docker-validation|INDEX_HOST_PATH|PKM_HOST_PATH|npm run dev|healthcheck' README.md docs/dev-setup.md docs/docker-validation.md`
- `rg -n 'refresh externo|index|pkm|models|reference|\\.agents/skills|AGENTS\\.md' docs/docker-validation.md`
- Checkpoint manual de `docker compose config`, `docker compose build` e `docker compose up`
</verification>

<success_criteria>
- O repositório documenta o fluxo minimo para validar a imagem localmente no contrato novo.
- As docs separam claramente desenvolvimento, runtime container e refresh externo futuro.
- O checkpoint final prova login e leitura coerente de `pkm` + `index` montados externamente.
</success_criteria>

<output>
After completion, create `.planning/phases/07-container-packaging-foundation/07-03-SUMMARY.md`
</output>
