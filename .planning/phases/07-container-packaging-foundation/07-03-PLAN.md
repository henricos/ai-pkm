---
phase: 07-container-packaging-foundation
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - README.md
  - docs/dev-setup.md
  - docs/docker-validation.md
autonomous: false
requirements: [PKG-03]

must_haves:
  truths:
    - "Um operador consegue preparar a validacao local da imagem sem adivinhar env vars, mounts ou comandos"
    - "A documentacao separa claramente dev via `npm run dev` de validacao do artefato distribuivel via `docker compose`"
    - "A ausencia de healthcheck automatico, se mantida, fica documentada com racional tecnico e nao como omissao acidental"
    - "A fase registra o follow-up tecnico de D-09 sobre compatibilidade de path externo para skills/agentes"
    - "A verificacao final da fase inclui um roteiro manual objetivo para provar que o container sobe, autentica e le o `pkm` montado"
  artifacts:
    - path: "docs/docker-validation.md"
      provides: "guia dedicado de validacao local do container, incluindo o follow-up tecnico de D-09"
    - path: "README.md"
      provides: "ponte de descoberta para o fluxo de validacao Docker"
    - path: "docs/dev-setup.md"
      provides: "separacao explicita entre setup dev e validacao do container"
  key_links:
    - from: "README.md"
      to: "docs/docker-validation.md"
      via: "entrada principal para operadores encontrarem o fluxo"
    - from: "docs/docker-validation.md"
      to: "compose.yaml"
      via: "comandos oficiais de build/up/down"
    - from: "docs/dev-setup.md"
      to: "docs/docker-validation.md"
      via: "contraste entre dev local e validacao do artefato"
---

<objective>
Documentar e verificar o fluxo minimo da Phase 7 para que a imagem possa ser validada localmente antes de qualquer release, sem misturar isso com o setup de desenvolvimento nem com o pipeline das fases 8-9.

Purpose: fechar PKG-03 com instrucoes objetivas de operador e checkpoint manual do comportamento do container.
Output: documentacao dedicada do fluxo `docker compose` e checkpoint manual cobrindo build, subida, autenticacao e leitura do `pkm` montado.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/07-container-packaging-foundation/07-CONTEXT.md
@.planning/phases/07-container-packaging-foundation/07-RESEARCH.md
@.planning/phases/07-container-packaging-foundation/07-VALIDATION.md
@AGENTS.md
@README.md
@docs/dev-setup.md
@compose.yaml
@.env.compose.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a documentacao canonica da validacao local do container</name>
  <files>README.md, docs/dev-setup.md, docs/docker-validation.md</files>
  <action>Adicionar `docs/docker-validation.md` com o fluxo oficial da fase: copiar `.env.compose.example`, ajustar `PKM_HOST_PATH`, executar `docker compose config`, `docker compose build` e `docker compose up`, autenticar na aplicacao e confirmar leitura do acervo. Explicitar que este fluxo valida o artefato distribuivel, enquanto `npm run dev` continua sendo o caminho de desenvolvimento. Atualizar `README.md` para apontar para o guia Docker e ajustar a secao de producao para usar o novo contrato canonico, sem puxar GHCR, tags, GitHub Actions ou Portainer. Atualizar `docs/dev-setup.md` apenas para referenciar o guia Docker como fluxo separado. Documentar tambem a decisao de nao forcar healthcheck na Phase 7 caso o compose permaneça sem ele, com a justificativa de evitar endpoint inseguro ou acoplado a auth/proxy. Referenciar `07-VALIDATION.md` como contrato Nyquist da fase e deixar claro que o checkpoint final desta plan e a prova operacional de PKG-01 e PKG-03.</action>
  <verify>
    <automated>rg -n 'docker compose|docs/docker-validation|npm run dev|healthcheck' README.md docs/dev-setup.md docs/docker-validation.md</automated>
  </verify>
  <done>Existe um guia dedicado e descobrivel para validar a imagem localmente, separado do setup dev e sem escopo indevido de release/publicacao.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar o follow-up tecnico de compatibilidade de path externo para skills/agentes</name>
  <files>docs/docker-validation.md</files>
  <action>Executar o follow-up pedido em D-09: auditar `AGENTS.md` e as conventions operacionais relevantes para determinar se o ecossistema atual de skills/agentes assume `pkm/` relativo ao workspace ou se ja tolera path externo configuravel. Registrar em `docs/docker-validation.md` um resultado curto e objetivo: o que hoje depende do workspace do repo `ai-pkm`, o que continua preso a `pkm/` local e qual e a implicacao para o futuro milestone com agente no mesmo container. Nao expandir isso para refactor de skills nesta fase; o objetivo aqui e fechar a rastreabilidade tecnica do risco, nao resolver o milestone futuro.</action>
  <verify>
    <automated>rg -n 'D-09|path externo|pkm/' .planning/phases/07-container-packaging-foundation/07-CONTEXT.md && rg -n 'path externo|pkm/' docs/docker-validation.md</automated>
  </verify>
  <done>O repositorio registra explicitamente o resultado do follow-up de D-09 e deixa claro se a compatibilidade com path externo e uma restricao atual ou apenas um cuidado futuro.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Executar o checkpoint operacional final do container</name>
  <files>docs/docker-validation.md, .planning/phases/07-container-packaging-foundation/07-VALIDATION.md</files>
  <action>Usar `docs/docker-validation.md` e `07-VALIDATION.md` como roteiro oficial para validar o artefato Docker da fase. Este checkpoint e a prova operacional final de PKG-01 e PKG-03: ele precisa confirmar que a imagem builda no contrato real, o container sobe por `docker compose`, a autenticacao continua obrigatoria e o `pkm` montado externamente e realmente lido em runtime.</action>
  <verify>
    <automated>test -f .planning/phases/07-container-packaging-foundation/07-VALIDATION.md && test -f docs/docker-validation.md && rg -n 'docker compose config|docker compose build|docker compose up|/data/pkm' docs/docker-validation.md .planning/phases/07-container-packaging-foundation/07-VALIDATION.md</automated>
  </verify>
  <done>O operador executou o roteiro manual, confirmou login e leitura do `pkm` montado, e respondeu `approved` ou listou os desvios encontrados.</done>
  <what-built>Fluxo completo de validacao local do container da Phase 7 via `docker compose`.</what-built>
  <how-to-verify>
    1. Copie `.env.compose.example` para um arquivo local de trabalho e preencha valores reais, incluindo um `PKM_HOST_PATH` absoluto.
    2. Execute `docker compose config` e confirme que `PKM_PATH=/data/pkm` aparece no ambiente do servico e que o bind mount aponta para `/data/pkm`.
    3. Execute `docker compose build` seguido de `docker compose up`.
    4. Abra `http://localhost:3000`, faça login com `AUTH_USERNAME` e `AUTH_PASSWORD`.
    5. Confirme que a arvore e/ou a home leem o acervo montado, em vez de exibir erro de `PKM_PATH` ou de indices ausentes.
    6. Pare o stack e registre qualquer divergencia entre o comportamento observado e o guia.
  </how-to-verify>
  <resume-signal>Responder com `approved` se o fluxo bateu com a documentacao, ou listar os desvios encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> operator actions | O operador depende das instrucoes para montar o `pkm` correto e nao expor configs indevidas |
| Browser login -> container runtime | A verificacao manual precisa confirmar auth obrigatoria e leitura real do volume montado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Repudiation | operator validation | mitigate | Registrar um roteiro manual objetivo com passos e resultados esperados |
| T-07-10 | Spoofing | docs/runtime expectations | mitigate | Separar claramente `npm run dev` de `docker compose` para evitar validar o fluxo errado |
| T-07-11 | Information Disclosure | production docs | mitigate | Nao documentar secrets reais nem instrucoes de publicacao/deploy fora do escopo da fase |
</threat_model>

<verification>
- `rg -n 'docker compose|docs/docker-validation|npm run dev|healthcheck' README.md docs/dev-setup.md docs/docker-validation.md`
- `rg -n 'path externo|pkm/' docs/docker-validation.md`
- Checkpoint manual de `docker compose config`, `docker compose build` e `docker compose up`
</verification>

<success_criteria>
- O repositório documenta o fluxo minimo para validar a imagem localmente.
- A documentacao separa claramente desenvolvimento e validacao do artefato distribuivel.
- O follow-up tecnico de D-09 fica registrado dentro da fase.
- Existe checkpoint manual objetivo para confirmar login e leitura do `pkm` montado.
- Nenhum passo da fase invade versionamento, GHCR, GitHub Actions ou Portainer.
</success_criteria>

<output>
After completion, create `.planning/phases/07-container-packaging-foundation/07-03-SUMMARY.md`
</output>
