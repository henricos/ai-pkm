---
phase: 07-container-packaging-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env.ts
  - src/lib/runtime-paths.ts
  - src/lib/pkm/fs-item-repository.ts
  - src/lib/navigation/navigation-service.ts
  - docs/dev-setup.md
autonomous: true
requirements: [PKG-01, PKG-02]

must_haves:
  truths:
    - "A app deixa de depender implicitamente de `process.cwd()` para encontrar artefatos dinamicos de runtime"
    - "`pkm` e `index` passam a ter contrato explicito e simetrico de configuracao externa"
    - "Artefatos versionados do repo continuam distinguiveis de artefatos dinamicos montados no runtime"
    - "O comportamento em dev permanece ergonomico sem esconder o contrato real de producao"
  artifacts:
    - path: "src/lib/runtime-paths.ts"
      provides: "fonte unica de resolucao para paths dinamicos e versionados"
    - path: "src/lib/env.ts"
      provides: "validacao fail-fast das env vars novas ou ajustadas do runtime"
    - path: "src/lib/pkm/fs-item-repository.ts"
      provides: "consumo de `pkm` e `index` via contrato explicito, sem `cwd` implicito"
    - path: "src/lib/navigation/navigation-service.ts"
      provides: "leitura dos indices via contrato central de runtime"
    - path: "docs/dev-setup.md"
      provides: "documentacao atualizada dos defaults locais e dos novos paths"
  key_links:
    - from: "src/lib/runtime-paths.ts"
      to: "src/lib/pkm/fs-item-repository.ts"
      via: "repositório filesystem consome paths centrais em vez de resolver localmente"
    - from: "src/lib/runtime-paths.ts"
      to: "src/lib/navigation/navigation-service.ts"
      via: "navigation deixa de assumir `index/` na raiz do cwd"
    - from: "src/lib/env.ts"
      to: "docs/dev-setup.md"
      via: "novas env vars e defaults ficam documentadas e verificaveis"
---

<objective>
Fechar a base arquitetural do replanejamento: transformar paths de runtime em contrato explicito antes de empacotar a imagem, para que `pkm` e `index` possam ser montados externamente sem ambiguidade e sem depender do layout atual do workspace.

Purpose: satisfazer o novo boundary de PKG-01/PKG-02, que agora exige separar artefatos versionados por release de artefatos dinamicos de runtime.
Output: modulo central de runtime paths, ajustes de env e consumo da app migrado para esse contrato.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-container-packaging-foundation/07-CONTEXT.md
@.planning/phases/07-container-packaging-foundation/07-RESEARCH.md
@.planning/phases/07-container-packaging-foundation/07-VALIDATION.md
@AGENTS.md
@reference/pkm/pkm-structure.md
@src/lib/env.ts
@src/lib/pkm/fs-item-repository.ts
@src/lib/navigation/navigation-service.ts
@docs/dev-setup.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar o contrato central de runtime paths</name>
  <files>src/lib/env.ts, src/lib/runtime-paths.ts</files>
  <behavior>
    - `PKM_PATH` continua obrigatorio e externo
    - `index/` deixa de ser inferido apenas por `cwd` e ganha resolucao explicita
    - artefatos versionados do repo podem ser localizados de forma clara tanto em dev quanto no container
    - defaults locais nao mascaram configuracao obrigatoria de producao
  </behavior>
  <action>Criar `src/lib/runtime-paths.ts` como fonte unica para resolver paths dinamicos e versionados. Esse modulo deve distinguir pelo menos: `pkm`, `index`, raiz versionada do app e referencias fixas (`models`, `reference`, `.agents/skills`, `AGENTS.md`). Atualizar `src/lib/env.ts` apenas no necessario para suportar o novo contrato, preferindo env explicita para `index` em producao e fallback local previsivel em dev. O resultado final nao pode depender de chamadas ad hoc a `process.cwd()` espalhadas pelo codigo.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>A app passa a ter um contrato unico e auditavel para paths de runtime, desacoplando a fase do layout acidental do workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar consumidores server-side para o contrato novo</name>
  <files>src/lib/pkm/fs-item-repository.ts, src/lib/navigation/navigation-service.ts, docs/dev-setup.md</files>
  <action>Refatorar os consumidores atuais de `index/` para usar o contrato central de runtime paths, mantendo `pkm` como dependencia externa configurada. Ajustar a documentacao de `docs/dev-setup.md` para explicar o novo comportamento: em dev, a app pode continuar funcionando com defaults locais coerentes, mas o contrato real passa a ser `PKM_PATH` + path explicito ou resolvido centralmente para `index`. Se houver mensagens de erro ou comentarios de codigo que ainda assumem `index/` na raiz do `cwd`, corrigir para o novo modelo.</action>
  <verify>
    <automated>npm run test && npm run typecheck</automated>
  </verify>
  <done>Os pontos server-side que antes dependiam de `process.cwd()` passam a consumir paths centralizados, e a documentacao local deixa de ensinar um contrato desalinhado com o container.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime env -> path resolution | Paths externos precisam ser validados e previsiveis |
| Dynamic data -> versioned app assets | `pkm` e `index` nao podem ser confundidos com artefatos versionados da release |
| Dev workspace -> production container | Defaults locais nao podem mascarar dependencias reais de runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | dynamic path resolution | mitigate | Centralizar resolucao de paths e eliminar dependencias implícitas de `cwd` |
| T-07-02 | Denial of Service | runtime bootstrap | mitigate | Validar cedo envs e caminhos dinamicos necessarios |
| T-07-03 | Repudiation | runtime contract | mitigate | Documentar explicitamente o contrato de dev vs container |
</threat_model>

<verification>
- `npm run test`
- `npm run typecheck`
- `rg -n 'process\\.cwd\\(\\).*index|path\\.join\\(process\\.cwd\\(\\), \"index\"\\)' src/lib`
- `test -f src/lib/runtime-paths.ts`
</verification>

<success_criteria>
- A app deixa de assumir implicitamente `index/` na raiz do `cwd`.
- Existe um contrato central para localizar `pkm`, `index` e artefatos versionados.
- O comportamento local continua ergonomico sem esconder o contrato de producao.
</success_criteria>

<output>
After completion, create `.planning/phases/07-container-packaging-foundation/07-01-SUMMARY.md`
</output>
