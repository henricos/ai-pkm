---
phase: 03-reading-viewer
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - package.json
  - src/app/globals.css
autonomous: true
requirements: [VIEW-02, VIEW-03, VIEW-08]

must_haves:
  truths:
    - "react-markdown, remark-gfm, remark-math, rehype-katex, @shikijs/rehype, shiki estão em dependencies no package.json"
    - "@tailwindcss/typography está em devDependencies no package.json"
    - "globals.css contém @import 'katex/dist/katex.min.css'"
    - "globals.css contém @plugin '@tailwindcss/typography'"
    - "globals.css contém overrides das variáveis --tw-prose-* com tokens do design system"
  artifacts:
    - path: "package.json"
      provides: "Dependências do pipeline de renderização instaladas"
    - path: "src/app/globals.css"
      provides: "Plugin typography carregado + KaTeX CSS + overrides de cor prose"
  key_links:
    - from: "src/app/globals.css"
      to: "@tailwindcss/typography"
      via: "@plugin directive — método CSS-first do Tailwind v4"
    - from: "src/app/globals.css"
      to: "katex/dist/katex.min.css"
      via: "@import — CSS global para renderização de fórmulas"
---

<objective>
Instalar todas as dependências do pipeline de renderização Markdown e configurar o globals.css com o plugin de tipografia e o CSS do KaTeX. Este plano não cria nenhum componente — apenas prepara o ambiente para que os componentes do Wave 2 possam importar as libs e ter seus estilos aplicados.

Purpose: Sem as dependências instaladas e o plugin configurado, MarkdownViewer e InfoPanel não compilam nem renderizam corretamente.
Output: package.json atualizado, globals.css com typography plugin + KaTeX CSS + prose overrides alinhados ao design system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-reading-viewer/03-CONTEXT.md
@.planning/phases/03-reading-viewer/03-RESEARCH.md

@src/app/globals.css
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Instalar dependências do pipeline de renderização</name>
  <files>package.json</files>
  <read_first>
    - package.json (estado atual das dependências — para não duplicar nem conflitar)
    - .planning/phases/03-reading-viewer/03-RESEARCH.md §Standard Stack (versões verificadas: react-markdown@10.1.0, remark-gfm@4.0.1, remark-math@6.0.0, rehype-katex@7.0.1, @shikijs/rehype@4.0.2, shiki@4.0.2, @tailwindcss/typography@0.5.19)
  </read_first>
  <action>
Executar instalação de pacotes via npm. Usar as versões verificadas no RESEARCH.md.

**Dependências de runtime (não devDependencies):**
```bash
npm install react-markdown@10.1.0 remark-gfm@4.0.1 remark-math@6.0.0 rehype-katex@7.0.1 @shikijs/rehype@4.0.2 shiki@4.0.2
```

**Dev dependency (apenas para compilação — o plugin roda no build):**
```bash
npm install -D @tailwindcss/typography@0.5.19
```

Após instalação, verificar que nenhum peer dependency warning crítico foi emitido para @tailwindcss/typography (peer: tailwindcss>=4.0.0-beta.1 — satisfeito pela versão atual no projeto).

NÃO modificar manualmente o package.json — usar npm install para garantir que package-lock.json seja atualizado.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && node -e "const p=require('./package.json'); const deps={...p.dependencies,...p.devDependencies}; ['react-markdown','remark-gfm','remark-math','rehype-katex','@shikijs/rehype','shiki','@tailwindcss/typography'].forEach(k=>{ if(!deps[k]) throw new Error('MISSING: '+k); console.log('OK:',k,deps[k]) })"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contém "react-markdown" (versão ~10.x)
    - package.json dependencies contém "remark-gfm" (versão ~4.x)
    - package.json dependencies contém "remark-math" (versão ~6.x)
    - package.json dependencies contém "rehype-katex" (versão ~7.x)
    - package.json dependencies contém "@shikijs/rehype" (versão ~4.x)
    - package.json dependencies contém "shiki" (versão ~4.x)
    - package.json devDependencies contém "@tailwindcss/typography" (versão ~0.5.x)
    - node_modules/react-markdown existe
    - node_modules/@shikijs/rehype existe
    - npm install termina sem erros (exit code 0)
  </acceptance_criteria>
  <done>Todos os 7 pacotes instalados com sucesso; package.json e package-lock.json atualizados; nenhum erro de peer dependency</done>
</task>

<task type="auto">
  <name>Tarefa 2: Configurar globals.css com typography plugin, KaTeX CSS e prose overrides</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (arquivo atual completo — não sobrescrever configurações existentes)
    - .planning/phases/03-reading-viewer/03-RESEARCH.md §Padrão 3 (typography com Tailwind v4), §Padrão 4 (KaTeX CSS)
    - DESIGN.md §2 (tokens de cor: on_surface=#2b3437, tertiary=#0055d7, outline-variant=#c8cfd1, surface-container-low=#f1f4f6, on-surface)
  </read_first>
  <action>
Adicionar três blocos ao globals.css, após os imports existentes (`@import "tailwindcss"`, `@import "tw-animate-css"`, `@import "shadcn/tailwind.css"`):

**1. Import do KaTeX CSS** (logo após os @imports existentes, antes do @theme):
```css
@import "katex/dist/katex.min.css";
```

**2. Plugin do typography** (após @import "katex/dist/katex.min.css"):
```css
@plugin "@tailwindcss/typography";
```

**3. Overrides das variáveis prose** (dentro de um bloco @layer utilities, após o @plugin):
```css
@layer utilities {
  .prose {
    --tw-prose-body: var(--color-on-surface);
    --tw-prose-headings: var(--color-on-surface);
    --tw-prose-links: var(--color-tertiary);
    --tw-prose-bold: var(--color-on-surface);
    --tw-prose-counters: color-mix(in srgb, var(--color-on-surface) 60%, transparent);
    --tw-prose-bullets: color-mix(in srgb, var(--color-on-surface) 40%, transparent);
    --tw-prose-hr: var(--color-outline-variant);
    --tw-prose-quotes: var(--color-on-surface);
    --tw-prose-quote-borders: var(--color-outline-variant);
    --tw-prose-captions: color-mix(in srgb, var(--color-on-surface) 60%, transparent);
    --tw-prose-code: var(--color-on-surface);
    --tw-prose-pre-code: var(--color-on-surface);
    --tw-prose-pre-bg: var(--color-surface-container-low);
    --tw-prose-th-borders: var(--color-outline-variant);
    --tw-prose-td-borders: var(--color-outline-variant);
  }
}
```

**Posicionamento correto no arquivo:**
- `@import "katex/dist/katex.min.css"` → imediatamente após `@import "shadcn/tailwind.css"`, antes do bloco `@theme`
- `@plugin "@tailwindcss/typography"` → logo após o @import do KaTeX
- O bloco `@layer utilities { .prose { ... } }` → após o @plugin, antes do bloco `.glass`

**NÃO remover** nada do arquivo existente: @theme, .glass, .gradient-cta, .rail-scroll, @theme inline, :root, .dark, @layer base.

**Verificação de posição:** O arquivo deve manter a ordem: imports globais → @theme → .glass → .gradient-cta → .rail-scroll → @theme inline → :root → .dark → @layer base.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && grep -n "@import \"katex" src/app/globals.css && grep -n "@plugin \"@tailwindcss/typography\"" src/app/globals.css && grep -n "\-\-tw-prose-body" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@import \"katex/dist/katex.min.css\"" src/app/globals.css` encontra exatamente 1 ocorrência
    - `grep "@plugin \"@tailwindcss/typography\"" src/app/globals.css` encontra exatamente 1 ocorrência
    - `grep "\-\-tw-prose-body" src/app/globals.css` encontra exatamente 1 ocorrência com valor `var(--color-on-surface)`
    - `grep "\-\-tw-prose-links" src/app/globals.css` encontra exatamente 1 ocorrência com valor `var(--color-tertiary)`
    - `grep "\-\-tw-prose-pre-bg" src/app/globals.css` encontra exatamente 1 ocorrência com valor `var(--color-surface-container-low)`
    - O @theme block com as variáveis de cor existentes (--color-surface, --color-tertiary, etc.) permanece intacto
    - O .glass helper permanece intacto
    - npm run build (ou npm run typecheck) não emite erro relacionado a CSS
  </acceptance_criteria>
  <done>globals.css configurado com KaTeX CSS, typography plugin e prose overrides com tokens do design system; arquivo existente não danificado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | Versões pinadas explicitamente — sem range aberto que puxe breaking change |
| globals.css → browser | CSS global sem input de usuário — sem surface de ataque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02-01 | Tampering | package.json | accept | Versões pinadas no RESEARCH.md; npm install determina resolução — nenhum dado de usuário envolvido |
| T-3-02-02 | Information Disclosure | globals.css | accept | CSS global sem tokens secretos; prose overrides usam variáveis CSS públicas |
</threat_model>

<verification>
Após conclusão deste plano:
- `node -e "require('./package.json')"` mostra todos os 7 pacotes
- `grep "katex" src/app/globals.css` retorna 1 linha com o @import
- `grep "typography" src/app/globals.css` retorna 1 linha com o @plugin
- `grep "tw-prose-body" src/app/globals.css` retorna override com var(--color-on-surface)
- `npm test` continua passando nos testes existentes (sem regressão)
</verification>

<success_criteria>
- Todos os 7 pacotes do pipeline instalados (react-markdown, remark-gfm, remark-math, rehype-katex, @shikijs/rehype, shiki, @tailwindcss/typography)
- globals.css tem KaTeX CSS, typography plugin e prose overrides com tokens do DESIGN.md
- Nenhum arquivo de componente criado neste plano
- npm test existente continua verde
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-reading-viewer/03-02-SUMMARY.md`
</output>
