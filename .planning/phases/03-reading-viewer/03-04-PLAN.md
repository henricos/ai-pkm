---
phase: 03-reading-viewer
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - src/components/viewer/markdown-viewer.tsx
  - src/components/viewer/viewer-header.tsx
  - src/app/api/pkm/raw/[...path]/route.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03, VIEW-08, CTX-01, CTX-02, RUN-04]

must_haves:
  truths:
    - "MarkdownViewer é um Server Component assíncrono que usa MarkdownAsync (não Markdown síncrono)"
    - "MarkdownViewer aplica classe prose e prose-sm ao article container"
    - "Links externos em Markdown recebem target=_blank e rel=noopener noreferrer"
    - "Links internos não recebem target=_blank"
    - "ViewerHeader é um Client Component que exibe tópico › grupo no estilo label-sm uppercase"
    - "Header fica sticky e aplica classe glass ao rolar (isScrolled=true)"
    - "Botão de apresentação existe e está desabilitado"
    - "Botão de download aponta para /api/pkm/raw/{itemId}"
    - "Botão ℹ️ chama onTogglePanel e reflete panelOpen via aria-pressed"
    - "GET /api/pkm/raw/[...path] retorna 401 quando não autenticado"
    - "GET /api/pkm/raw/[...path] retorna arquivo com Content-Disposition: attachment quando autenticado"
    - "npm test -- markdown-viewer passa (RED → GREEN)"
    - "npm test -- viewer-header passa (RED → GREEN)"
    - "npm test -- raw-route passa (RED → GREEN)"
  artifacts:
    - path: "src/components/viewer/markdown-viewer.tsx"
      provides: "Server Component async com MarkdownAsync + plugins"
      exports: ["MarkdownViewer"]
    - path: "src/components/viewer/viewer-header.tsx"
      provides: "Client Component sticky com glassmorphism e ações"
      exports: ["ViewerHeader"]
    - path: "src/app/api/pkm/raw/[...path]/route.ts"
      provides: "Route Handler autenticado para download de arquivo raw"
      exports: ["GET"]
  key_links:
    - from: "src/components/viewer/viewer-header.tsx"
      to: "document.getElementById('viewer-scroll')"
      via: "useEffect scroll listener — id fixo do container de scroll"
    - from: "src/components/viewer/markdown-viewer.tsx"
      to: "MarkdownAsync"
      via: "import { MarkdownAsync } from 'react-markdown' — NÃO o default export"
    - from: "src/app/api/pkm/raw/[...path]/route.ts"
      to: "auth()"
      via: "guard obrigatório antes de qualquer leitura de filesystem (T-3-04)"
---

<objective>
Criar os dois componentes visuais principais da fase: MarkdownViewer (Server Component que renderiza Markdown rico com Shiki + KaTeX) e ViewerHeader (Client Component com header sticky glassmorphism + ações CTX-02). Também criar o Route Handler `/api/pkm/raw/[...path]` que serve o arquivo raw para download autenticado (CTX-02, T-3-04). Estes componentes são consumidos pelo ViewerPage no plano seguinte.

Purpose: MarkdownViewer entrega VIEW-01, VIEW-02, VIEW-03, VIEW-08 (renderização rica, composição de leitura). ViewerHeader entrega CTX-01 (contexto estrutural), CTX-02 (ações), D-10 (glassmorphism), D-11 (tópico › grupo), D-12 (download, apresentação, info), D-13 (slot de tema). A rota raw entrega o download seguro que o ViewerHeader referencia via href.
Output: Dois componentes na pasta src/components/viewer/ e um Route Handler em src/app/api/pkm/raw/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-reading-viewer/03-CONTEXT.md
@.planning/phases/03-reading-viewer/03-RESEARCH.md
@DESIGN.md

@src/app/globals.css
@src/lib/pkm/types.ts
@src/__tests__/markdown-viewer.test.tsx
@src/__tests__/viewer-header.test.tsx
@src/__tests__/raw-route.test.ts

<interfaces>
<!-- Contrato do MarkdownViewer -->
```typescript
// Server Component — sem "use client"
interface MarkdownViewerProps {
  content: string;   // Markdown puro sem frontmatter (de getItemContent)
}
export async function MarkdownViewer({ content }: MarkdownViewerProps): Promise<JSX.Element>
```

<!-- Contrato do ViewerHeader -->
```typescript
// Client Component — "use client"
interface ViewerHeaderProps {
  topic: string;           // ex: "tecnologia" ou "INBOX" para inbox items
  group?: string;          // ex: "superapp" (opcional)
  estado: "rascunho" | "finalizado";
  itemId: string;          // ID do item — para montar URL de download
  panelOpen: boolean;      // estado do painel de informações
  onTogglePanel: () => void;
}
export function ViewerHeader(props: ViewerHeaderProps): JSX.Element
```

<!-- Tokens de cor usados — do globals.css @theme -->
<!-- --color-on-surface: #2b3437 -->
<!-- --color-surface: #f8f9fa -->
<!-- --color-surface-container-lowest: #ffffff -->
<!-- --color-outline-variant: #c8cfd1 -->
<!-- --color-primary-container: #dde5f5 -->
<!-- --color-on-primary-container: #0a2456 -->
<!-- --color-surface-container: #eaeff1 -->
<!-- .glass { background: color-mix(surface 70%, transparent); backdrop-filter: blur(12px); } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: MarkdownViewer — Server Component com MarkdownAsync + plugins</name>
  <files>src/components/viewer/markdown-viewer.tsx</files>
  <read_first>
    - src/__tests__/markdown-viewer.test.tsx (testes RED criados no Wave 0 — implementar contra esses contratos)
    - .planning/phases/03-reading-viewer/03-RESEARCH.md §Padrão 1 (código exato do MarkdownAsync + plugins), §Anti-Patterns (MarkdownAsync obrigatório, não Markdown síncrono)
    - src/app/globals.css (verificar que @plugin typography já está lá — confirmar antes de usar classe prose)
    - DESIGN.md §3 (tipografia: prose-sm = body-md 14px; max-w-prose; Technical Journal rhythm)
  </read_first>
  <behavior>
    - Render sem crash com conteúdo Markdown simples
    - article container tem className contendo "prose" e "prose-sm"
    - Link externo https:// → target="_blank" + rel="noopener noreferrer"
    - Link interno /caminho → sem target="_blank"
    - Task lists renderizadas como checkboxes HTML (remark-gfm)
  </behavior>
  <action>
Criar o diretório `src/components/viewer/` se não existir.

Criar `src/components/viewer/markdown-viewer.tsx` com o seguinte conteúdo **exato** (Server Component — sem "use client"):

```typescript
/**
 * MarkdownViewer — Server Component assíncrono (Phase 3, VIEW-01, VIEW-02, VIEW-03, VIEW-08)
 *
 * Usa MarkdownAsync (não Markdown síncrono) porque @shikijs/rehype é plugin async.
 * O highlighting é produzido no servidor — zero JS de highlighting no bundle do cliente.
 *
 * Segurança (T-3-02): react-markdown sanitiza por padrão — defaultUrlTransform
 * rejeita URIs javascript:. NÃO substituir por rehype-raw.
 *
 * Decisões (03-CONTEXT.md):
 * - D-01: react-markdown + remark-gfm
 * - D-02: Shiki (github-light theme)
 * - D-03: KaTeX (remark-math + rehype-katex)
 * - D-04: Links externos → nova aba; internos → shell
 * - D-05: Callouts sem tratamento especial (blockquote padrão)
 * - D-06: prose-sm base de estilo
 * - D-07: max-w-prose alinhado à esquerda
 */

import { MarkdownAsync } from "react-markdown";
import remarkGfm from "remark-gfm";
import remarkMath from "remark-math";
import rehypeKatex from "rehype-katex";
import rehypeShiki from "@shikijs/rehype";

interface MarkdownViewerProps {
  content: string;
}

export async function MarkdownViewer({ content }: MarkdownViewerProps) {
  return (
    <article
      className="prose prose-sm max-w-prose px-8 py-10 bg-surface-container-lowest"
      data-testid="markdown-content"
    >
      <MarkdownAsync
        remarkPlugins={[remarkGfm, remarkMath]}
        rehypePlugins={[
          rehypeKatex,
          [rehypeShiki, { theme: "github-light" }],
        ]}
        components={{
          a({ href, children, ...props }) {
            const isExternal = href?.startsWith("http://") || href?.startsWith("https://");
            return (
              <a
                href={href}
                {...(isExternal
                  ? { target: "_blank", rel: "noopener noreferrer" }
                  : {})}
                {...props}
              >
                {children}
              </a>
            );
          },
        }}
      >
        {content}
      </MarkdownAsync>
    </article>
  );
}
```

**Notas de implementação:**
- NÃO adicionar "use client" — o Server Component assíncrono é crítico para o highlighting Shiki funcionar sem JS no cliente
- O `data-testid="markdown-content"` permite que os testes de Wave 0 encontrem o container
- A classe `bg-surface-container-lowest` aplica fundo branco (#ffffff) conforme D-09
- O `max-w-prose` (~65ch) e `px-8 py-10` seguem D-07 e o ritmo do DESIGN.md "Technical Journal"
- Para RUN-04 (responsividade): max-w-prose cria coluna estreita que funciona em viewports móveis sem scroll horizontal
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm test -- markdown-viewer 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/viewer/markdown-viewer.tsx existe
    - `grep "MarkdownAsync" src/components/viewer/markdown-viewer.tsx` encontra 1 ocorrência (não "Markdown" síncrono)
    - `grep "\"use client\"" src/components/viewer/markdown-viewer.tsx` NÃO encontra ocorrência (Server Component)
    - `grep "prose prose-sm" src/components/viewer/markdown-viewer.tsx` encontra 1 ocorrência no article
    - `grep "max-w-prose" src/components/viewer/markdown-viewer.tsx` encontra 1 ocorrência
    - `grep "rehypeShiki" src/components/viewer/markdown-viewer.tsx` encontra import e uso
    - `grep "rehypeKatex" src/components/viewer/markdown-viewer.tsx` encontra import e uso
    - `grep "target.*_blank" src/components/viewer/markdown-viewer.tsx` encontra no componente "a" customizado
    - `grep "noopener noreferrer" src/components/viewer/markdown-viewer.tsx` encontra 1 ocorrência
    - `npm test -- markdown-viewer` passa (GREEN — stubs de Wave 0 agora encontram o componente)
    - npm run typecheck sem erros neste arquivo
  </acceptance_criteria>
  <done>MarkdownViewer Server Component criado com MarkdownAsync + Shiki + KaTeX + GFM; links externos em nova aba; prose-sm aplicado; testes de markdown-viewer verdes</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: ViewerHeader — Client Component sticky com glassmorphism e ações</name>
  <files>src/components/viewer/viewer-header.tsx</files>
  <read_first>
    - src/__tests__/viewer-header.test.tsx (testes RED de Wave 0 — implementar contra esses contratos)
    - .planning/phases/03-reading-viewer/03-RESEARCH.md §Padrão 5 (código do ViewerHeader com isScrolled, .glass, ações)
    - src/app/globals.css (classe .glass: surface 70% + blur(12px); shadow-ambient definido em @theme)
    - DESIGN.md §3 (label-sm: 0.6875rem font-semibold uppercase tracking +0.05em), §5 Chips (surface-container-high, label-md)
    - src/components/shell/workspace-item-state.tsx (padrão de chip de estado já estabelecido em Phase 2)
  </read_first>
  <behavior>
    - Exibe topic e group no estilo label-sm uppercase
    - topic="__inbox" → exibe "INBOX" (não o valor bruto)
    - itemId é URL-encoded para montar href do download: `/api/pkm/raw/${encodeURIComponent(itemId)}`
    - Botão de apresentação está presente e disabled (aria-disabled="true" ou disabled attr)
    - Botão ℹ️ tem aria-pressed={panelOpen}; ao clicar chama onTogglePanel
    - Div placeholder de 32px reservada para futuro botão de tema (D-13)
    - Ao rolar (isScrolled=true), header recebe classe "glass shadow-ambient"; sem scroll fica "bg-surface-container-lowest"
  </behavior>
  <action>
Criar `src/components/viewer/viewer-header.tsx`:

```typescript
/**
 * ViewerHeader — Client Component (Phase 3, CTX-01 revisado, CTX-02)
 *
 * Header sticky do viewer:
 * - Esquerda: tópico › grupo (label-sm uppercase) + chip de estado
 * - Direita: [slot tema Phase 5] + [apresentação desabilitado] + [download] + [ℹ️ painel]
 *
 * Glassmorphism ao rolar (D-10): ouve scroll no elemento #viewer-scroll
 * (id fixo do container de scroll definido em ViewerPage — 03-05-PLAN.md)
 *
 * Decisões (03-CONTEXT.md):
 * - D-10: sticky + glassmorphism ao rolar
 * - D-11: tópico › grupo, não filename
 * - D-12: download raw, apresentação (disabled), toggle ℹ️
 * - D-13: div placeholder para tema (Phase 5)
 */

"use client";

import { useState, useEffect } from "react";

interface ViewerHeaderProps {
  topic: string;
  group?: string;
  estado: "rascunho" | "finalizado";
  itemId: string;
  panelOpen: boolean;
  onTogglePanel: () => void;
}

export function ViewerHeader({
  topic,
  group,
  estado,
  itemId,
  panelOpen,
  onTogglePanel,
}: ViewerHeaderProps) {
  const [isScrolled, setIsScrolled] = useState(false);

  useEffect(() => {
    const el = document.getElementById("viewer-scroll");
    if (!el) return;
    const handler = () => setIsScrolled(el.scrollTop > 8);
    el.addEventListener("scroll", handler, { passive: true });
    return () => el.removeEventListener("scroll", handler);
  }, []);

  // D-11: __inbox exibe "INBOX"; demais tópicos são formatados com inicial maiúscula
  const topicLabel = topic === "__inbox"
    ? "INBOX"
    : topic.replace(/-/g, " ").replace(/\b\w/g, (c) => c.toUpperCase());
  const groupLabel = group
    ? group.replace(/-/g, " ").replace(/\b\w/g, (c) => c.toUpperCase())
    : undefined;

  return (
    <header
      className={[
        "sticky top-0 z-10 flex items-center justify-between h-11 px-8 transition-all duration-150",
        isScrolled ? "glass shadow-ambient" : "bg-surface-container-lowest",
      ].join(" ")}
      data-testid="viewer-header"
    >
      {/* Esquerda: contexto estrutural */}
      <div className="flex items-center gap-1.5 min-w-0">
        <span className="text-[0.6875rem] font-semibold uppercase tracking-[0.05em] text-on-surface/40 truncate">
          {topicLabel}
        </span>
        {groupLabel && (
          <>
            <span className="text-on-surface/25 text-[0.6875rem]" aria-hidden="true">›</span>
            <span className="text-[0.6875rem] font-semibold uppercase tracking-[0.05em] text-on-surface/40 truncate">
              {groupLabel}
            </span>
          </>
        )}
        {/* Chip de estado — consistente com Phase 2 (workspace-item-state.tsx) */}
        <span
          className={[
            "ml-2 inline-flex items-center px-2 py-0.5 rounded-sm text-[0.6875rem] font-semibold uppercase tracking-[0.05em]",
            estado === "finalizado"
              ? "bg-primary-container text-on-primary-container"
              : "bg-surface-container text-on-surface/50",
          ].join(" ")}
          aria-label={`Estado: ${estado}`}
        >
          {estado}
        </span>
      </div>

      {/* Direita: ações */}
      <div className="flex items-center gap-1 shrink-0">
        {/* D-13: Slot reservado para tema — Phase 5 */}
        <div className="w-8 h-8" aria-hidden="true" data-slot="theme-button-phase5" />

        {/* D-12: Botão de apresentação — desabilitado (Phase 5 implementa) */}
        <button
          type="button"
          disabled
          aria-disabled="true"
          aria-label="Modo apresentação (disponível na Phase 5)"
          title="Modo apresentação — disponível em breve"
          className="flex items-center justify-center w-8 h-8 rounded-sm text-on-surface/25 cursor-not-allowed"
        >
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <rect x="1" y="2" width="14" height="10" rx="1" stroke="currentColor" strokeWidth="1.5" />
            <path d="M5 14H11" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
            <path d="M8 12V14" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
          </svg>
        </button>

        {/* D-12: Download do arquivo raw — autenticado via /api/pkm/raw/ */}
        <a
          href={`/api/pkm/raw/${encodeURIComponent(itemId)}`}
          download
          aria-label="Baixar arquivo raw"
          title="Baixar arquivo raw"
          className="flex items-center justify-center w-8 h-8 rounded-sm text-on-surface/50 hover:text-on-surface hover:bg-surface-container transition-colors"
          data-testid="download-link"
        >
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M8 2V10M8 10L5 7M8 10L11 7" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
            <path d="M2 13H14" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
          </svg>
        </a>

        {/* D-12: Toggle do painel de informações */}
        <button
          type="button"
          onClick={onTogglePanel}
          aria-pressed={panelOpen}
          aria-label={panelOpen ? "Fechar painel de informações" : "Abrir painel de informações"}
          title="Painel de informações"
          className={[
            "flex items-center justify-center w-8 h-8 rounded-sm transition-colors",
            panelOpen
              ? "bg-primary-container text-on-primary-container"
              : "text-on-surface/50 hover:text-on-surface hover:bg-surface-container",
          ].join(" ")}
          data-testid="toggle-panel-button"
        >
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <circle cx="8" cy="8" r="6" stroke="currentColor" strokeWidth="1.5" />
            <path d="M8 7V11" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
            <circle cx="8" cy="5" r="0.75" fill="currentColor" />
          </svg>
        </button>
      </div>
    </header>
  );
}
```

**Notas de implementação:**
- O `id="viewer-scroll"` é o elemento que o ViewerHeader escuta — esse ID será colocado no container de scroll pelo ViewerPage (03-05-PLAN.md). Os dois devem usar exatamente o mesmo ID.
- O `{ passive: true }` no addEventListener evita warning de performance
- O link de download usa `encodeURIComponent(itemId)` — o itemId já é um path relativo (ex: "tecnologia/nota.md"), não um path absoluto (T-3-03)
- Para RUN-04 (responsividade): em viewport estreito, o groupLabel pode ser truncado (classe truncate no span)
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm test -- viewer-header 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/viewer/viewer-header.tsx existe
    - `grep "\"use client\"" src/components/viewer/viewer-header.tsx` encontra 1 ocorrência (primeira linha)
    - `grep "sticky" src/components/viewer/viewer-header.tsx` encontra classe sticky no header
    - `grep "glass" src/components/viewer/viewer-header.tsx` encontra uso da classe .glass
    - `grep "encodeURIComponent" src/components/viewer/viewer-header.tsx` encontra 1 ocorrência na href do download
    - `grep "aria-pressed" src/components/viewer/viewer-header.tsx` encontra no botão de toggle
    - `grep "disabled" src/components/viewer/viewer-header.tsx` encontra no botão de apresentação
    - `grep "data-slot=\"theme-button-phase5\"" src/components/viewer/viewer-header.tsx` encontra o slot reservado
    - `grep "viewer-scroll" src/components/viewer/viewer-header.tsx` encontra o ID no useEffect (getElementById)
    - `npm test -- viewer-header` passa (GREEN — stubs de Wave 0 agora encontram o componente)
    - npm run typecheck sem erros neste arquivo
  </acceptance_criteria>
  <done>ViewerHeader Client Component criado com sticky glassmorphism, tópico›grupo, chip de estado, slot de tema, botão de apresentação desabilitado, link de download e toggle do painel; testes viewer-header verdes</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: Route Handler /api/pkm/raw/[...path] — download autenticado (CTX-02, T-3-04)</name>
  <files>src/app/api/pkm/raw/[...path]/route.ts</files>
  <read_first>
    - src/__tests__/raw-route.test.ts (testes RED de Wave 0 — implementar contra esses contratos)
    - src/lib/pkm/fs-item-repository.ts (getItemContent — para leitura do arquivo, já com resolveAndValidatePath)
    - src/lib/auth.ts (ou equivalente — importar auth() do mesmo path usado no restante do projeto)
    - .planning/phases/03-reading-viewer/03-VALIDATION.md §T-3-04 (auth guard obrigatório)
  </read_first>
  <behavior>
    - Sem sessão autenticada (auth() retorna null) → resposta 401 sem corpo
    - Com sessão autenticada e path válido → resposta 200 com Content-Disposition: attachment; filename="{filename}"
    - Path com traversal ("../etc/passwd") → resolveAndValidatePath lança erro → resposta 400
    - Path inexistente → resposta 404
  </behavior>
  <action>
Criar o diretório `src/app/api/pkm/raw/[...path]/` e o arquivo `route.ts`:

```typescript
/**
 * GET /api/pkm/raw/[...path]
 *
 * Serve o conteúdo raw de um arquivo do PKM para download autenticado.
 * Referenciado pelo ViewerHeader como href do botão de download (CTX-02).
 *
 * Segurança (T-3-04):
 * - auth() guard obrigatório: retorna 401 imediatamente se não autenticado
 * - resolveAndValidatePath() interno ao FsItemRepository previne path traversal
 *
 * O parâmetro [...path] captura paths multi-segmento como "tecnologia/nota.md"
 * (ViewerHeader usa encodeURIComponent no itemId inteiro, não por segmento).
 */

import { type NextRequest, NextResponse } from "next/server";
import { auth } from "@/lib/auth";
import { FsItemRepository } from "@/lib/pkm/fs-item-repository";
import path from "path";

export async function GET(
  request: NextRequest,
  { params }: { params: Promise<{ path: string[] }> }
) {
  // T-3-04: verificar autenticação antes de qualquer operação de filesystem
  const session = await auth();
  if (!session) {
    return new NextResponse(null, { status: 401 });
  }

  const resolvedParams = await params;
  // Reconstruir o itemId a partir dos segmentos do path
  // (o encodeURIComponent no ViewerHeader codifica o itemId inteiro — Next.js decodifica automaticamente)
  const itemId = resolvedParams.path.join("/");

  try {
    const repo = new FsItemRepository();
    const content = repo.getItemContent(itemId);

    if (!content && content !== "") {
      return new NextResponse(null, { status: 404 });
    }

    const filename = path.basename(itemId);

    return new NextResponse(content, {
      status: 200,
      headers: {
        "Content-Type": "text/plain; charset=utf-8",
        "Content-Disposition": `attachment; filename="${filename}"`,
        // Previne que browsers interpretem o conteúdo de forma inesperada
        "X-Content-Type-Options": "nosniff",
      },
    });
  } catch (err) {
    // resolveAndValidatePath lança Error para path traversal
    if (err instanceof Error && err.message.includes("Path traversal")) {
      return new NextResponse(null, { status: 400 });
    }
    // Arquivo não encontrado (getItemContent retorna "" para inexistente — ver FsItemRepository)
    return new NextResponse(null, { status: 404 });
  }
}
```

**Notas de implementação:**
- `auth` deve ser importado do mesmo path usado nos outros Route Handlers do projeto — verificar `src/lib/auth.ts` (ou `@/auth`, dependendo do projeto)
- `FsItemRepository.getItemContent()` já chama `resolveAndValidatePath()` internamente — segundo nível de defesa além do auth guard
- O `filename` usa apenas o basename para evitar expor estrutura de diretórios no header HTTP
- O `X-Content-Type-Options: nosniff` impede que browsers tentem inferir o tipo do arquivo
- Se o projeto usar NextAuth v5 (`import { auth } from "@/auth"`) em vez de `"@/lib/auth"`, ajustar o import — verificar o padrão existente antes de escrever
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm test -- raw-route 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/pkm/raw/[...path]/route.ts existe
    - `grep "auth()" src/app/api/pkm/raw/[...path]/route.ts` encontra a chamada de auth guard
    - `grep "status: 401" src/app/api/pkm/raw/[...path]/route.ts` encontra o retorno 401
    - `grep "Content-Disposition" src/app/api/pkm/raw/[...path]/route.ts` encontra o header de attachment
    - `grep "resolveAndValidatePath\|getItemContent" src/app/api/pkm/raw/[...path]/route.ts` encontra uso do FsItemRepository
    - `npm test -- raw-route` passa (GREEN — stubs de Wave 0 agora encontram o handler)
    - npm run typecheck sem erros neste arquivo
  </acceptance_criteria>
  <done>Route Handler GET /api/pkm/raw/[...path] criado com auth guard T-3-04, path traversal protegido via FsItemRepository, Content-Disposition: attachment para download; testes raw-route verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown content → React JSX | react-markdown transforma string em JSX — sanitização built-in |
| item.id → /api/pkm/raw/ URL | itemId é path relativo, não absoluto — encodeURIComponent evita caracteres especiais |
| External links → new tab | Links https:// abrem nova aba com noopener noreferrer |
| HTTP request → /api/pkm/raw/ | Rota pública — auth() guard obrigatório antes de qualquer leitura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Tampering (XSS) | MarkdownViewer | mitigate | react-markdown sanitiza por padrão; defaultUrlTransform rejeita javascript: URIs. NÃO usar rehype-raw. Verificado em acceptance_criteria: ausência de dangerouslySetInnerHTML |
| T-3-03 | Information Disclosure | ViewerHeader (download link) | mitigate | href usa `encodeURIComponent(itemId)` onde itemId é path relativo (ex: "tecnologia/nota.md") — nunca item.path absoluto. Verificado por grep em acceptance_criteria |
| T-3-04 | Elevation of Privilege | GET /api/pkm/raw/[...path] | mitigate | `auth()` chamado na primeira linha do handler — retorna 401 antes de qualquer leitura de filesystem. Segundo nível: `resolveAndValidatePath()` no FsItemRepository bloqueia path traversal. Verificado por `npm test -- raw-route` |
</threat_model>

<verification>
Após conclusão do plano:
- `npm test -- markdown-viewer` → GREEN (todos os testes passam)
- `npm test -- viewer-header` → GREEN (todos os testes passam)
- `npm test -- raw-route` → GREEN (401 sem auth, 200 com auth)
- `npm run typecheck` → sem erros
- `grep "MarkdownAsync" src/components/viewer/markdown-viewer.tsx` → 1 ocorrência (NÃO "Markdown")
- `grep "\"use client\"" src/components/viewer/markdown-viewer.tsx` → ZERO ocorrências (Server Component)
- `grep "\"use client\"" src/components/viewer/viewer-header.tsx` → 1 ocorrência (Client Component)
- `grep "auth()" src/app/api/pkm/raw/[...path]/route.ts` → 1 ocorrência (guard na primeira linha)
</verification>

<success_criteria>
- MarkdownViewer Server Component assíncrono com MarkdownAsync + Shiki + KaTeX + GFM + link handling
- ViewerHeader Client Component com sticky glassmorphism, tópico›grupo, ações CTX-02, D-13 slot
- Route Handler /api/pkm/raw/[...path] com auth guard, path traversal protegido, Content-Disposition: attachment
- Todos os três testes de Wave 0 passam (RED → GREEN)
- npm run typecheck limpo
- Nenhuma regressão nos testes existentes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-reading-viewer/03-04-SUMMARY.md`
</output>
