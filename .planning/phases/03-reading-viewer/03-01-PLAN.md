---
phase: 03-reading-viewer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/markdown-viewer.test.tsx
  - src/__tests__/viewer-header.test.tsx
  - src/__tests__/info-panel.test.tsx
  - src/__tests__/raw-route.test.ts
autonomous: true
requirements: [VIEW-02, VIEW-08, CTX-01, CTX-02, CTX-03, CTX-04]

must_haves:
  truths:
    - "npm test -- markdown-viewer sai com erro (test failing — RED)"
    - "npm test -- viewer-header sai com erro (test failing — RED)"
    - "npm test -- info-panel sai com erro (test failing — RED)"
    - "npm test -- raw-route sai com erro (test failing — RED)"
  artifacts:
    - path: "src/__tests__/markdown-viewer.test.tsx"
      provides: "Stub de teste para VIEW-02 e VIEW-08"
    - path: "src/__tests__/viewer-header.test.tsx"
      provides: "Stub de teste para CTX-01 e CTX-02"
    - path: "src/__tests__/info-panel.test.tsx"
      provides: "Stub de teste para CTX-03 e CTX-04"
    - path: "src/__tests__/raw-route.test.ts"
      provides: "Stub de teste para CTX-02 (download) e T-3-04 (auth guard)"
  key_links:
    - from: "src/__tests__/markdown-viewer.test.tsx"
      to: "src/components/viewer/markdown-viewer.tsx"
      via: "import — arquivo ainda não existe, provoca RED"
    - from: "src/__tests__/viewer-header.test.tsx"
      to: "src/components/viewer/viewer-header.tsx"
      via: "import — arquivo ainda não existe, provoca RED"
    - from: "src/__tests__/info-panel.test.tsx"
      to: "src/components/viewer/info-panel.tsx"
      via: "import — arquivo ainda não existe, provoca RED"
    - from: "src/__tests__/raw-route.test.ts"
      to: "src/app/api/pkm/raw/[...path]/route.ts"
      via: "import — arquivo ainda não existe, provoca RED"
---

<objective>
Criar stubs de teste antes de qualquer implementação (Nyquist Rule). Quatro arquivos de teste importam componentes/routes que ainda não existem — garantindo que o executor de Wave 2-4 construa contra contratos verificáveis.

Purpose: Conformidade com a estratégia de validação VALIDATION.md (nyquist_compliant). Cada arquivo de teste falha imediatamente ao tentar importar o componente/route inexistente, confirmando RED.
Output: Quatro arquivos de teste com suites descrevendo os comportamentos esperados de VIEW-02, VIEW-08, CTX-01, CTX-02, CTX-03, CTX-04 e T-3-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-reading-viewer/03-CONTEXT.md
@.planning/phases/03-reading-viewer/03-VALIDATION.md

<!-- Padrão de teste existente para referência de estrutura e mocks -->
@src/__tests__/item-repository.test.ts
@src/__tests__/app-shell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Stubs de teste markdown-viewer e viewer-header</name>
  <files>src/__tests__/markdown-viewer.test.tsx, src/__tests__/viewer-header.test.tsx</files>
  <read_first>
    - src/__tests__/app-shell.test.tsx (padrão de imports, vi.mock, render/screen)
    - src/__tests__/item-repository.test.ts (padrão de mocks para env e fs)
    - .planning/phases/03-reading-viewer/03-VALIDATION.md (comportamentos esperados por requirement ID)
    - .planning/phases/03-reading-viewer/03-CONTEXT.md (D-01 a D-11 — o que cada componente deve expor)
  </read_first>
  <action>
Criar dois arquivos de teste com suites RED que importam componentes ainda inexistentes.

**src/__tests__/markdown-viewer.test.tsx**

Cabeçalho de mock necessário (jsdom não processa CSS):
```typescript
vi.mock("react-markdown", () => ({
  MarkdownAsync: ({ children }: { children: string }) => <div data-testid="markdown-content">{children}</div>,
}));
vi.mock("remark-gfm", () => ({ default: () => {} }));
vi.mock("remark-math", () => ({ default: () => {} }));
vi.mock("rehype-katex", () => ({ default: () => {} }));
vi.mock("@shikijs/rehype", () => ({ default: () => {} }));
```

Suites a criar:
- `describe("MarkdownViewer", () => {`
  - `test("VIEW-02: renderiza conteúdo Markdown sem crash", ...)` — render com string simples, expect getByTestId("markdown-content") ou article element
  - `test("VIEW-08: aplica classe prose e prose-sm ao article container", ...)` — render, expect container with className containing "prose"
  - `test("VIEW-02: links externos recebem target=_blank e rel=noopener noreferrer", ...)` — render com conteúdo `[texto](https://externo.com)`, expect anchor com atributos corretos
  - `test("VIEW-02: links internos NÃO recebem target=_blank", ...)` — render com `[texto](/local)`, expect anchor sem target

**src/__tests__/viewer-header.test.tsx**

Stub necessário para next/navigation:
```typescript
vi.mock("next/navigation", () => ({ useRouter: () => ({ push: vi.fn() }) }));
```

Suites a criar:
- `describe("ViewerHeader", () => {`
  - `test("CTX-01: exibe tópico e grupo no cabeçalho (label-sm uppercase)", ...)` — render com props topic="tecnologia" group="superapp", expect both texts present
  - `test("CTX-01: exibe INBOX quando topic é __inbox", ...)` — render com topic="__inbox", expect text "INBOX"
  - `test("CTX-02: botão de download está presente e tem href correto", ...)` — render com itemId="tecnologia/nota.md", expect link href="/api/pkm/raw/tecnologia%2Fnota.md"
  - `test("CTX-02: botão de apresentação está presente e desabilitado", ...)` — expect button[disabled] ou aria-disabled
  - `test("CTX-02: botão de toggle do painel (ℹ️) chama onTogglePanel ao clicar", ...)` — fireEvent.click, expect mock called
  - `test("CTX-03: aria-pressed reflete estado panelOpen", ...)` — render com panelOpen=true/false, check aria-pressed

Ambos os arquivos devem importar do path real do componente:
```typescript
import { MarkdownViewer } from "@/components/viewer/markdown-viewer";
import { ViewerHeader } from "@/components/viewer/viewer-header";
```

Os componentes não existem ainda — o import vai causar erro de módulo → RED confirmado.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm test -- markdown-viewer 2>&1 | grep -E "(FAIL|Cannot find|Module not found|Error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/markdown-viewer.test.tsx existe
    - src/__tests__/viewer-header.test.tsx existe
    - Cada arquivo contém pelo menos 3 testes com descrições que referenciam os IDs de requisitos (VIEW-02, VIEW-08, CTX-01, CTX-02, CTX-03)
    - `npm test -- markdown-viewer` falha com erro de import (Cannot find module '@/components/viewer/markdown-viewer')
    - `npm test -- viewer-header` falha com erro de import (Cannot find module '@/components/viewer/viewer-header')
    - Os imports são `@/components/viewer/markdown-viewer` e `@/components/viewer/viewer-header` (paths exatos que os planos posteriores criarão)
  </acceptance_criteria>
  <done>Dois arquivos de teste RED existem; imports falham com module not found; suites descrevem comportamentos dos requisitos VIEW-02, VIEW-08, CTX-01, CTX-02, CTX-03</done>
</task>

<task type="auto">
  <name>Tarefa 2: Stub de teste info-panel</name>
  <files>src/__tests__/info-panel.test.tsx</files>
  <read_first>
    - src/__tests__/app-shell.test.tsx (padrão de render, fireEvent, screen)
    - .planning/phases/03-reading-viewer/03-CONTEXT.md (D-14 a D-18 — campos e comportamentos do painel)
    - reference/schemas/frontmatter-item.md (campos: estado, modelo, data_captura, data_publicacao, url, autores)
  </read_first>
  <action>
Criar src/__tests__/info-panel.test.tsx com suites RED.

Import do componente real (ainda inexistente):
```typescript
import { InfoPanel } from "@/components/viewer/info-panel";
```

Mock de next/navigation se necessário:
```typescript
vi.mock("next/navigation", () => ({ useRouter: () => ({ push: vi.fn() }) }));
```

Suites a criar dentro de `describe("InfoPanel", () => {`:

**CTX-03 — toggle e Escape:**
- `test("CTX-03: painel visível quando panelOpen=true", ...)` — render com panelOpen=true, expect aside element present (ou data-testid="info-panel")
- `test("CTX-04: chip de estado exibe 'rascunho' ou 'finalizado'", ...)` — render com frontmatter={estado:"rascunho",...}, expect text "rascunho"
- `test("CTX-04: data_captura formatada como '7 mar 2026' a partir de '2026-03-07'", ...)` — render com frontmatter={data_captura:"2026-03-07",...}, expect text matching /7.*mar.*2026/i
- `test("CTX-04: campos ausentes (autores) são omitidos completamente", ...)` — render sem autores, expect queryByText(/autores/i) to be null
- `test("CTX-04: url exibida como link clicável quando presente", ...)` — render com frontmatter={url:"https://exemplo.com",...}, expect anchor with href="https://exemplo.com"
- `test("CTX-04: autores exibidos como chips quando presentes", ...)` — render com frontmatter={autores:["João"],...}, expect text "João"
- `test("CTX-04: data_publicacao formatada — ano-mês: 'nov. 2025' a partir de '2025-11'", ...)` — render com data_publicacao:"2025-11", expect text matching /nov.*2025/i

Props esperadas pelo InfoPanel (devem constar no import e no render do teste):
```typescript
interface InfoPanelProps {
  panelOpen: boolean;
  frontmatter: RawFrontmatter;
  topic: string;
  group?: string;
}
```

O arquivo deve importar também o tipo RawFrontmatter:
```typescript
import type { RawFrontmatter } from "@/lib/pkm/types";
```

Ambos os imports causarão erro de módulo → RED confirmado (RawFrontmatter não existe em types.ts ainda).
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm test -- info-panel 2>&1 | grep -E "(FAIL|Cannot find|Module not found|Error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/info-panel.test.tsx existe
    - Arquivo contém pelo menos 6 testes descrevendo CTX-03 e CTX-04
    - `npm test -- info-panel` falha com erro de import (Cannot find module '@/components/viewer/info-panel' ou '@/lib/pkm/types')
    - Testes descrevem: toggle de visibilidade, chips de estado, formatação de data_captura, omissão de campos ausentes, url como link, chips de autores, formatação de data_publicacao
    - O import de InfoPanel usa o path exato `@/components/viewer/info-panel`
  </acceptance_criteria>
  <done>Arquivo info-panel.test.tsx RED existe; importa InfoPanel e RawFrontmatter de paths que não existem ainda; suites descrevem CTX-03 e CTX-04 em detalhes testáveis</done>
</task>

<task type="auto">
  <name>Tarefa 3: Stub de teste raw-route (T-3-04, CTX-02)</name>
  <files>src/__tests__/raw-route.test.ts</files>
  <read_first>
    - src/__tests__/item-repository.test.ts (padrão de mocks para módulos Next.js e auth)
    - .planning/phases/03-reading-viewer/03-VALIDATION.md (task 3-download: CTX-02, T-3-04)
    - .planning/phases/03-reading-viewer/03-CONTEXT.md (CTX-02 — download raw autenticado)
  </read_first>
  <action>
Criar src/__tests__/raw-route.test.ts com suites RED que importam o Route Handler ainda inexistente.

Import do handler real (ainda inexistente):
```typescript
import { GET } from "@/app/api/pkm/raw/[...path]/route";
```

Mocks necessários:
```typescript
vi.mock("@/lib/auth", () => ({
  auth: vi.fn(),
}));
vi.mock("@/lib/pkm/fs-item-repository", () => ({
  FsItemRepository: vi.fn().mockImplementation(() => ({
    getItemContent: vi.fn().mockReturnValue("# Conteúdo"),
  })),
}));
```

Suites a criar dentro de `describe("GET /api/pkm/raw/[...path]", () => {`:
- `test("T-3-04: retorna 401 quando não autenticado", ...)` — mock auth() para retornar null, espera Response com status 401
- `test("CTX-02: retorna 200 com conteúdo quando autenticado", ...)` — mock auth() para retornar { user: { id: "1" } }, espera Response com status 200 e Content-Disposition contendo "attachment"

O import causará erro de módulo → RED confirmado (route ainda não existe).
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm test -- raw-route 2>&1 | grep -E "(FAIL|Cannot find|Module not found|Error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/raw-route.test.ts existe
    - Arquivo contém 2 testes descrevendo T-3-04 (auth guard) e CTX-02 (download autenticado)
    - `npm test -- raw-route` falha com erro de import (Cannot find module '@/app/api/pkm/raw/[...path]/route')
    - O import usa o path exato `@/app/api/pkm/raw/[...path]/route`
  </acceptance_criteria>
  <done>Arquivo raw-route.test.ts RED existe; importa GET do route handler que ainda não existe; suites descrevem auth guard (401) e download autenticado (200)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test ↔ Component | Mocks controlam imports externos — sem dados reais ou filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-W0-01 | Tampering | test mocks | accept | Wave 0 não toca dados reais; mocks são controlados e sem efeito colateral |
</threat_model>

<verification>
Após criação dos quatro stubs:
- `npm test -- markdown-viewer` → FAIL (import error)
- `npm test -- viewer-header` → FAIL (import error)
- `npm test -- info-panel` → FAIL (import error)
- `npm test -- raw-route` → FAIL (import error)

Todos os testes que falham por import-not-found são VALID RED (conformidade Nyquist).
</verification>

<success_criteria>
- Quatro arquivos de teste criados com suites RED verificáveis
- Nenhum arquivo de componente ou route criado neste plano (Wave 0 é só stubs)
- `npm test` sem filtro continua passando nos testes existentes (item-repository, auth, etc.)
- Os paths de import nos stubs são exatamente os que os planos Wave 2-4 vão criar
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-reading-viewer/03-01-SUMMARY.md`
</output>
