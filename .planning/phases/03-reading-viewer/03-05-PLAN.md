---
phase: 03-reading-viewer
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - src/components/viewer/info-panel.tsx
  - src/components/viewer/viewer-page.tsx
  - src/components/viewer/viewer-client-shell.tsx
  - src/app/(shell)/library/[...path]/page.tsx
  - src/app/(shell)/inbox/[item]/page.tsx
autonomous: true
requirements: [VIEW-01, VIEW-08, CTX-03, CTX-04, RUN-04]

must_haves:
  truths:
    - "InfoPanel exibe chips de tipo+estado (D-16: top, maior destaque), campo modelo, tópico›grupo, data_captura formatada em pt-BR"
    - "InfoPanel omite campos ausentes completamente (sem N/A)"
    - "InfoPanel tem slot vazio para texto de sidecar no final (D-18)"
    - "Clicar no conteúdo Markdown NÃO fecha o painel"
    - "Escape fecha o painel quando panelOpen=true"
    - "Painel abre ao lado do conteúdo (push layout flex), NÃO como overlay"
    - "ViewerPage orquestra MarkdownViewer + ViewerHeader + InfoPanel em Server Component"
    - "library/[...path]/page.tsx usa ViewerPage em vez de WorkspaceItemState"
    - "inbox/[item]/page.tsx usa ViewerPage em vez de WorkspaceItemState"
    - "npm test -- info-panel passa (RED → GREEN)"
  artifacts:
    - path: "src/components/viewer/info-panel.tsx"
      provides: "Client Component de painel push com metadados formatados"
      exports: ["InfoPanel"]
    - path: "src/components/viewer/viewer-client-shell.tsx"
      provides: "Client Component wrapper que gerencia estado panelOpen"
      exports: ["ViewerClientShell"]
    - path: "src/components/viewer/viewer-page.tsx"
      provides: "Server Component orquestrador: busca conteúdo + frontmatter + renderiza layout completo"
      exports: ["ViewerPage"]
    - path: "src/app/(shell)/library/[...path]/page.tsx"
      provides: "Rota de biblioteca atualizada para usar ViewerPage"
    - path: "src/app/(shell)/inbox/[item]/page.tsx"
      provides: "Rota de inbox atualizada para usar ViewerPage"
  key_links:
    - from: "src/app/(shell)/library/[...path]/page.tsx"
      to: "src/components/viewer/viewer-page.tsx"
      via: "<ViewerPage item={item} /> — substitui WorkspaceItemState"
    - from: "src/components/viewer/viewer-page.tsx"
      to: "ItemRepository.getItemContent()"
      via: "chamada server-side para ler Markdown puro"
    - from: "src/components/viewer/viewer-page.tsx"
      to: "ItemRepository.getItemFrontmatter()"
      via: "chamada server-side para metadados do InfoPanel"
    - from: "src/components/viewer/viewer-header.tsx"
      to: "id='viewer-scroll'"
      via: "useEffect escuta scroll neste elemento — definido no wrapper de ViewerPage"
---

<objective>
Criar InfoPanel (painel de informações lateral push), ViewerPage (orquestrador server-side) e ViewerClientShell (wrapper client para estado do painel), e atualizar as duas rotas (library e inbox) para usar o novo viewer. Este plano fecha o loop: usuário seleciona item na árvore, rota chama ViewerPage, que lê conteúdo e frontmatter, renderiza MarkdownViewer + ViewerHeader + InfoPanel na mesma shell.

Purpose: Sem este plano, os componentes criados no Wave 3 existem isoladamente. ViewerPage os conecta ao Item do repositório. As rotas passam a entregar conteúdo rico em vez de WorkspaceItemState.
Output: Viewer completo funcionando — seleção de item na shell exibe Markdown rico com cabeçalho sticky e painel de informações abrível.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-reading-viewer/03-CONTEXT.md
@.planning/phases/03-reading-viewer/03-RESEARCH.md
@reference/schemas/frontmatter-item.md
@DESIGN.md

@src/lib/pkm/types.ts
@src/lib/pkm/item-repository.ts
@src/lib/navigation/navigation-types.ts
@src/app/(shell)/library/[...path]/page.tsx
@src/app/(shell)/inbox/[item]/page.tsx
@src/__tests__/info-panel.test.tsx
@src/components/viewer/viewer-header.tsx
@src/components/viewer/markdown-viewer.tsx

<interfaces>
<!-- De src/lib/navigation/navigation-types.ts -->
```typescript
export interface NavigationItemRef {
  id: string;
  label: string;
  scope: NavigationScope;
  itemKind: NavigationItemKind;
  estado: "rascunho" | "finalizado";
  href: string;
}
```

<!-- De src/lib/pkm/types.ts (após 03-03-PLAN.md) -->
```typescript
export interface RawFrontmatter {
  tipo?: string;
  estado: string;
  modelo?: string;
  data_captura?: string;
  data_publicacao?: string;
  url?: string;
  autores?: string[];
}
```

<!-- De src/components/viewer/viewer-header.tsx (03-04-PLAN.md) -->
```typescript
// ViewerHeader props
interface ViewerHeaderProps {
  topic: string; group?: string; estado: "rascunho" | "finalizado";
  itemId: string; panelOpen: boolean; onTogglePanel: () => void;
}
```

<!-- De src/components/viewer/markdown-viewer.tsx (03-04-PLAN.md) -->
```typescript
// MarkdownViewer props
interface MarkdownViewerProps { content: string; }
export async function MarkdownViewer({ content }: MarkdownViewerProps): Promise<JSX.Element>
```

<!-- De src/lib/navigation/navigation-service.ts — getItemById retorna -->
```typescript
// Tipo retornado por getItemById (já existente Phase 2):
export interface NavigationItemRef {
  id: string; label: string; scope: NavigationScope;
  itemKind: NavigationItemKind; estado: "rascunho" | "finalizado"; href: string;
}
```

<!-- As rotas atuais importam WorkspaceItemState e getItemById — manter getItemById, substituir WorkspaceItemState -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: InfoPanel — Client Component com metadados formatados e push layout</name>
  <files>src/components/viewer/info-panel.tsx</files>
  <read_first>
    - src/__tests__/info-panel.test.tsx (testes RED de Wave 0 — implementar contra esses contratos exatos)
    - .planning/phases/03-reading-viewer/03-CONTEXT.md (D-14 a D-18: push 280px, Escape fecha, campos exibidos, omissão de ausentes, slot sidecar; D-16: tipo+estado chips coloridos, top, maior destaque)
    - .planning/phases/03-reading-viewer/03-RESEARCH.md §Padrão 6 (aside com w-70 shrink-0), §Padrão 7 (formatação Intl.DateTimeFormat pt-BR)
    - reference/schemas/frontmatter-item.md (campos obrigatórios e opcionais do frontmatter, incluindo tipo)
    - DESIGN.md §5 Chips (surface-container-high, label-md, radius-md), §3 tipografia
    - src/components/shell/workspace-item-state.tsx (padrão de chip de estado já definido em Phase 2)
  </read_first>
  <behavior>
    - panelOpen=true → aside com data-testid="info-panel" visível no DOM
    - panelOpen=false → aside não renderizado (null)
    - chip de tipo exibido acima (ou ao lado) do chip de estado quando frontmatter.tipo presente (D-16)
    - chip de tipo ausente → nenhum chip de tipo renderizado (D-17)
    - estado "rascunho" → chip com classe bg-surface-container; "finalizado" → bg-primary-container
    - data_captura "2026-03-07" → texto formatado como "7 mar. 2026" ou "7 de mar. de 2026" (Intl.DateTimeFormat pt-BR)
    - data_publicacao "2025-11" → texto formatado como "nov. 2025" (mês + ano)
    - data_publicacao "2025" → texto "2025" (só ano)
    - autores ausente → nenhum texto "autores" no DOM
    - url presente → anchor com href exato da url
    - slot de sidecar renderizado vazio (div vazia ou comentário), não omitido
    - Escape fecha o painel: onClose chamado quando tecla Escape pressionada
  </behavior>
  <action>
Criar `src/components/viewer/info-panel.tsx`:

```typescript
/**
 * InfoPanel — Client Component (Phase 3, CTX-03, CTX-04)
 *
 * Painel lateral direito com metadados do item, apresentados de forma editorial.
 * Push layout (flex ao lado do conteúdo, não overlay) — D-14.
 * Toggle via ℹ️ no header + Escape fecha — D-15.
 * D-16: tipo + estado → chips coloridos no topo (maior destaque).
 * Campos ausentes omitidos completamente, sem N/A — D-17.
 * Slot de sidecar reservado vazio em Phase 3, Phase 4 preenche — D-18.
 *
 * Segurança (T-3-03): item.path absoluto nunca chega aqui —
 * apenas topic, group e RawFrontmatter (campos de frontmatter) são recebidos.
 */

"use client";

import { useEffect } from "react";
import type { RawFrontmatter } from "@/lib/pkm/types";

interface InfoPanelProps {
  panelOpen: boolean;
  onClose: () => void;
  frontmatter: RawFrontmatter;
  topic: string;
  group?: string;
}

// ── Formatação de datas em pt-BR (sem date-fns — Intl nativo) ──────────────

function formatDataCaptura(isoDate: string): string {
  // "2026-03-07" → "7 mar. 2026"
  const date = new Date(isoDate + "T00:00:00");
  return new Intl.DateTimeFormat("pt-BR", {
    day: "numeric",
    month: "short",
    year: "numeric",
  }).format(date);
}

function formatDataPublicacao(isoPartial: string): string {
  if (/^\d{4}$/.test(isoPartial)) return isoPartial;
  if (/^\d{4}-\d{2}$/.test(isoPartial)) {
    const [year, month] = isoPartial.split("-");
    const date = new Date(`${year}-${month}-01T00:00:00`);
    return new Intl.DateTimeFormat("pt-BR", {
      month: "short",
      year: "numeric",
    }).format(date);
  }
  return formatDataCaptura(isoPartial);
}

function formatLabel(slug: string): string {
  return slug.replace(/-/g, " ").replace(/\b\w/g, (c) => c.toUpperCase());
}

// ── Componentes internos ────────────────────────────────────────────────────

function FieldRow({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div className="flex flex-col gap-1">
      <span className="text-[0.6875rem] font-semibold uppercase tracking-[0.05em] text-on-surface/40">
        {label}
      </span>
      <div>{children}</div>
    </div>
  );
}

function Chip({ children, variant = "neutral" }: {
  children: React.ReactNode;
  variant?: "neutral" | "rascunho" | "finalizado";
}) {
  const colorMap = {
    neutral: "bg-surface-container-high text-on-surface/70",
    rascunho: "bg-surface-container text-on-surface/50",
    finalizado: "bg-primary-container text-on-primary-container",
  };
  return (
    <span className={[
      "inline-flex items-center px-2 py-0.5 rounded-md text-[0.6875rem] font-semibold uppercase tracking-[0.05em]",
      colorMap[variant],
    ].join(" ")}>
      {children}
    </span>
  );
}

// ── Componente principal ────────────────────────────────────────────────────

export function InfoPanel({ panelOpen, onClose, frontmatter, topic, group }: InfoPanelProps) {
  // D-15: Escape fecha o painel
  useEffect(() => {
    if (!panelOpen) return;
    const handler = (e: KeyboardEvent) => {
      if (e.key === "Escape") onClose();
    };
    document.addEventListener("keydown", handler);
    return () => document.removeEventListener("keydown", handler);
  }, [panelOpen, onClose]);

  if (!panelOpen) return null;

  const topicLabel = topic === "__inbox" ? "Inbox" : formatLabel(topic);
  const groupLabel = group ? formatLabel(group) : undefined;

  return (
    <aside
      className="w-70 shrink-0 border-l border-outline-variant/20 overflow-y-auto bg-surface-container-low"
      aria-label="Painel de informações"
      data-testid="info-panel"
    >
      <div className="px-5 py-6 flex flex-col gap-5">
        {/* D-16: tipo + estado — chips coloridos no topo, maior destaque */}
        <FieldRow label="Tipo e Estado">
          <div className="flex items-center gap-2 flex-wrap">
            {/* Chip de tipo — exibido quando presente (D-16, D-17: omitir se ausente) */}
            {frontmatter.tipo && (
              <Chip variant="neutral">{frontmatter.tipo}</Chip>
            )}
            {/* Chip de estado — sempre presente (estado é obrigatório) */}
            <Chip variant={frontmatter.estado === "finalizado" ? "finalizado" : "rascunho"}>
              {frontmatter.estado}
            </Chip>
          </div>
        </FieldRow>

        {/* Modelo (D-16: chip neutro) */}
        {frontmatter.modelo && (
          <FieldRow label="Modelo">
            <Chip variant="neutral">{frontmatter.modelo}</Chip>
          </FieldRow>
        )}

        {/* Tópico › grupo (D-16: texto simples) */}
        <FieldRow label="Localização">
          <span className="text-[0.875rem] text-on-surface/70">
            {topicLabel}
            {groupLabel && (
              <> <span aria-hidden="true">›</span> {groupLabel}</>
            )}
          </span>
        </FieldRow>

        {/* Data de captura (D-16: formatada "7 mar. 2026") */}
        {frontmatter.data_captura && (
          <FieldRow label="Capturado em">
            <span className="text-[0.875rem] text-on-surface/70">
              {formatDataCaptura(frontmatter.data_captura)}
            </span>
          </FieldRow>
        )}

        {/* Data de publicação (D-17: omitir se ausente) */}
        {frontmatter.data_publicacao && (
          <FieldRow label="Publicado em">
            <span className="text-[0.875rem] text-on-surface/70">
              {formatDataPublicacao(frontmatter.data_publicacao)}
            </span>
          </FieldRow>
        )}

        {/* URL — apenas para itens url_ (D-17: omitir se ausente) */}
        {frontmatter.url && (
          <FieldRow label="URL">
            <a
              href={frontmatter.url}
              target="_blank"
              rel="noopener noreferrer"
              className="text-[0.875rem] text-tertiary hover:underline break-all"
              data-testid="info-panel-url"
            >
              {frontmatter.url}
            </a>
          </FieldRow>
        )}

        {/* Autores (D-17: omitir se ausente) */}
        {frontmatter.autores && frontmatter.autores.length > 0 && (
          <FieldRow label="Autores">
            <div className="flex flex-wrap gap-1.5">
              {frontmatter.autores.map((autor) => (
                <Chip key={autor} variant="neutral">{autor}</Chip>
              ))}
            </div>
          </FieldRow>
        )}

        {/* D-18: Slot reservado para texto de sidecar — vazio em Phase 3 */}
        <div data-slot="sidecar-content-phase4" aria-hidden="true" />
      </div>
    </aside>
  );
}
```

**Notas de implementação:**
- `w-70` = 17.5rem = 280px (Tailwind v4 tem escala contínua — verificar se w-70 resolve para ~280px, alternativa: `w-[280px]`)
- Se Tailwind v4 não resolver `w-70` para ~280px, usar `w-[280px]` diretamente
- O `border-outline-variant/20` aplica a "Ghost Border" da DESIGN.md §4 (15% opacity — o valor /20 é equivalente a ~20% opacity, aceitável)
- O slot D-18 é um `<div>` vazio com `data-slot` — presente no DOM mas sem conteúdo e sem ocupar espaço
- O chip de tipo usa variant="neutral" (surface-container-high) — diferente do chip de estado que tem cores semânticas (D-16)
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm test -- info-panel 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/viewer/info-panel.tsx existe
    - `grep "\"use client\"" src/components/viewer/info-panel.tsx` encontra 1 ocorrência
    - `grep "data-testid=\"info-panel\"" src/components/viewer/info-panel.tsx` encontra 1 ocorrência no aside
    - `grep "frontmatter.tipo" src/components/viewer/info-panel.tsx` encontra renderização condicional do chip de tipo (D-16)
    - `grep "data-slot=\"sidecar-content-phase4\"" src/components/viewer/info-panel.tsx` encontra 1 ocorrência (D-18)
    - `grep "Escape" src/components/viewer/info-panel.tsx` encontra no handler de Escape (D-15)
    - `grep "Intl.DateTimeFormat" src/components/viewer/info-panel.tsx` encontra 2 ocorrências (data_captura e data_publicacao)
    - `grep "pt-BR" src/components/viewer/info-panel.tsx` encontra nas chamadas Intl
    - `grep "noopener noreferrer" src/components/viewer/info-panel.tsx` encontra no link de url
    - `npm test -- info-panel` passa (GREEN — stubs de Wave 0 agora encontram o componente)
    - npm run typecheck sem erros neste arquivo
  </acceptance_criteria>
  <done>InfoPanel Client Component criado com push layout, chips de tipo+estado no topo (D-16), campos formatados em pt-BR, campos ausentes omitidos, slot de sidecar D-18, Escape fecha, link url como anchor; testes info-panel verdes</done>
</task>

<task type="auto">
  <name>Tarefa 2: ViewerPage + atualizar rotas library e inbox</name>
  <files>src/components/viewer/viewer-page.tsx, src/components/viewer/viewer-client-shell.tsx, src/app/(shell)/library/[...path]/page.tsx, src/app/(shell)/inbox/[item]/page.tsx</files>
  <read_first>
    - src/app/(shell)/library/[...path]/page.tsx (estado atual — substituir WorkspaceItemState por ViewerPage)
    - src/app/(shell)/inbox/[item]/page.tsx (estado atual — mesma substituição)
    - src/components/shell/app-shell.tsx (estrutura do main: verificar overflow atual — viewer-scroll precisa ser filho de elemento com overflow-hidden para scroll funcionar corretamente)
    - src/lib/navigation/navigation-service.ts (getItemById retorna NavigationItemRef com id, topic, group, estado)
    - .planning/phases/03-reading-viewer/03-RESEARCH.md §Padrão 6 (push layout flex com id="viewer-scroll")
    - src/components/viewer/viewer-header.tsx (props: topic, group, estado, itemId, panelOpen, onTogglePanel)
    - src/components/viewer/markdown-viewer.tsx (props: content)
    - src/components/viewer/info-panel.tsx (props: panelOpen, onClose, frontmatter, topic, group)
  </read_first>
  <action>
**Criar src/components/viewer/viewer-page.tsx:**

ViewerPage é um Server Component que busca conteúdo + frontmatter do repositório e passa para sub-componentes. O estado do painel (panelOpen) é gerenciado em um Client Component wrapper interno.

```typescript
/**
 * ViewerPage — Server Component orquestrador do viewer (Phase 3)
 *
 * Responsabilidades:
 * - Buscar conteúdo Markdown via getItemContent()
 * - Buscar frontmatter via getItemFrontmatter()
 * - Renderizar layout: ViewerHeader + MarkdownViewer + InfoPanel (push)
 *
 * Segurança (T-3-03): NavigationItemRef não contém item.path absoluto.
 * O item.id (path relativo) é passado para getItemContent — validado server-side.
 *
 * O id="viewer-scroll" é colocado no div de scroll do conteúdo.
 * ViewerHeader escuta esse id para detectar scroll e ativar glassmorphism.
 */

import { FsItemRepository } from "@/lib/pkm/fs-item-repository";
import { MarkdownViewer } from "@/components/viewer/markdown-viewer";
import { ViewerClientShell } from "@/components/viewer/viewer-client-shell";
import type { NavigationItemRef } from "@/lib/navigation/navigation-types";

interface ViewerPageProps {
  item: Pick<NavigationItemRef, "id" | "label" | "scope" | "estado">;
}

export async function ViewerPage({ item }: ViewerPageProps) {
  const repo = new FsItemRepository();
  const content = repo.getItemContent(item.id);
  const frontmatter = repo.getItemFrontmatter(item.id) ?? {
    estado: item.estado,
  };

  // Derivar topic e group do item.id (path relativo: "topico/grupo/arquivo.md")
  const segments = decodeURIComponent(item.id).split("/");
  const topic = segments[0] ?? item.scope === "inbox" ? "__inbox" : "";
  const groupSegment = segments.find((s, i) => i > 0 && s.startsWith("_"));
  const group = groupSegment?.replace(/^_/, "") ?? undefined;

  return (
    <ViewerClientShell
      topic={topic}
      group={group}
      estado={item.estado}
      itemId={item.id}
      frontmatter={frontmatter}
    >
      <MarkdownViewer content={content} />
    </ViewerClientShell>
  );
}
```

**Criar src/components/viewer/viewer-client-shell.tsx** (Client Component que gerencia estado do painel):

```typescript
/**
 * ViewerClientShell — Client Component para estado do painel de informações.
 *
 * Gerencia panelOpen/onTogglePanel/onClose sem precisar tornar ViewerPage um Client Component.
 * Usa id="viewer-scroll" no container de scroll para ViewerHeader detectar scroll.
 */

"use client";

import { useState, useCallback } from "react";
import { ViewerHeader } from "@/components/viewer/viewer-header";
import { InfoPanel } from "@/components/viewer/info-panel";
import type { RawFrontmatter } from "@/lib/pkm/types";

interface ViewerClientShellProps {
  topic: string;
  group?: string;
  estado: "rascunho" | "finalizado";
  itemId: string;
  frontmatter: RawFrontmatter;
  children: React.ReactNode;
}

export function ViewerClientShell({
  topic, group, estado, itemId, frontmatter, children,
}: ViewerClientShellProps) {
  const [panelOpen, setPanelOpen] = useState(false);
  const togglePanel = useCallback(() => setPanelOpen((p) => !p), []);
  const closePanel = useCallback(() => setPanelOpen(false), []);

  return (
    <div className="flex flex-1 min-w-0 h-full overflow-hidden">
      {/* Container de scroll do conteúdo — id fixo para ViewerHeader detectar scroll */}
      <div
        id="viewer-scroll"
        className="flex-1 min-w-0 overflow-y-auto bg-surface-container-lowest"
      >
        <ViewerHeader
          topic={topic}
          group={group}
          estado={estado}
          itemId={itemId}
          panelOpen={panelOpen}
          onTogglePanel={togglePanel}
        />
        {/* Conteúdo Markdown (children = <MarkdownViewer content={...} />) */}
        {children}
      </div>

      {/* D-14: Painel push — ao lado do conteúdo, não overlay */}
      <InfoPanel
        panelOpen={panelOpen}
        onClose={closePanel}
        frontmatter={frontmatter}
        topic={topic}
        group={group}
      />
    </div>
  );
}
```

**Atualizar src/app/(shell)/library/[...path]/page.tsx:**

Substituir o import e uso de `WorkspaceItemState` por `ViewerPage`:

```typescript
import { notFound } from "next/navigation";
import { decodeLibraryParams } from "@/lib/navigation/route-helpers";
import { getItemById } from "@/lib/navigation/navigation-service";
import { ViewerPage } from "@/components/viewer/viewer-page";

export default async function LibraryItemPage({
  params,
}: {
  params: Promise<{ path: string[] }>;
}) {
  const resolvedParams = await params;
  const itemId = decodeLibraryParams(resolvedParams);
  const item = await getItemById(itemId);

  if (!item || item.scope !== "library") {
    notFound();
  }

  return <ViewerPage item={item} />;
}
```

**Atualizar src/app/(shell)/inbox/[item]/page.tsx:**

```typescript
import { notFound } from "next/navigation";
import { decodeInboxParam } from "@/lib/navigation/route-helpers";
import { getItemById } from "@/lib/navigation/navigation-service";
import { ViewerPage } from "@/components/viewer/viewer-page";

export default async function InboxItemPage({
  params,
}: {
  params: Promise<{ item: string }>;
}) {
  const resolvedParams = await params;
  const itemId = decodeInboxParam(resolvedParams.item);
  const item = await getItemById(itemId);

  if (!item || item.scope !== "inbox") {
    notFound();
  }

  return <ViewerPage item={item} />;
}
```

**Nota sobre AppShell e o id="viewer-scroll":**
Ler `src/components/shell/app-shell.tsx` antes de implementar para verificar o overflow do `<main>`. O `<main>` do AppShell atualmente pode ter `overflow-y-auto` — se sim, ele precisa ser ajustado para `overflow-hidden` quando há viewer carregado, ou o `div#viewer-scroll` com `h-full overflow-y-auto` precisa ser posicionado como filho direto do main com `h-full`.

O layout correto esperado:
```
AppShell > aside (rail) + main (flex-1 overflow-hidden)
  ViewerClientShell (flex flex-1 h-full overflow-hidden)
    div#viewer-scroll (flex-1 overflow-y-auto)
      ViewerHeader (sticky top-0)
      MarkdownViewer (article prose)
    InfoPanel (w-[280px] aside, quando panelOpen)
```

Se o AppShell usa `overflow-y-auto` no main, a solução mais simples é passar uma prop ou usar `overflow-hidden` via className no wrapper do ViewerClientShell. Verificar e ajustar conforme necessário ao ler o arquivo.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm test && npm run typecheck 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/viewer/viewer-page.tsx existe
    - src/components/viewer/viewer-client-shell.tsx existe
    - `grep "WorkspaceItemState" src/app/(shell)/library/[...path]/page.tsx` NÃO encontra ocorrência
    - `grep "ViewerPage" src/app/(shell)/library/[...path]/page.tsx` encontra import e uso
    - `grep "WorkspaceItemState" src/app/(shell)/inbox/[item]/page.tsx` NÃO encontra ocorrência
    - `grep "ViewerPage" src/app/(shell)/inbox/[item]/page.tsx` encontra import e uso
    - `grep "viewer-scroll" src/components/viewer/viewer-client-shell.tsx` encontra id="viewer-scroll" no div de scroll
    - `grep "FsItemRepository" src/components/viewer/viewer-page.tsx` encontra instanciação
    - `grep "getItemContent" src/components/viewer/viewer-page.tsx` encontra chamada
    - `grep "getItemFrontmatter" src/components/viewer/viewer-page.tsx` encontra chamada
    - `npm test` passa todos os testes (incluindo todos os 3 testes de Wave 0 agora GREEN)
    - `npm run typecheck` sem erros
  </acceptance_criteria>
  <done>ViewerPage + ViewerClientShell criados; rotas library e inbox atualizadas para usar ViewerPage; todos os testes da Phase 3 passam (GREEN); typecheck limpo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NavigationItemRef → ViewerPage | item.id (path relativo) passa para getItemContent — validado por resolveAndValidatePath no servidor |
| frontmatter.url → InfoPanel | URL do frontmatter renderizada como link — target=_blank + rel=noopener noreferrer |
| item.path → cliente | NUNCA enviado ao cliente — ViewerPage usa item.id e derivados (topic, group) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (path traversal) | ViewerPage → getItemContent | mitigate | item.id vem de getItemById (server-side, via NavigationService que valida a URL). getItemContent chama resolveAndValidatePath internamente — segundo nível de defesa |
| T-3-03 | Information Disclosure | ViewerPage | mitigate | NavigationItemRef não contém item.path. ViewerPage extrai topic/group do item.id (path relativo), nunca expõe path absoluto ao ViewerClientShell ou InfoPanel |
| T-3-02 | Tampering (XSS via frontmatter.url) | InfoPanel | mitigate | frontmatter.url renderizado como href de anchor React — React escapa atributos automaticamente. target="_blank" + rel="noopener noreferrer" previne abertura de janela controlada |
</threat_model>

<verification>
Após conclusão do plano:
- `npm test` → todos os testes GREEN (incluindo markdown-viewer, viewer-header, info-panel, raw-route que eram RED no Wave 0)
- `npm run typecheck` → sem erros
- `grep -r "WorkspaceItemState" src/app/` → ZERO ocorrências (substituição completa)
- `grep "viewer-scroll" src/components/viewer/viewer-client-shell.tsx` → id no div de scroll
- `grep "frontmatter.tipo" src/components/viewer/info-panel.tsx` → chip de tipo renderizado condicionalmente (D-16)
- Seleção de item na shell navega para ViewerPage com conteúdo rico (verificação manual em 03-checkpoint)
</verification>

<success_criteria>
- InfoPanel com push layout, chips de tipo+estado no topo (D-16), formatação pt-BR, campos ausentes omitidos, slot D-18, Escape fecha
- ViewerPage Server Component orquestra conteúdo + frontmatter + renderização
- ViewerClientShell gerencia estado do painel sem tornar ViewerPage client
- Rotas library e inbox usam ViewerPage em vez de WorkspaceItemState
- Todos os testes (Wave 0 + existentes) passam GREEN
- typecheck limpo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-reading-viewer/03-05-SUMMARY.md`
</output>
