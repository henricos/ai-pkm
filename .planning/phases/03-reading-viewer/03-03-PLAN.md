---
phase: 03-reading-viewer
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - src/lib/pkm/types.ts
  - src/lib/pkm/item-repository.ts
  - src/lib/pkm/fs-item-repository.ts
autonomous: true
requirements: [VIEW-01, CTX-04]

must_haves:
  truths:
    - "RawFrontmatter está exportado de src/lib/pkm/types.ts"
    - "RawFrontmatter inclui campo tipo (string opcional) além de estado"
    - "ItemRepository define getItemContent(id: string): string e getItemFrontmatter(id: string): RawFrontmatter | null"
    - "FsItemRepository implementa getItemContent() com resolveAndValidatePath() — mesma proteção anti-path traversal de getItem()"
    - "FsItemRepository implementa getItemFrontmatter() com resolveAndValidatePath() — mesma proteção"
    - "npm test -- item-repository passa incluindo testes de getItemContent e getItemFrontmatter"
  artifacts:
    - path: "src/lib/pkm/types.ts"
      provides: "Tipo RawFrontmatter exportado com campo tipo"
      contains: "export interface RawFrontmatter"
    - path: "src/lib/pkm/item-repository.ts"
      provides: "Contrato atualizado com getItemContent e getItemFrontmatter"
      exports: ["getItemContent", "getItemFrontmatter"]
    - path: "src/lib/pkm/fs-item-repository.ts"
      provides: "Implementação filesystem dos novos métodos com path validation"
  key_links:
    - from: "src/lib/pkm/fs-item-repository.ts"
      to: "resolveAndValidatePath()"
      via: "método privado compartilhado — getItemContent e getItemFrontmatter usam o mesmo guard"
    - from: "src/components/viewer/markdown-viewer.tsx"
      to: "ItemRepository.getItemContent()"
      via: "chamada server-side em Wave 3"
---

<objective>
Estender o ItemRepository e o FsItemRepository com dois novos métodos: getItemContent() para ler o Markdown puro sem frontmatter, e getItemFrontmatter() para ler os campos do painel de informações. Também adicionar o tipo RawFrontmatter a types.ts, incluindo o campo `tipo` exigido por D-16. A segurança anti-path traversal deve ser idêntica à do getItem() existente.

Purpose: Sem getItemContent() o MarkdownViewer não tem como ler o conteúdo. Sem getItemFrontmatter() o InfoPanel não tem metadados para exibir. O campo `tipo` é necessário para os chips coloridos de tipo+estado do InfoPanel (D-16). O método privado resolveAndValidatePath() encapsula o guard de segurança T-3-01 para evitar duplicação.
Output: Interface ItemRepository atualizada, FsItemRepository com implementações seguras, tipo RawFrontmatter disponível para os componentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-reading-viewer/03-CONTEXT.md
@.planning/phases/03-reading-viewer/03-RESEARCH.md
@reference/schemas/frontmatter-item.md

@src/lib/pkm/types.ts
@src/lib/pkm/item-repository.ts
@src/lib/pkm/fs-item-repository.ts
@src/__tests__/item-repository.test.ts

<interfaces>
<!-- Contratos existentes que o executor deve manter compatíveis -->
<!-- De src/lib/pkm/item-repository.ts (atual) -->
```typescript
export interface ItemRepository {
  listTopics(): Topic[];
  listGroups(topic: string): Group[];
  getItem(id: string): Item | null;
  searchByName(q: string): Item[];
}
```

<!-- De src/lib/pkm/fs-item-repository.ts — padrão de segurança em getItem() -->
```typescript
// Segurança: validar que o path resolvido começa com pkmRoot
const absPath = path.resolve(this.pkmRoot, decoded);
if (!absPath.startsWith(this.pkmRoot + path.sep) && absPath !== this.pkmRoot) {
  throw new Error(`Path traversal detectado: ${id}`);
}
```

<!-- De reference/schemas/frontmatter-item.md — campos do RawFrontmatter -->
<!-- tipo: opcional (indica o tipo do item, ex: "nota-conceito", "url-extrato") -->
<!-- estado: obrigatório; modelo: obrigatório; data_captura: obrigatório -->
<!-- url: condicional (url_* files); autores: opcional; data_publicacao: opcional -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Adicionar RawFrontmatter a types.ts e métodos à interface ItemRepository</name>
  <files>src/lib/pkm/types.ts, src/lib/pkm/item-repository.ts</files>
  <read_first>
    - src/lib/pkm/types.ts (estado atual — manter exports existentes intactos)
    - src/lib/pkm/item-repository.ts (estado atual — adicionar ao final da interface)
    - reference/schemas/frontmatter-item.md (campos obrigatórios e opcionais do frontmatter)
    - .planning/phases/03-reading-viewer/03-RESEARCH.md §Padrão 2 (assinaturas exatas dos métodos)
  </read_first>
  <behavior>
    - RawFrontmatter tem: tipo (string opcional), estado (string obrigatório), modelo (string opcional), data_captura (string opcional), data_publicacao (string opcional), url (string opcional), autores (string[] opcional)
    - ItemRepository.getItemContent(id) retorna string
    - ItemRepository.getItemFrontmatter(id) retorna RawFrontmatter | null
  </behavior>
  <action>
**Em src/lib/pkm/types.ts** — adicionar após a interface Item, antes da interface Subtopic:

```typescript
/**
 * Campos de frontmatter lidos diretamente do arquivo Markdown (Phase 3 — CTX-04)
 * Usado pelo InfoPanel para exibir metadados de forma editorial.
 * Campos opcionais são omitidos quando ausentes no frontmatter.
 *
 * D-16: tipo + estado → chips coloridos (top, maior destaque no InfoPanel)
 */
export interface RawFrontmatter {
  tipo?: string;
  estado: string;
  modelo?: string;
  data_captura?: string;
  data_publicacao?: string;
  url?: string;
  autores?: string[];
}
```

**Em src/lib/pkm/item-repository.ts** — adicionar dois métodos ao final da interface ItemRepository, antes do fechamento `}`:

```typescript
/**
 * Retorna o conteúdo Markdown puro do item, sem o bloco de frontmatter.
 * Usado pelo MarkdownViewer (VIEW-01, VIEW-02).
 * Lança Error se o ID contém path traversal.
 */
getItemContent(id: string): string;

/**
 * Retorna os campos de frontmatter do item para o InfoPanel (CTX-04).
 * Retorna null se o arquivo não existir.
 * Lança Error se o ID contém path traversal.
 */
getItemFrontmatter(id: string): RawFrontmatter | null;
```

Adicionar o import de RawFrontmatter no topo de item-repository.ts:
```typescript
import type { Item, Topic, Group, RawFrontmatter } from "./types";
```

Manter todos os métodos e imports existentes intactos.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export interface RawFrontmatter" src/lib/pkm/types.ts` encontra 1 ocorrência
    - `grep "tipo\?" src/lib/pkm/types.ts` encontra o campo tipo opcional na interface RawFrontmatter
    - `grep "getItemContent" src/lib/pkm/item-repository.ts` encontra 1 ocorrência (assinatura do método)
    - `grep "getItemFrontmatter" src/lib/pkm/item-repository.ts` encontra 1 ocorrência
    - `grep "RawFrontmatter" src/lib/pkm/item-repository.ts` encontra import e uso na assinatura de retorno
    - npm run typecheck termina sem erros relacionados a ItemRepository ou RawFrontmatter
    - Exports existentes em types.ts (Item, ItemType, ItemEstado, Topic, Group, Subtopic) permanecem sem alteração
  </acceptance_criteria>
  <done>RawFrontmatter exportado de types.ts com campo tipo (D-16); interface ItemRepository tem getItemContent() e getItemFrontmatter() com tipos corretos; typecheck limpo</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar getItemContent() e getItemFrontmatter() em FsItemRepository</name>
  <files>src/lib/pkm/fs-item-repository.ts, src/__tests__/item-repository.test.ts</files>
  <read_first>
    - src/lib/pkm/fs-item-repository.ts (estado ATUAL após Tarefa 1 — manter getItem() e métodos existentes)
    - src/__tests__/item-repository.test.ts (testes existentes — adicionar novos testes no final)
    - .planning/phases/03-reading-viewer/03-RESEARCH.md §Padrão 2 (implementação com resolveAndValidatePath e gray-matter)
    - .planning/phases/03-reading-viewer/03-VALIDATION.md §Per-Task Verification Map (task 3-repo — teste de path traversal)
  </read_first>
  <behavior>
    - getItemContent("tecnologia/nota.md") retorna string do conteúdo Markdown sem frontmatter (matter(raw).content.trim())
    - getItemContent("../../../etc/passwd") lança Error "Path traversal detectado"
    - getItemFrontmatter("tecnologia/nota.md") retorna objeto com os campos do frontmatter
    - getItemFrontmatter de arquivo inexistente retorna null
    - getItemFrontmatter("../etc/passwd") lança Error "Path traversal detectado"
  </behavior>
  <action>
**Em src/lib/pkm/fs-item-repository.ts:**

**1. Extrair o guard de path traversal de getItem() para método privado.** Substituir o bloco inline de validação por chamada ao helper:

```typescript
private resolveAndValidatePath(id: string): string {
  const decoded = decodeURIComponent(id);
  const absPath = path.resolve(this.pkmRoot, decoded);
  if (!absPath.startsWith(this.pkmRoot + path.sep) && absPath !== this.pkmRoot) {
    throw new Error(`Path traversal detectado: ${id}`);
  }
  return absPath;
}
```

**2. Refatorar getItem() para usar o novo método privado** (extrair as linhas de decode e validação, substituir por `const absPath = this.resolveAndValidatePath(id)`). A lógica de leitura e parse permanece igual.

**3. Adicionar getItemContent():**

```typescript
getItemContent(id: string): string {
  const absPath = this.resolveAndValidatePath(id);
  if (!fs.existsSync(absPath)) return "";
  const raw = fs.readFileSync(absPath, "utf-8");
  const { content } = matter(raw);
  return content.trim();
}
```

**4. Adicionar getItemFrontmatter():**

```typescript
getItemFrontmatter(id: string): import("./types").RawFrontmatter | null {
  const absPath = this.resolveAndValidatePath(id);
  if (!fs.existsSync(absPath)) return null;
  const raw = fs.readFileSync(absPath, "utf-8");
  const { data } = matter(raw);
  return data as import("./types").RawFrontmatter;
}
```

Adicionar o import de RawFrontmatter no topo do arquivo (junto com os imports existentes de types):
```typescript
import type { Item, Topic, Group, ItemType, ItemEstado, RawFrontmatter } from "./types";
```

**Em src/__tests__/item-repository.test.ts** — adicionar novos testes após os testes existentes:

```typescript
describe("FsItemRepository — Phase 3 methods", () => {
  test("VIEW-01: getItemContent() retorna conteúdo Markdown sem frontmatter", () => {
    vi.mocked(fs.readFileSync).mockReturnValue(
      "---\nestado: rascunho\nmodelo: nota-conceito\ndata_captura: 2026-01-01\n---\n# Meu Conteúdo\n\nTexto aqui."
    );
    vi.mocked(fs.existsSync).mockReturnValue(true);
    const repo = new FsItemRepository();
    const content = repo.getItemContent("tecnologia/nota.md");
    expect(content).toContain("# Meu Conteúdo");
    expect(content).not.toContain("estado:");
    expect(content).not.toContain("---");
  });

  test("T-3-01: getItemContent() lança Path traversal para id com ../", () => {
    const repo = new FsItemRepository();
    expect(() => repo.getItemContent("../../../etc/passwd")).toThrow("Path traversal detectado");
  });

  test("CTX-04: getItemFrontmatter() retorna campos do frontmatter incluindo tipo", () => {
    vi.mocked(fs.readFileSync).mockReturnValue(
      "---\ntipo: url-extrato\nestado: finalizado\nmodelo: url-extrato\ndata_captura: 2026-03-07\nurl: https://exemplo.com\nautores: [\"Autor Um\"]\n---\nConteúdo."
    );
    vi.mocked(fs.existsSync).mockReturnValue(true);
    const repo = new FsItemRepository();
    const fm = repo.getItemFrontmatter("tecnologia/url_exemplo.md");
    expect(fm?.estado).toBe("finalizado");
    expect(fm?.tipo).toBe("url-extrato");
    expect(fm?.url).toBe("https://exemplo.com");
    expect(fm?.autores).toEqual(["Autor Um"]);
  });

  test("T-3-01: getItemFrontmatter() lança Path traversal para id com ../", () => {
    const repo = new FsItemRepository();
    expect(() => repo.getItemFrontmatter("../../../etc/passwd")).toThrow("Path traversal detectado");
  });

  test("CTX-04: getItemFrontmatter() retorna null para arquivo inexistente", () => {
    vi.mocked(fs.existsSync).mockReturnValue(false);
    const repo = new FsItemRepository();
    const fm = repo.getItemFrontmatter("tecnologia/inexistente.md");
    expect(fm).toBeNull();
  });
});
```
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm test -- item-repository 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "resolveAndValidatePath" src/lib/pkm/fs-item-repository.ts` encontra pelo menos 3 ocorrências (definição + 2 chamadas)
    - `grep "getItemContent" src/lib/pkm/fs-item-repository.ts` encontra definição do método
    - `grep "getItemFrontmatter" src/lib/pkm/fs-item-repository.ts` encontra definição do método
    - `npm test -- item-repository` passa com saída "all tests passed" ou similar (incluindo os 5 novos testes)
    - Os testes novos incluem: getItemContent retorna conteúdo sem frontmatter, path traversal lança erro em getItemContent, getItemFrontmatter retorna campos incluindo tipo, path traversal em getItemFrontmatter, retorna null para inexistente
    - Testes existentes (listTopics, listGroups, getItem, etc.) continuam passando sem regressão
  </acceptance_criteria>
  <done>FsItemRepository implementa getItemContent() e getItemFrontmatter() com resolveAndValidatePath() compartilhado; todos os testes do item-repository passam incluindo os 5 novos; campo tipo coberto no teste CTX-04; path traversal detectado em ambos os métodos</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param → filesystem | ID do item vem da URL — deve ser validado antes de qualquer operação fs |
| FsItemRepository → pkm filesystem | Leitura limitada ao pkmRoot via resolveAndValidatePath |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | getItemContent(), getItemFrontmatter() | mitigate | resolveAndValidatePath() usa path.resolve + startsWith(pkmRoot+sep) — idêntico ao getItem() existente. Teste explícito com "../../../etc/passwd" no item-repository.test.ts |
| T-3-03 | Information Disclosure | RawFrontmatter | accept | RawFrontmatter não contém item.path (path absoluto) — apenas campos de frontmatter e inferidos. O campo Item.path permanece no servidor |
</threat_model>

<verification>
Após conclusão do plano:
- `npm test -- item-repository` → todos os testes verdes, incluindo os 5 novos
- `npm run typecheck` → sem erros em types.ts, item-repository.ts, fs-item-repository.ts
- `grep "resolveAndValidatePath" src/lib/pkm/fs-item-repository.ts` → mínimo 3 ocorrências (declaração + chamadas em getItemContent e getItemFrontmatter)
- `grep "export interface RawFrontmatter" src/lib/pkm/types.ts` → 1 ocorrência
- `grep "tipo?" src/lib/pkm/types.ts` → campo tipo opcional presente
</verification>

<success_criteria>
- RawFrontmatter exportado com campos corretos (tipo, estado, modelo, data_captura, data_publicacao, url, autores)
- Interface ItemRepository atualizada com getItemContent e getItemFrontmatter
- FsItemRepository implementa os dois métodos com segurança anti-path traversal via resolveAndValidatePath()
- 5 novos testes verdes em item-repository.test.ts (incluindo cobertura do campo tipo)
- Nenhuma regressão nos testes existentes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-reading-viewer/03-03-SUMMARY.md`
</output>
