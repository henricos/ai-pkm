---
phase: 03-reading-viewer
plan: 06
type: execute
wave: 7
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05]
files_modified:
  - src/components/viewer/markdown-viewer.tsx
  - src/components/viewer/viewer-page.tsx
  - src/app/api/pkm/raw/[...path]/route.ts
  - src/lib/pkm/fs-item-repository.ts
  - src/__tests__/raw-route.test.ts
autonomous: true
gap_closure: true
requirements: [VIEW-01, VIEW-02, VIEW-03, CTX-01, CTX-02, CTX-03, CTX-04]

must_haves:
  truths:
    - "Cifrão ($) em texto comum não ativa math mode LaTeX"
    - "Arquivos não-Markdown exibem mensagem de formato não suportado — não conteúdo bruto"
    - "Download de binários (PDF, JPG, Excalidraw) serve o arquivo original sem corrupção"
    - "Painel de informações abre sem crash ao clicar ℹ️"
  artifacts:
    - path: "src/components/viewer/markdown-viewer.tsx"
      provides: "remarkMath com singleDollarTextMath: false"
    - path: "src/components/viewer/viewer-page.tsx"
      provides: "branch por item.itemKind — binários não passam para MarkdownViewer"
    - path: "src/app/api/pkm/raw/[...path]/route.ts"
      provides: "leitura como Buffer para não-.md, Content-Type correto"
    - path: "src/lib/pkm/fs-item-repository.ts"
      provides: "data_captura normalizada para string ISO antes de retornar RawFrontmatter"
  key_links:
    - from: "viewer-page.tsx"
      to: "MarkdownViewer"
      via: "branch item.itemKind !== 'markdown'"
      pattern: "item\\.itemKind.*markdown"
    - from: "fs-item-repository.ts getItemFrontmatter"
      to: "info-panel.tsx formatDataCaptura"
      via: "data_captura como string ISO YYYY-MM-DD"
      pattern: "data_captura.*instanceof Date"
    - from: "raw route handler"
      to: "Buffer read"
      via: "extensão não-.md → readFileSync sem encoding"
      pattern: "readFileSync.*Buffer|readFileSync(?!.*utf)"
---

<objective>
Fechar os quatro gaps de UAT da Phase 3: false positive de LaTeX com cifrão monetário,
conteúdo bruto exibido para arquivos não-Markdown, download corrompido de binários,
e crash do InfoPanel ao abrir.

Purpose: Os testes de UAT falharam em três itens major e um blocker — a Phase 3 não
pode ser considerada encerrada com o InfoPanel quebrando ao ser aberto e binários
corrompendo no download.

Output: Quatro correções cirúrgicas em quatro arquivos distintos — sem refatoração
de estrutura, apenas alterações mínimas no ponto exato diagnosticado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reading-viewer/03-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desabilitar singleDollarTextMath no remarkMath</name>
  <files>src/components/viewer/markdown-viewer.tsx</files>
  <action>
Alterar a linha que passa `remarkMath` no array `remarkPlugins` de:

```tsx
remarkPlugins={[remarkGfm, remarkMath]}
```

para:

```tsx
remarkPlugins={[remarkGfm, [remarkMath, { singleDollarTextMath: false }]]}
```

Isso desabilita o delimitador `$...$` para math inline. O delimitador `$$...$$` para
blocos math continua funcionando. Nenhuma outra linha deve ser alterada.

Motivação (gap UAT #1): remarkMath com configuração padrão trata qualquer `$` como
início de expressão math, disparando KaTeX em valores monetários com chars Unicode
pt-BR (ã, õ, —, –) e gerando erros de render.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/markdown-viewer.test.tsx</automated>
  </verify>
  <done>Testes existentes do MarkdownViewer continuam verdes. A opção `singleDollarTextMath: false` está presente no source.</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir normalização de data_captura em getItemFrontmatter</name>
  <files>src/lib/pkm/fs-item-repository.ts</files>
  <action>
Em `getItemFrontmatter()`, após extrair `data` do gray-matter, adicionar normalização
de `data_captura` antes do return:

```typescript
getItemFrontmatter(id: string): RawFrontmatter | null {
  const absPath = this.resolveAndValidatePath(id);
  if (!fs.existsSync(absPath)) return null;
  const raw = fs.readFileSync(absPath, "utf-8");
  const { data } = matter(raw);

  // Normalizar data_captura: gray-matter parseia datas YAML não-quotadas como Date JS.
  // InfoPanel espera string "YYYY-MM-DD" — converter se necessário.
  if (data.data_captura instanceof Date) {
    data.data_captura = (data.data_captura as Date).toISOString().slice(0, 10);
  }

  return data as RawFrontmatter;
}
```

Motivação (gap UAT #4 — blocker): YAML sem aspas como `data_captura: 2026-03-07`
é parseado pelo gray-matter como objeto `Date` JS. `formatDataCaptura` em
`info-panel.tsx` concatena esse valor com `"T00:00:00"`, gerando string inválida
para `new Date()` e lançando `RangeError: Invalid time value`.

Nenhum outro método deve ser alterado.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/item-repository.test.ts</automated>
  </verify>
  <done>Todos os testes existentes de FsItemRepository continuam verdes. O método normaliza Date para string ISO antes de retornar.</done>
</task>

<task type="auto">
  <name>Task 3: Bloquear binários antes de chegar ao MarkdownViewer</name>
  <files>src/components/viewer/viewer-page.tsx, src/__tests__/viewer-page.test.tsx</files>
  <action>
O tipo `NavigationItemRef` já possui o campo `itemKind: NavigationItemKind` com valores
`'markdown' | 'image' | 'excalidraw' | 'pdf' | 'binary'`. O caller (`LibraryItemPage`)
já passa esse campo — nenhuma mudança no caller é necessária.

Adicionar um branch em `ViewerPage` que detecta `item.itemKind !== 'markdown'` e retorna
uma mensagem de formato não suportado em vez de chamar `<MarkdownViewer>`.

Alterações precisas em `src/components/viewer/viewer-page.tsx`:

1. Verificar se `ViewerPageProps.item` já inclui `itemKind`. Se não incluir, adicionar o
   campo usando o tipo existente `NavigationItemKind` (importado de onde já estiver definido
   no projeto — provavelmente `@/lib/pkm/types` ou `@/lib/navigation`):

```typescript
interface ViewerPageProps {
  item: {
    id: string;
    label: string;
    scope: string;
    estado: "rascunho" | "finalizado";
    itemKind: NavigationItemKind;  // campo já presente em NavigationItemRef
  };
}
```

2. Após derivar `topic`/`group` a partir de `item.id` (antes de buscar `content`),
   adicionar o branch de itemKind:

```typescript
if (item.itemKind !== "markdown") {
  return (
    <ViewerClientShell
      topic={topic}
      group={group}
      estado={item.estado}
      itemId={item.id}
      frontmatter={frontmatter}
    >
      <div
        className="flex flex-col items-center justify-center h-64 gap-3 text-on-surface/40"
        data-testid="unsupported-format"
      >
        <span className="text-4xl" aria-hidden="true">📄</span>
        <p className="text-sm">Formato não suportado para visualização</p>
        <p className="text-xs">Use o botão de download para acessar o arquivo</p>
      </div>
    </ViewerClientShell>
  );
}
```

3. Se `src/__tests__/viewer-page.test.tsx` não existir, criá-lo com ao menos um teste
   que verifique o branch de itemKind não-markdown:

```typescript
import { render, screen } from "@testing-library/react";
import { describe, it, expect, vi } from "vitest";
import ViewerPage from "@/components/viewer/viewer-page";

// Mock de dependências pesadas (repo, auth) conforme padrão dos demais testes do projeto
vi.mock("@/lib/pkm/fs-item-repository");
vi.mock("@/lib/auth");

const baseItem = {
  id: "grupos/tech/arquivo.pdf",
  label: "arquivo.pdf",
  scope: "tech",
  estado: "finalizado" as const,
};

describe("ViewerPage — branch itemKind", () => {
  it("exibe unsupported-format para itemKind=pdf", async () => {
    const page = await ViewerPage({ item: { ...baseItem, itemKind: "pdf" } });
    render(page);
    expect(screen.getByTestId("unsupported-format")).toBeInTheDocument();
  });

  it("exibe unsupported-format para itemKind=binary", async () => {
    const page = await ViewerPage({ item: { ...baseItem, itemKind: "binary" } });
    render(page);
    expect(screen.getByTestId("unsupported-format")).toBeInTheDocument();
  });

  it("NÃO exibe unsupported-format para itemKind=markdown", async () => {
    // mock getItemContent para retornar markdown válido
    const page = await ViewerPage({ item: { ...baseItem, id: "grupos/tech/nota.md", itemKind: "markdown" } });
    render(page);
    expect(screen.queryByTestId("unsupported-format")).not.toBeInTheDocument();
  });
});
```

Adaptar os mocks conforme o padrão já estabelecido nos demais arquivos de teste do projeto
(verificar `src/__tests__/` para o padrão de mock de FsItemRepository e auth antes de
escrever o arquivo de teste).

Motivação (gap UAT #2): ViewerPage não inspeciona `item.itemKind` — passa o conteúdo bruto
de PDFs, Excalidraw e imagens diretamente ao MarkdownViewer, que tenta renderizá-lo
como Markdown e exibe lixo. O campo correto é `itemKind` (não `type`) e os valores são
`'markdown' | 'image' | 'excalidraw' | 'pdf' | 'binary'` (não `'binario'`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/viewer-page.test.tsx</automated>
  </verify>
  <done>O componente `ViewerPage` não renderiza `MarkdownViewer` quando `item.itemKind !== 'markdown'`. A div `data-testid="unsupported-format"` é renderizada para `itemKind` igual a `'pdf'`, `'image'`, `'excalidraw'` ou `'binary'`. Os testes em `src/__tests__/viewer-page.test.tsx` passam.</done>
</task>

<task type="auto">
  <name>Task 4: Servir binários como Buffer no route handler raw</name>
  <files>src/app/api/pkm/raw/[...path]/route.ts, src/lib/pkm/fs-item-repository.ts, src/__tests__/raw-route.test.ts</files>
  <action>
Substituir a lógica de leitura de arquivo no route handler para detectar a extensão
e usar leitura binária (Buffer) para arquivos não-.md:

```typescript
import { type NextRequest, NextResponse } from "next/server";
import { auth } from "@/lib/auth";
import { FsItemRepository } from "@/lib/pkm/fs-item-repository";
import fs from "fs";
import path from "path";
import { env } from "@/lib/env";

// Mapa de extensão → Content-Type
const CONTENT_TYPE_MAP: Record<string, string> = {
  ".pdf": "application/pdf",
  ".jpg": "image/jpeg",
  ".jpeg": "image/jpeg",
  ".png": "image/png",
  ".gif": "image/gif",
  ".webp": "image/webp",
  ".excalidraw": "application/json",
  ".svg": "image/svg+xml",
  ".md": "text/plain; charset=utf-8",
};

export async function GET(
  request: NextRequest,
  { params }: { params: Promise<{ path: string[] }> }
) {
  // T-3-04: verificar autenticação antes de qualquer operação de filesystem
  const session = await auth();
  if (!session) {
    return new NextResponse(null, { status: 401 });
  }

  const resolvedParams = await params;
  const itemId = resolvedParams.path.join("/");

  try {
    // Usar FsItemRepository apenas para validação de path (resolveAndValidatePath)
    // A leitura em si é feita diretamente para suportar binários.
    const repo = new FsItemRepository();
    const absPath = repo.resolveItemPath(itemId); // ver abaixo

    if (!fs.existsSync(absPath)) {
      return new NextResponse(null, { status: 404 });
    }

    const ext = path.extname(itemId).toLowerCase();
    const isBinary = ext !== ".md" && ext !== ".txt";
    const filename = path.basename(itemId);
    const contentType = CONTENT_TYPE_MAP[ext] ?? "application/octet-stream";

    if (isBinary) {
      // Ler como Buffer — sem encoding, sem gray-matter
      const buffer = fs.readFileSync(absPath);
      return new NextResponse(buffer, {
        status: 200,
        headers: {
          "Content-Type": contentType,
          "Content-Disposition": `attachment; filename="${filename}"`,
          "X-Content-Type-Options": "nosniff",
        },
      });
    } else {
      // Arquivos .md: usar getItemContent() existente (strip frontmatter)
      const content = repo.getItemContent(itemId);
      return new NextResponse(content, {
        status: 200,
        headers: {
          "Content-Type": "text/plain; charset=utf-8",
          "Content-Disposition": `attachment; filename="${filename}"`,
          "X-Content-Type-Options": "nosniff",
        },
      });
    }
  } catch (err) {
    if (err instanceof Error && err.message.includes("Path traversal")) {
      return new NextResponse(null, { status: 400 });
    }
    return new NextResponse(null, { status: 404 });
  }
}
```

IMPORTANTE — alteração em `src/lib/pkm/fs-item-repository.ts`: o route handler acima
requer que `FsItemRepository` exponha um método público para resolver e validar o path
absoluto sem ler o arquivo. Adicionar o método público:

```typescript
/** Expõe resolveAndValidatePath como público para uso no route handler de download */
resolveItemPath(id: string): string {
  return this.resolveAndValidatePath(id);
}
```

IMPORTANTE — atualização de `src/__tests__/raw-route.test.ts`: o handler refatorado
chama `repo.resolveItemPath()` (método novo) e usa `fs.existsSync` + `fs.readFileSync`
diretamente. O mock atual de `FsItemRepository` contém apenas `getItemContent` — precisa
ser expandido, e o módulo `fs` precisa ser mockado. Realizar as seguintes alterações no
arquivo de teste existente:

1. Adicionar `resolveItemPath` ao mock de `FsItemRepository`:

```typescript
vi.mock("@/lib/pkm/fs-item-repository", () => ({
  FsItemRepository: vi.fn().mockImplementation(() => ({
    getItemContent: vi.fn().mockReturnValue("# Mock content"),
    resolveItemPath: vi.fn().mockReturnValue("/mock/pkm/path/to/file.md"),
  })),
}));
```

2. Adicionar mock do módulo `fs` antes dos testes (após os imports), para que
   `fs.existsSync` e `fs.readFileSync` não acessem o filesystem real:

```typescript
vi.mock("fs", () => ({
  default: {
    existsSync: vi.fn().mockReturnValue(true),
    readFileSync: vi.fn().mockReturnValue(Buffer.from("mock binary content")),
  },
}));
```

3. Para o teste CTX-02 (download de arquivo .md — caso onde `isBinary === false`),
   verificar se a expectativa ainda está correta: o handler agora chama
   `repo.getItemContent(itemId)` para `.md`, portanto o mock de `getItemContent`
   deve retornar o conteúdo esperado. Ajustar o mock por teste se necessário via
   `(repo.getItemContent as ReturnType<typeof vi.fn>).mockReturnValueOnce(...)`.

4. Para testes de arquivo binário (ex.: `.pdf`), o mock de `fs.readFileSync` deve
   retornar `Buffer.from("mock binary content")` — o default acima já cobre isso.
   Verificar se algum teste esperava resposta de texto e ajustar a expectativa para
   `Content-Type: application/pdf` e status 200.

Adaptar os detalhes do mock ao padrão já estabelecido nos demais arquivos de teste do
projeto — verificar `src/__tests__/` antes de escrever para não divergir do estilo.

Motivação (gap UAT #3): O route handler chamava `getItemContent()` para todos os
arquivos — essa função usa `readFileSync` com encoding `utf-8` e gray-matter, o que
corrompe dados binários. PDFs, imagens e Excalidraw são arquivos binários que devem
ser lidos como Buffer sem nenhum processamento de texto.

Manter o comportamento atual para `.md`: serve o conteúdo sem frontmatter (comportamento
preferido pelo usuário conforme relatado no UAT). Sem nenhum outro comportamento alterado.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/raw-route.test.ts</automated>
  </verify>
  <done>O método `resolveItemPath` existe em `FsItemRepository` (em `src/lib/pkm/fs-item-repository.ts`). A lógica de branch por extensão está presente no route handler. O arquivo `src/__tests__/raw-route.test.ts` foi atualizado: o mock de `FsItemRepository` inclui `resolveItemPath`, o módulo `fs` está mockado com `existsSync` e `readFileSync`, e todos os testes do raw route passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| cliente → route handler raw | itemId vem dos segmentos do URL — deve ser validado contra path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06-01 | Elevation | resolveItemPath (novo método público) | mitigate | Método delega para resolveAndValidatePath() existente — mesmo boundary de segurança já validado em T-3-01 |
| T-3-06-02| Information Disclosure | raw route — servir binários | accept | Auth guard (T-3-04) já presente; apenas usuário autenticado acessa; pkm é repositório privado local |
| T-3-06-03 | Spoofing | Content-Type por extensão | accept | X-Content-Type-Options: nosniff já presente; extensão inferida do path validado |
</threat_model>

<verification>
Executar a suite de testes completa após todas as tasks:

```bash
npx vitest run
```

Todos os testes devem passar (93+ green). Verificar especificamente:
- `markdown-viewer.test.tsx` — continua verde após mudança no remarkMath
- `item-repository.test.ts` — continua verde após normalização de data_captura
- `viewer-page.test.tsx` — verde com testes do branch itemKind
- `raw-route.test.ts` — continua verde após refactor do route handler
- `info-panel.test.tsx` — continua verde (não testa data Date, apenas string)
</verification>

<success_criteria>
- `src/components/viewer/markdown-viewer.tsx` passa `{ singleDollarTextMath: false }` para remarkMath
- `src/lib/pkm/fs-item-repository.ts` normaliza `data_captura instanceof Date` para string ISO em `getItemFrontmatter()`
- `src/components/viewer/viewer-page.tsx` retorna mensagem de formato não suportado para `item.itemKind !== 'markdown'`
- `src/app/api/pkm/raw/[...path]/route.ts` lê arquivos binários como Buffer sem encoding
- `npx vitest run` retorna todos os testes green
- Os quatro gaps do 03-UAT.md passam para `status: fixed`
</success_criteria>

<output>
Após execução, criar `.planning/phases/03-reading-viewer/03-06-SUMMARY.md` com os
artefatos criados, decisões tomadas e resultado dos testes.

Atualizar o status dos gaps em `.planning/phases/03-reading-viewer/03-UAT.md`:
alterar cada `status: failed` dos quatro gaps para `status: fixed`.
</output>
