---
phase: 01-secure-read-model-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/lib/env.ts
  - vitest.config.ts
  - src/__tests__/auth.test.ts
  - src/__tests__/item-repository.test.ts
  - src/__tests__/env.test.ts
autonomous: true
requirements:
  - ARC-01
  - ARC-04
  - RUN-01

must_haves:
  truths:
    - "O projeto Next.js 16.2.2 inicializa com App Router e TypeScript, sem erros de build"
    - "Os tokens de cor e tipografia do DESIGN.md estão definidos como CSS variables em @theme no globals.css"
    - "O Tailwind v4 gera classes utilitárias a partir dos tokens customizados (bg-surface, text-on-surface, etc.)"
    - "O Vitest está configurado e os arquivos de teste stub existem com pelo menos um teste por arquivo"
    - "npm run test passa (testes stub) e npm run build passa sem erros de tipo"
  artifacts:
    - path: "package.json"
      provides: "Dependências pinnadas: next@16.2.2, next-auth@5.0.0-beta.30, tailwindcss@4.2.2, vitest"
      contains: "\"next\": \"16.2.2\""
    - path: "src/app/globals.css"
      provides: "Design system tokens via @theme CSS block"
      contains: "--color-surface"
    - path: "vitest.config.ts"
      provides: "Configuração Vitest com ambiente jsdom"
      contains: "jsdom"
    - path: "src/__tests__/auth.test.ts"
      provides: "Stubs de teste para ACC-01, ACC-02, ACC-03"
    - path: "src/__tests__/item-repository.test.ts"
      provides: "Stubs de teste para ARC-01, ARC-02, ARC-03, ARC-04, RUN-02"
    - path: "src/__tests__/env.test.ts"
      provides: "Stubs de teste para RUN-01"
    - path: "src/lib/env.ts"
      provides: "Stub minimal de env vars — permite que 01-03 (read model) importe PKM_PATH em paralelo com 01-02 (auth)"
      contains: "PKM_PATH"
  key_links:
    - from: "src/app/globals.css @theme block"
      to: "Tailwind v4 utility classes"
      via: "@import tailwindcss"
      pattern: "@theme\\s*\\{"
    - from: "vitest.config.ts"
      to: "src/__tests__/*.test.ts"
      via: "include glob"
      pattern: "include.*__tests__"
---

<objective>
Bootstrap do projeto Next.js 16 + configuração do design system via Tailwind v4 + scaffolding do framework de testes Vitest.

Purpose: Este plano cria a fundação técnica sobre a qual todos os outros planos da Fase 1 são construídos. Sem o projeto criado, os tokens de design definidos e a infraestrutura de testes pronta, os planos de auth e read model não têm onde aterrissar.

Output: Repositório com Next.js App Router funcional, design system tokens mapeados de DESIGN.md para @theme CSS, Vitest configurado com stubs de teste para todos os requisitos da fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-secure-read-model-foundation/01-CONTEXT.md
@.planning/phases/01-secure-read-model-foundation/01-RESEARCH.md
@DESIGN.md
@reference/ui/screens/01-login/code.html

<interfaces>
<!-- Tokens de cor do DESIGN.md que devem aparecer no @theme do globals.css -->

Cores (hex exatos do DESIGN.md):
- surface: #f8f9fa
- surface_container_lowest: #ffffff
- surface_container_low: #f1f4f6
- surface_container: #eaeff1
- surface_bright: (não especificado explicitamente, usar #ffffff com 80% opacity via glassmorphism)
- on_surface: #2b3437
- tertiary: #0055d7
- tertiary_container: #0266ff
- outline_variant: (não especificado — usar derivado de on_surface com 15% opacity)
- inverse_surface: #0c0f10
- inverse_on_surface: (contraste de inverse_surface — branco ou near-white)
- primary_container: (referenciado no DESIGN.md, extrair cor exata do arquivo)
- on_primary_container: (idem)
- on_tertiary: (contraste de tertiary — branco)

Tipografia (DESIGN.md §3):
- Família: Inter exclusivamente
- display-lg: 3.5rem / weight 600 / tracking -0.02em
- headline-sm: 1.5rem / weight 500 / tracking -0.01em
- title-md: 1.125rem / weight 500 / tracking 0
- body-md: 0.875rem / weight 400 / tracking 0
- label-sm: 0.6875rem / weight 600 / tracking +0.05em

Elevação (DESIGN.md §4):
- Ambient shadow: box-shadow: 0 12px 40px rgba(43, 52, 55, 0.06)
- Border radius: sm (0.125rem), md (0.375rem) — nunca xl

Formato Tailwind v4 @theme (RESEARCH.md Pitfall 2):
```css
@import "tailwindcss";
@theme {
  --color-surface: #f8f9fa;
  --color-surface-container-lowest: #ffffff;
  /* ... demais tokens ... */
  --font-family-sans: "Inter", sans-serif;
}
```
Classes geradas: bg-surface, bg-surface-container-lowest, text-on-surface, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar projeto Next.js 16 com App Router e instalar dependências pinnadas</name>
  <files>
    package.json
    next.config.ts
    tsconfig.json
    .gitignore
    .env.example
  </files>
  <read_first>
    - .planning/phases/01-secure-read-model-foundation/01-RESEARCH.md (Standard Stack, Installation commands, Environment Availability)
    - .planning/phases/01-secure-read-model-foundation/01-CONTEXT.md (D-01, D-02, D-09)
  </read_first>
  <action>
Executar na raiz do repositório (onde já existe pkm/, index/, reference/, etc.):

```bash
# 1. Criar o projeto Next.js no diretório atual
npx create-next-app@16.2.2 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes

# 2. Instalar dependências específicas com versões pinnadas
npm install next-auth@5.0.0-beta.30
npm install gray-matter@4.0.3 zod@4.3.6
npm install -D @types/node@25.5.2 vitest@3.2.4 @vitejs/plugin-react@4.5.0 @testing-library/react@16.3.0 @testing-library/jest-dom@6.6.3 jsdom@26.1.0 @vitest/coverage-v8@3.2.4

# 3. Inicializar shadcn/ui
npx shadcn@4.2.0 init --yes

# 4. Adicionar componentes shadcn iniciais (per D-02)
npx shadcn@4.2.0 add button input label form

# 5. Configurar script de teste no package.json
```

Após os comandos, editar `package.json` para garantir:
- Versões pinnadas exatas (sem `^` ou `~`): `"next": "16.2.2"`, `"next-auth": "5.0.0-beta.30"`, `"tailwindcss": "4.2.2"`
- Adicionar script: `"test": "vitest run"`, `"test:watch": "vitest"`, `"typecheck": "tsc --noEmit"`

Criar `.env.example` na raiz com:
```
# Configuração obrigatória — copie para .env.local e preencha com valores reais
PKM_PATH=/absolute/path/to/your/pkm
AUTH_USERNAME=seu_usuario
AUTH_PASSWORD=senha_segura_minimo_8_chars
NEXTAUTH_SECRET=string_aleatoria_minimo_32_chars
NEXTAUTH_URL=http://localhost:3000
```

Verificar que `.env.local` está no `.gitignore` (create-next-app já inclui, mas confirmar).

Criar `src/lib/env.ts` como stub minimal (01-02 completará com Zod + todos os campos):

```typescript
// Stub — implementação completa em PLAN-02 (auth)
// Criado aqui para permitir paralelismo entre PLAN-02 e PLAN-03 na Wave 2
export const env = {
  PKM_PATH: process.env.PKM_PATH ?? "",
};
```

ATENÇÃO — proibições (per RESEARCH.md Anti-Patterns):
- Não usar `pnpm` ou `yarn` — apenas `npm` (único disponível no ambiente)
- Não hardcodar valores reais em nenhum arquivo commitado
- Não usar versões com `^` para next-auth ou next — pinnar exato
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && node -e "const pkg = require('./package.json'); console.log(pkg.dependencies.next, pkg.dependencies['next-auth']); process.exit(pkg.dependencies.next === '16.2.2' && pkg.dependencies['next-auth'] === '5.0.0-beta.30' ? 0 : 1)"</automated>
  </verify>
  <done>package.json contém next@16.2.2 e next-auth@5.0.0-beta.30 sem operadores de versão. Script "test" existe. .env.example existe. .env.local está no .gitignore.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Configurar design system — tokens DESIGN.md em @theme Tailwind v4</name>
  <files>
    src/app/globals.css
    src/app/layout.tsx
  </files>
  <read_first>
    - DESIGN.md (arquivo completo — fonte de verdade canônica dos tokens)
    - reference/ui/screens/01-login/code.html (ver quais classes Stitch usa para mapear para tokens)
    - .planning/phases/01-secure-read-model-foundation/01-RESEARCH.md (Pattern 5 — mapeamento Stitch → DESIGN.md; Pitfall 2 — formato @theme v4)
    - .planning/phases/01-secure-read-model-foundation/01-CONTEXT.md (D-12, D-14, D-16)
  </read_first>
  <action>
Substituir o conteúdo de `src/app/globals.css` gerado pelo create-next-app com o design system completo:

```css
@import "tailwindcss";

@theme {
  /* === Cores — The Digital Curator (DESIGN.md §2) === */
  /* Base Layer */
  --color-surface: #f8f9fa;
  /* Primary Workspaces */
  --color-surface-container-lowest: #ffffff;
  /* Supportive Panels */
  --color-surface-container-low: #f1f4f6;
  --color-surface-container: #eaeff1;
  /* Overlays */
  --color-surface-bright: #ffffff;
  /* Text */
  --color-on-surface: #2b3437;
  /* Accent — Laser Pointer Blue (use sparingly) */
  --color-tertiary: #0055d7;
  --color-tertiary-container: #0266ff;
  --color-on-tertiary: #ffffff;
  /* Secondary actions */
  --color-primary-container: #dde5f5;
  --color-on-primary-container: #0a2456;
  /* Inverse (tooltips) */
  --color-inverse-surface: #0c0f10;
  --color-inverse-on-surface: #f3f4f4;
  /* Borders (ghost — nunca 100% opaque per DESIGN.md) */
  --color-outline-variant: #c8cfd1;
  /* Surface high (chips) */
  --color-surface-container-high: #dee3e5;

  /* === Tipografia — Inter Grid Scale (DESIGN.md §3) === */
  --font-sans: "Inter", sans-serif;
  /* Font sizes */
  --text-display-lg: 3.5rem;
  --text-headline-sm: 1.5rem;
  --text-title-md: 1.125rem;
  --text-body-md: 0.875rem;
  --text-label-sm: 0.6875rem;

  /* === Elevation (DESIGN.md §4) === */
  --shadow-ambient: 0 12px 40px rgba(43, 52, 55, 0.06);
  /* Border radius — nunca xl (DESIGN.md Don't) */
  --radius-sm: 0.125rem;
  --radius-md: 0.375rem;
}

/* Google Fonts — Inter */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap');

/* Base reset alinhado ao design system */
body {
  font-family: var(--font-sans);
  background-color: var(--color-surface);
  color: var(--color-on-surface);
}

/* Glassmorphism helper (DESIGN.md §4 — nav bars) */
.glass {
  background-color: color-mix(in srgb, var(--color-surface) 70%, transparent);
  backdrop-filter: blur(12px);
}

/* CTA gradient — "liquid" depth (DESIGN.md §2 Signature Textures) */
.gradient-cta {
  background: linear-gradient(180deg, var(--color-tertiary), var(--color-tertiary-container));
}
```

Em `src/app/layout.tsx`, garantir que o `<html>` e `<body>` não tenham classes inline que conflitem com os tokens (criar-next-app às vezes adiciona classes). O layout deve importar apenas `./globals.css` e não adicionar nenhuma cor ou fonte inline que sobrescreva os tokens do @theme.

PROIBIÇÕES (per DESIGN.md Don'ts e RESEARCH.md Pitfall 2):
- Não definir tokens em tailwind.config.ts — Tailwind v4 usa @theme no CSS
- Não usar rounded-xl — usar rounded-sm (--radius-sm) ou rounded-md (--radius-md)
- Não usar black puro (#000000) — usar on_surface (#2b3437)
- Não usar border-radius maior que md
- Não usar cores literais Tailwind (slate-*, blue-*) — apenas os custom tokens definidos acima
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && grep -c "\-\-color-surface" src/app/globals.css && grep -c "\-\-color-tertiary" src/app/globals.css && grep -c "\-\-font-sans" src/app/globals.css</automated>
  </verify>
  <done>globals.css contém bloco @theme com todos os tokens de cor (--color-surface, --color-on-surface, --color-tertiary, --color-tertiary-container, --color-on-tertiary, --color-primary-container, --color-on-primary-container, --color-inverse-surface, --color-outline-variant, --color-surface-container*) e tokens de tipografia (--font-sans, --text-display-lg etc). npm run build passa sem erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: Configurar Vitest e criar stubs de teste para todos os requisitos da fase</name>
  <files>
    vitest.config.ts
    src/__tests__/auth.test.ts
    src/__tests__/item-repository.test.ts
    src/__tests__/env.test.ts
  </files>
  <read_first>
    - .planning/phases/01-secure-read-model-foundation/01-VALIDATION.md (Wave 0 Requirements, Per-Task Verification Map)
    - .planning/phases/01-secure-read-model-foundation/01-RESEARCH.md (Validation Architecture, Phase Requirements → Test Map)
  </read_first>
  <behavior>
    - auth.test.ts: describe('auth middleware') com 3 testes stub (ACC-01: redireciona sem sessão; ACC-02: credenciais validadas via env; ACC-03: sessão criada com cookie httpOnly) — todos marcados com test.todo()
    - item-repository.test.ts: describe('FsItemRepository') com 5 testes stub (ARC-01: listTopics retorna Topic[]; ARC-02: getItem resolve ID estável; ARC-03: retorna Item com type correto; ARC-04: implementa interface ItemRepository; RUN-02: usa PKM_PATH) — todos test.todo()
    - env.test.ts: describe('env validation') com 2 testes stub (RUN-01: falha com mensagem clara quando env ausente; RUN-01: passa com todas as vars presentes) — todos test.todo()
    - npm run test deve retornar exit 0 (testes todo são skip, não fail)
  </behavior>
  <action>
Criar `vitest.config.ts` na raiz:

```typescript
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
import path from "path";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: [],
    include: ["src/__tests__/**/*.test.ts", "src/__tests__/**/*.test.tsx"],
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
});
```

Criar `src/__tests__/auth.test.ts`:

```typescript
/**
 * Testes de autenticação — Phase 1
 * Stubs para ACC-01, ACC-02, ACC-03
 * Implementação completa após PLAN-2 (autenticação)
 */
import { describe, test } from "vitest";

describe("auth middleware", () => {
  test.todo("ACC-01: redireciona para /login quando sessão ausente");
  test.todo("ACC-02: credenciais validadas contra AUTH_USERNAME e AUTH_PASSWORD env vars");
  test.todo("ACC-03: login bem-sucedido cria sessão com cookie httpOnly");
});
```

Criar `src/__tests__/item-repository.test.ts`:

```typescript
/**
 * Testes do ItemRepository — Phase 1
 * Stubs para ARC-01, ARC-02, ARC-03, ARC-04, RUN-02
 * Implementação completa após PLAN-3 (read model)
 */
import { describe, test } from "vitest";

describe("FsItemRepository", () => {
  test.todo("ARC-01: listTopics() retorna array de Topic[] a partir do index/topicos.json");
  test.todo("ARC-02: getItem(id) resolve ID estável (path relativo URL-encoded)");
  test.todo("ARC-03: getItem retorna Item com type discriminado correto (nota | url | binario)");
  test.todo("ARC-04: FsItemRepository satisfaz a interface ItemRepository (typecheck)");
  test.todo("RUN-02: constructor usa process.env.PKM_PATH, não path relativo hardcoded");
});
```

Criar `src/__tests__/env.test.ts`:

```typescript
/**
 * Testes de validação de env vars — Phase 1
 * Stubs para RUN-01
 * Implementação completa após PLAN-2 (env.ts)
 */
import { describe, test } from "vitest";

describe("env validation", () => {
  test.todo("RUN-01: lança ZodError com mensagem clara quando PKM_PATH está ausente");
  test.todo("RUN-01: parse bem-sucedido quando todas as 5 vars obrigatórias estão presentes");
});
```

Verificar que `package.json` tem os scripts:
- `"test": "vitest run"`
- `"test:watch": "vitest"`
- `"typecheck": "tsc --noEmit"`
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm run test -- --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>npm run test retorna exit 0. vitest.config.ts existe com environment jsdom. Os 3 arquivos de teste existem com describe blocks e test.todo() para cada requisito mapeado (ACC-01/02/03, ARC-01/02/03/04, RUN-01/02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create-next-app output → committed config | O scaffolding pode gerar configs inseguros que precisam ser revisados |
| .env.local → repositório git | Credenciais não devem jamais ser commitadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | .env.local / .env.example | mitigate | Confirmar que .env.local está no .gitignore; .env.example contém apenas placeholders sem valores reais; rodar `git status` para garantir que .env.local não aparece como untracked |
| T-1-02 | Tampering | package.json — versões de dependência | mitigate | Pinnar versões exatas sem `^` ou `~` para next, next-auth, tailwindcss — evitar supply chain upgrades automáticos; verificar `npm audit` após install |
| T-1-03 | Information Disclosure | create-next-app default config | accept | O scaffolding padrão do Next.js não expõe credenciais; risco é baixo; configs gerados são revisados na tarefa 1 |
</threat_model>

<verification>
Checklist de conclusão do PLAN-01:

1. `npm run build` passa sem erros de tipo ou compilação
2. `npm run test` retorna exit 0 (todos os stubs em todo, nenhum falhando)
3. `npm run typecheck` passa (tsc --noEmit)
4. `src/app/globals.css` contém bloco `@theme {}` com todos os tokens do DESIGN.md
5. `.env.example` existe com os 5 placeholders (PKM_PATH, AUTH_USERNAME, AUTH_PASSWORD, NEXTAUTH_SECRET, NEXTAUTH_URL)
6. `.env.local` está listado no `.gitignore`
7. `package.json` tem next pinado em 16.2.2 e next-auth em 5.0.0-beta.30
</verification>

<success_criteria>
- Projeto Next.js 16.2.2 com App Router roda `npm run dev` sem erros
- `npm run build` completa com exit 0
- `npm run test` completa com exit 0 (stubs todo)
- Design system tokens do DESIGN.md presentes em globals.css como CSS variables no @theme
- Tailwind v4 gera classes utilitárias a partir dos tokens (bg-surface, text-on-surface, bg-tertiary)
- shadcn/ui inicializado com componentes button, input, label, form disponíveis em src/components/ui/
- .env.example commitado, .env.local no .gitignore
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-secure-read-model-foundation/01-01-SUMMARY.md` com:
- Versões exatas instaladas (next, next-auth, tailwindcss, vitest)
- Estrutura de pastas criada (src/app/, src/components/ui/, src/__tests__/)
- Tokens CSS definidos no @theme (lista completa)
- Scripts npm disponíveis
- Quaisquer desvios das instruções e motivos
</output>
