---
phase: 01-secure-read-model-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/env.ts
  - src/lib/auth.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/middleware.ts
  - src/types/next-auth.d.ts
  - src/app/(auth)/login/page.tsx
  - src/components/login-form.tsx
  - src/__tests__/auth.test.ts
  - src/__tests__/env.test.ts
autonomous: true
requirements:
  - ACC-01
  - ACC-02
  - ACC-03
  - RUN-01

must_haves:
  truths:
    - "Qualquer rota acessada sem sessão redireciona para /login — sem exceção, inclusive em local/dev"
    - "As credenciais de login são lidas exclusivamente de AUTH_USERNAME e AUTH_PASSWORD no ambiente — nunca hardcodadas"
    - "Login bem-sucedido cria sessão JWT em cookie httpOnly e redireciona para /"
    - "Login com credenciais erradas retorna mensagem de erro sem revelar qual campo está incorreto"
    - "A aplicação falha imediatamente com mensagem clara se qualquer env var obrigatória estiver ausente"
    - "A tela de login segue a composição visual do Stitch (code.html) com tokens do DESIGN.md"
  artifacts:
    - path: "src/lib/env.ts"
      provides: "Validação Zod fail-fast de env vars obrigatórias"
      exports: ["env"]
      contains: "PKM_PATH"
    - path: "src/lib/auth.ts"
      provides: "Configuração NextAuth v5 com credentials provider"
      exports: ["handlers", "auth", "signIn", "signOut"]
      contains: "AUTH_USERNAME"
    - path: "src/middleware.ts"
      provides: "Proteção universal de rotas via NextAuth middleware"
      contains: "export { auth as middleware }"
    - path: "src/app/api/auth/[...nextauth]/route.ts"
      provides: "Handler NextAuth para /api/auth/*"
      contains: "export const { GET, POST } = handlers"
    - path: "src/components/login-form.tsx"
      provides: "Formulário de login shadcn/ui adaptado do Stitch com tokens DESIGN.md"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Página /login que renderiza LoginForm"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/auth.ts"
      via: "import { auth as middleware }"
      pattern: "export \\{ auth as middleware \\}"
    - from: "src/lib/auth.ts authorize()"
      to: "process.env.AUTH_USERNAME / AUTH_PASSWORD"
      via: "env.AUTH_USERNAME (via src/lib/env.ts)"
      pattern: "env\\.AUTH_USERNAME"
    - from: "src/components/login-form.tsx"
      to: "src/app/api/auth/[...nextauth]/route.ts"
      via: "signIn('credentials', ...) do next-auth/react"
      pattern: "signIn.*credentials"

user_setup:
  - service: nextauth
    why: "Autenticação single-user via credenciais em env vars"
    env_vars:
      - name: AUTH_USERNAME
        source: "Definir em .env.local — qualquer string não-vazia"
      - name: AUTH_PASSWORD
        source: "Definir em .env.local — mínimo 8 caracteres"
      - name: NEXTAUTH_SECRET
        source: "Gerar com: openssl rand -base64 32 (resultado deve ter ≥32 chars)"
      - name: NEXTAUTH_URL
        source: "Para dev local: http://localhost:3000"
---

<objective>
Autenticação NextAuth v5 com credentials provider, middleware universal de proteção de rotas, validação fail-fast de env vars, e tela de login visual adaptada do Stitch.

Purpose: Implementa ACC-01, ACC-02, ACC-03 e RUN-01. Após este plano, nenhuma rota da aplicação é acessível sem autenticação — o contrato de segurança da Fase 1 está em vigor.

Output: Sistema de auth funcional end-to-end: env.ts (validação), auth.ts (NextAuth config), middleware.ts (proteção universal), route.ts (handler), login-form.tsx (UI Stitch → shadcn), login/page.tsx (página).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-secure-read-model-foundation/01-CONTEXT.md
@.planning/phases/01-secure-read-model-foundation/01-RESEARCH.md
@.planning/phases/01-secure-read-model-foundation/01-01-SUMMARY.md
@DESIGN.md
@reference/ui/screens/01-login/code.html

<interfaces>
<!-- Contratos do PLAN-01 que este plano usa -->

Tokens Tailwind v4 disponíveis (definidos no globals.css do PLAN-01):
- bg-surface, bg-surface-container-lowest, bg-surface-container-low
- text-on-surface, text-on-tertiary
- bg-tertiary, text-tertiary (accent — usar com parcimônia)
- rounded-sm (0.125rem), rounded-md (0.375rem) — NUNCA rounded-xl
- shadow-ambient

Componentes shadcn/ui disponíveis em src/components/ui/:
- Button, Input, Label, Form (instalados no PLAN-01)

Pattern NextAuth v5 App Router (RESEARCH.md Pattern 1):
```typescript
// src/lib/auth.ts
export const { handlers, auth, signIn, signOut } = NextAuth({ ... })

// src/middleware.ts
export { auth as middleware } from "@/lib/auth"
export const config = { matcher: ["/((?!api/auth|_next/static|_next/image|favicon.ico).*)"] }

// src/app/api/auth/[...nextauth]/route.ts
import { handlers } from "@/lib/auth"
export const { GET, POST } = handlers
```

Mapeamento Stitch → DESIGN.md (RESEARCH.md Pattern 5):
- bg-slate-50 → bg-surface
- bg-white (card) → bg-surface-container-lowest
- text-slate-900 → text-on-surface
- border-slate-200 → border-outline-variant/15 (ghost border)
- text-blue-600 → text-tertiary
- bg-blue-600 (button) → gradient-cta (classe definida no globals.css)
- rounded-xl (card Stitch) → rounded-sm (proibido xl per DESIGN.md)
- text-slate-500 → text-on-surface/60
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Validação de env vars (env.ts) + configuração NextAuth v5 (auth.ts + route.ts + middleware.ts)</name>
  <files>
    src/lib/env.ts
    src/lib/auth.ts
    src/app/api/auth/[...nextauth]/route.ts
    src/middleware.ts
    src/types/next-auth.d.ts
    src/__tests__/env.test.ts
    src/__tests__/auth.test.ts
  </files>
  <read_first>
    - .planning/phases/01-secure-read-model-foundation/01-RESEARCH.md (Pattern 1 — NextAuth v5; Pattern 2 — Middleware; Pattern 4 — Env vars; Pitfall 3 — NEXTAUTH_URL vs AUTH_URL; Security Domain; Common Pitfalls)
    - .planning/phases/01-secure-read-model-foundation/01-CONTEXT.md (D-03, D-04, D-05, D-09)
    - src/__tests__/env.test.ts (stubs existentes do PLAN-01 — implementar esses testes)
    - src/__tests__/auth.test.ts (stubs existentes do PLAN-01 — implementar esses testes)
  </read_first>
  <behavior>
    - env.test.ts: lança ZodError com mensagem "PKM_PATH é obrigatório" quando PKM_PATH ausente; parse bem-sucedido retorna objeto com as 5 chaves quando todas presentes
    - auth.test.ts ACC-02: authorize() com username/password corretos retorna { id: "1", name: username }; authorize() com credenciais erradas retorna null
    - middleware bloqueia rota não-auth sem sessão (mock da função auth): deve retornar redirect para /login
  </behavior>
  <action>
**1. Criar src/lib/env.ts:**

```typescript
import { z } from "zod";

const envSchema = z.object({
  PKM_PATH: z.string().min(1, "PKM_PATH é obrigatório"),
  AUTH_USERNAME: z.string().min(1, "AUTH_USERNAME é obrigatório"),
  AUTH_PASSWORD: z.string().min(8, "AUTH_PASSWORD deve ter pelo menos 8 caracteres"),
  NEXTAUTH_SECRET: z.string().min(32, "NEXTAUTH_SECRET deve ter pelo menos 32 caracteres"),
  NEXTAUTH_URL: z.string().url("NEXTAUTH_URL deve ser uma URL válida"),
});

export const env = envSchema.parse(process.env);
```

**2. Criar src/lib/auth.ts:**

```typescript
import NextAuth from "next-auth";
import Credentials from "next-auth/providers/credentials";
import { env } from "@/lib/env";

export const { handlers, auth, signIn, signOut } = NextAuth({
  providers: [
    Credentials({
      async authorize(credentials) {
        const { username, password } = credentials as {
          username: string;
          password: string;
        };
        // Comparação de string simples — aceitável para single-user local.
        // AVISO DE SEGURANÇA: para exposição pública, substituir por
        // crypto.timingSafeEqual() ou hashing com bcrypt (ver RESEARCH.md Security Domain)
        if (
          username === env.AUTH_USERNAME &&
          password === env.AUTH_PASSWORD
        ) {
          return { id: "1", name: username };
        }
        return null;
      },
    }),
  ],
  pages: {
    signIn: "/login",
  },
  session: { strategy: "jwt" },
});
```

**3. Criar src/app/api/auth/[...nextauth]/route.ts:**

```typescript
import { handlers } from "@/lib/auth";
export const { GET, POST } = handlers;
```

**4. Criar src/middleware.ts:**

```typescript
// Edge Runtime — apenas verificação de sessão aqui.
// NUNCA usar fs, path ou qualquer API Node.js neste arquivo.
// (RESEARCH.md Pitfall 1 — middleware roda no Edge Runtime)
export { auth as middleware } from "@/lib/auth";

export const config = {
  // Protege todas as rotas exceto NextAuth handler e assets estáticos
  matcher: ["/((?!api/auth|_next/static|_next/image|favicon.ico).*)"],
};
```

**5. Criar src/types/next-auth.d.ts** (augmentation de tipos):

```typescript
import { DefaultSession } from "next-auth";

declare module "next-auth" {
  interface Session {
    user: {
      id: string;
    } & DefaultSession["user"];
  }
}
```

**6. Implementar testes em src/__tests__/env.test.ts:**

Substituir os stubs test.todo() pela implementação real:
- Teste 1: `process.env` sem PKM_PATH → `expect(() => envSchema.parse({})).toThrow("PKM_PATH é obrigatório")`
- Teste 2: todos os 5 campos válidos → parse retorna objeto com todas as chaves

**7. Implementar testes em src/__tests__/auth.test.ts:**

- ACC-02 test: mockar `env` com username/password, chamar `authorize({username, password})`, verificar retorno

ATENÇÃO crítica de segurança (RESEARCH.md Security Domain):
- Cookie httpOnly e sameSite são gerenciados pelo NextAuth por padrão — não sobrescrever a configuração de session/cookies
- O NEXTAUTH_SECRET é lido automaticamente pelo NextAuth do process.env — não passar manualmente na config se usar o nome `NEXTAUTH_SECRET` (NextAuth v5 aceita este alias de `AUTH_SECRET`)
- Não adicionar `trustHost: true` em produção sem HTTPS
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm run typecheck && npm run test 2>&1 | tail -10</automated>
  </verify>
  <done>
- src/lib/env.ts: exporta `env` com Zod parse; lança erro com mensagem em pt-BR se var ausente
- src/lib/auth.ts: exporta handlers, auth, signIn, signOut; authorize() usa env.AUTH_USERNAME/AUTH_PASSWORD
- src/middleware.ts: exporta auth como middleware; config.matcher exclui api/auth e _next/*
- src/app/api/auth/[...nextauth]/route.ts: exporta GET e POST do handlers
- npm run test passa com os testes de env e auth implementados (não mais .todo())
- npm run typecheck passa sem erros
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Tela de login — LoginForm shadcn/ui adaptado do Stitch com tokens DESIGN.md</name>
  <files>
    src/components/login-form.tsx
    src/app/(auth)/login/page.tsx
  </files>
  <read_first>
    - reference/ui/screens/01-login/code.html (composição visual do Stitch — ler para entender layout, hierarquia e proporções)
    - DESIGN.md (fonte de verdade canônica — tokens, regras, do's e don'ts)
    - .planning/phases/01-secure-read-model-foundation/01-CONTEXT.md (D-15 — protocolo Stitch → React 5 passos; D-14 — DESIGN.md prevalece em conflitos)
    - .planning/phases/01-secure-read-model-foundation/01-RESEARCH.md (Pattern 5 — mapeamento Stitch → DESIGN.md tokens)
    - src/components/ui/button.tsx (interface do componente Button shadcn)
    - src/components/ui/input.tsx (interface do componente Input shadcn)
    - src/components/ui/label.tsx (interface do componente Label shadcn)
    - src/components/ui/form.tsx (interface do componente Form shadcn)
  </read_first>
  <action>
Seguir o protocolo D-15 em 5 passos:

**Passo 1 — Ler HTML do Stitch para layout:**
O code.html tem:
- Fundo de página: bg-slate-50 → **bg-surface**
- Card centralizado: max-w-[400px], padding p-8, bg-white, border → usar **bg-surface-container-lowest**, **rounded-sm** (não rounded-xl do Stitch — proibido pelo DESIGN.md), shadow-ambient
- Header do card: logo "memory" icon + "ai-pkm" title + "System Access" h1 + subtítulo
- Campos: username (label uppercase, input) e password (label + botão "Show" de toggle)
- Alerta de erro (mostrar apenas quando há erro): bg-red-50 → adaptar para surface_container com text-on-surface
- Botão submit: "Sign In" → gradient-cta (tertiary → tertiary_container)
- Footer: "Encrypted" · versão (texto sutil)

**Passo 2 — Criar src/components/login-form.tsx:**

```typescript
"use client";

import { useState } from "react";
import { signIn } from "next-auth/react";
import { useRouter, useSearchParams } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export function LoginForm() {
  const router = useRouter();
  const searchParams = useSearchParams();
  const callbackUrl = searchParams.get("callbackUrl") ?? "/";
  const error = searchParams.get("error");
  const [isLoading, setIsLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setIsLoading(true);
    const formData = new FormData(e.currentTarget);
    const result = await signIn("credentials", {
      username: formData.get("username") as string,
      password: formData.get("password") as string,
      redirect: false,
      callbackUrl,
    });
    setIsLoading(false);
    if (result?.ok) {
      router.push(callbackUrl);
      router.refresh();
    }
  }

  return (
    <form onSubmit={handleSubmit} className="space-y-6">
      <div className="space-y-4">
        {/* Campo username */}
        <div className="space-y-1.5">
          <Label
            htmlFor="username"
            className="text-[0.6875rem] font-semibold uppercase tracking-[0.05em] text-on-surface/60"
          >
            Username
          </Label>
          <Input
            id="username"
            name="username"
            type="text"
            placeholder="curator_id"
            required
            autoComplete="username"
            className="bg-surface-container-low focus:bg-surface-container-lowest focus:border-b-2 focus:border-b-tertiary rounded-sm"
          />
        </div>

        {/* Campo password */}
        <div className="space-y-1.5">
          <Label
            htmlFor="password"
            className="text-[0.6875rem] font-semibold uppercase tracking-[0.05em] text-on-surface/60"
          >
            Password
          </Label>
          <Input
            id="password"
            name="password"
            type="password"
            placeholder="••••••••"
            required
            autoComplete="current-password"
            className="bg-surface-container-low focus:bg-surface-container-lowest focus:border-b-2 focus:border-b-tertiary rounded-sm"
          />
        </div>
      </div>

      {/* Alerta de erro de autenticação */}
      {error && (
        <div className="flex items-center gap-3 p-3 bg-surface-container rounded-sm border border-outline-variant/15">
          <p className="text-[0.75rem] font-medium text-on-surface">
            Credenciais inválidas. Verifique usuário e senha.
          </p>
        </div>
      )}

      {/* Botão submit */}
      <Button
        type="submit"
        disabled={isLoading}
        className="w-full gradient-cta text-on-tertiary py-3 font-semibold text-[0.875rem] rounded-sm"
      >
        {isLoading ? "Aguarde..." : "Sign In"}
      </Button>
    </form>
  );
}
```

**Passo 3 — Criar src/app/(auth)/login/page.tsx:**

```typescript
import { auth } from "@/lib/auth";
import { redirect } from "next/navigation";
import { LoginForm } from "@/components/login-form";
import { Suspense } from "react";

export default async function LoginPage() {
  // Se já autenticado, redirecionar para /
  const session = await auth();
  if (session) redirect("/");

  return (
    <main className="min-h-screen bg-surface flex items-center justify-center">
      <div className="w-full max-w-[400px] p-6">
        {/* Card principal — bg-surface-container-lowest no bg-surface */}
        <div className="bg-surface-container-lowest p-8 rounded-sm shadow-ambient">
          {/* Header */}
          <div className="mb-8">
            <div className="flex items-center gap-2 mb-2">
              {/* Ícone texto — sem dependência de ícone externo na Fase 1 */}
              <span className="text-tertiary font-mono text-lg font-bold">◈</span>
              <span className="text-[1.125rem] font-semibold tracking-tight text-on-surface">
                ai-pkm
              </span>
            </div>
            <h1 className="text-[1.5rem] font-medium tracking-[-0.01em] text-on-surface">
              System Access
            </h1>
            <p className="text-on-surface/60 text-[0.875rem] mt-1">
              Authorized curators only.
            </p>
          </div>

          {/* Formulário */}
          <Suspense>
            <LoginForm />
          </Suspense>
        </div>

        {/* Footer discreto */}
        <footer className="mt-8 text-center">
          <div className="flex items-center justify-center gap-4 text-on-surface/30 text-[0.6875rem] font-semibold uppercase tracking-[0.1em]">
            <span>Encrypted</span>
            <span className="w-1 h-1 bg-outline-variant rounded-full" />
            <span>V. 2.0.0</span>
          </div>
        </footer>
      </div>
    </main>
  );
}
```

**Regras obrigatórias (per D-12, D-14, D-15, DESIGN.md Don'ts):**
- NUNCA usar rounded-xl — apenas rounded-sm ou rounded-md
- NUNCA usar text-slate-*, bg-blue-*, ou qualquer cor Tailwind default — apenas custom tokens
- NUNCA copiar classe CDN do HTML do Stitch diretamente — recriar com tokens
- NUNCA usar black puro — usar text-on-surface para texto principal
- Mensagem de erro NÃO revela qual campo está incorreto (apenas "Credenciais inválidas")
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm run typecheck && grep -L "rounded-xl\|text-slate\|bg-blue\|text-blue" src/components/login-form.tsx src/app/\(auth\)/login/page.tsx | wc -l</automated>
  </verify>
  <done>
- src/components/login-form.tsx: componente "use client" que chama signIn("credentials"), exibe erro quando searchParams.error presente, usa apenas tokens custom (bg-surface-container-low, text-on-surface, gradient-cta, rounded-sm)
- src/app/(auth)/login/page.tsx: Server Component que verifica sessão e redireciona se autenticado; renderiza LoginForm
- npm run typecheck passa
- Nenhum arquivo usa rounded-xl, text-slate-*, bg-blue-*, bg-white (literal), #ffffff (literal)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (unauthenticated) → Next.js routes | Qualquer requisição sem sessão válida deve ser bloqueada antes de chegar à lógica da aplicação |
| login form → /api/auth/[...nextauth] | Credenciais trafegam via POST — deve ser HTTPS em produção |
| process.env → src/lib/auth.ts | AUTH_USERNAME e AUTH_PASSWORD lidos do ambiente; exposição = comprometimento total da aplicação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Spoofing | src/lib/auth.ts authorize() | accept | Comparação de string simples para single-user local; risco de timing attack é baixo neste contexto (não há loop de tentativas, single-user, sem exposição pública); documentar limitação em docs/dev-setup.md; para exposição pública futura: usar crypto.timingSafeEqual() |
| T-1-05 | Elevation of Privilege | src/middleware.ts config.matcher | mitigate | Matcher exclui apenas `/api/auth/*` (necessário para NextAuth funcionar), `_next/static`, `_next/image` e `favicon.ico`; toda rota de aplicação está protegida; verificar com grep que matcher não tem exceções não-intencionais |
| T-1-06 | Information Disclosure | src/components/login-form.tsx — mensagem de erro | mitigate | Mensagem de erro genérica "Credenciais inválidas" — nunca revelar qual campo está errado (user not found vs wrong password); implementado na Tarefa 2 |
| T-1-07 | Tampering | cookie de sessão | accept | NextAuth v5 gerencia cookies httpOnly, sameSite=lax por padrão; secure=true ativado automaticamente quando NEXTAUTH_URL usa HTTPS; não sobrescrever configuração de cookies do NextAuth |
| T-1-08 | Spoofing | Brute-force de credenciais | accept | Single-user em ambiente local/dev — sem exposição pública no escopo da Fase 1; sem rate limiting implementado; documentar em docs/dev-setup.md que aplicação não deve ser exposta publicamente sem adicionar rate limiting |
| T-1-09 | Information Disclosure | src/lib/env.ts — mensagem de ZodError | accept | Mensagens de erro de env vars são exibidas apenas no terminal do servidor (startup), nunca no browser; aceitável para operador do servidor |
</threat_model>

<verification>
Checklist de conclusão do PLAN-02:

1. `npm run typecheck` passa sem erros
2. `npm run test` passa com testes de env.test.ts e auth.test.ts implementados (não .todo())
3. Acessar http://localhost:3000/ sem .env.local → deve falhar com ZodError claro no terminal
4. Com .env.local configurado: acessar http://localhost:3000/ → redireciona para /login
5. Na tela de login: credenciais corretas → redireciona para /
6. Na tela de login: credenciais erradas → exibe mensagem genérica sem revelar qual campo
7. Inspecionar cookie de sessão no browser: deve ser httpOnly=true
8. grep no middleware.ts: `export { auth as middleware }` presente
9. grep no login-form.tsx: nenhuma ocorrência de `rounded-xl`, `text-slate-`, `bg-blue-`, `bg-white`
</verification>

<success_criteria>
- Toda rota da aplicação (exceto /api/auth/* e assets) redireciona para /login sem sessão válida
- Login com credenciais corretas cria sessão JWT em cookie httpOnly e redireciona para /
- Login com credenciais erradas retorna mensagem genérica sem enumerar usuários
- Aplicação falha com erro claro se qualquer env var obrigatória (PKM_PATH, AUTH_USERNAME, AUTH_PASSWORD, NEXTAUTH_SECRET, NEXTAUTH_URL) estiver ausente
- Tela de login usa apenas tokens do DESIGN.md (bg-surface, bg-surface-container-lowest, text-on-surface, gradient-cta, rounded-sm)
- Nenhum valor de credencial hardcodado no repositório
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-secure-read-model-foundation/01-02-SUMMARY.md` com:
- Estrutura de arquivos de auth criados
- Padrão de middleware configurado (matcher exato)
- Tokens CSS usados na tela de login
- Comportamento de erro testado
- Quaisquer desvios e motivos
</output>
