---
phase: 01-secure-read-model-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/pkm/types.ts
  - src/lib/pkm/item-repository.ts
  - src/lib/pkm/fs-item-repository.ts
  - src/app/api/pkm/topics/route.ts
  - src/__tests__/item-repository.test.ts
autonomous: true
requirements:
  - ARC-01
  - ARC-02
  - ARC-03
  - ARC-04
  - RUN-02

must_haves:
  truths:
    - "listTopics() retorna os tópicos reais de index/topicos.json sem ler o pkm/ diretamente"
    - "listGroups(topic) retorna grupos filtrados por tópico de index/grupos.json"
    - "getItem(id) resolve o path relativo URL-encoded para o arquivo correto dentro de PKM_PATH"
    - "getItem rejeita caminhos com ../ que tentam escapar de PKM_PATH (path traversal)"
    - "Item.type é inferido do nome do arquivo (prefixo url_ → url; extensão não-.md → binario; sidecar → binario com sidecarPath)"
    - "A interface ItemRepository pode ser substituída por outra implementação sem alterar consumers"
  artifacts:
    - path: "src/lib/pkm/types.ts"
      provides: "Interfaces TypeScript: Item, ItemType, ItemEstado, Topic, Subtopic, Group"
      exports: ["Item", "ItemType", "ItemEstado", "Topic", "Subtopic", "Group"]
      contains: "ItemType"
    - path: "src/lib/pkm/item-repository.ts"
      provides: "Interface ItemRepository — seam para v3"
      exports: ["ItemRepository"]
      contains: "listTopics"
    - path: "src/lib/pkm/fs-item-repository.ts"
      provides: "Implementação filesystem do ItemRepository"
      exports: ["FsItemRepository"]
      contains: "PKM_PATH"
    - path: "src/app/api/pkm/topics/route.ts"
      provides: "Endpoint GET /api/pkm/topics para validar read model em execução"
      contains: "export async function GET"
  key_links:
    - from: "src/lib/pkm/fs-item-repository.ts"
      to: "process.env.PKM_PATH (via src/lib/env.ts)"
      via: "path.resolve(env.PKM_PATH)"
      pattern: "env\\.PKM_PATH"
    - from: "src/lib/pkm/fs-item-repository.ts listTopics()"
      to: "index/topicos.json (na raiz do ai-pkm)"
      via: "path.join(process.cwd(), 'index', 'topicos.json')"
      pattern: "topicos\\.json"
    - from: "src/lib/pkm/fs-item-repository.ts getItem()"
      to: "path dentro de PKM_PATH"
      via: "path.resolve + validação de prefix"
      pattern: "path\\.resolve"
    - from: "src/app/api/pkm/topics/route.ts"
      to: "src/lib/pkm/fs-item-repository.ts"
      via: "new FsItemRepository().listTopics()"
      pattern: "FsItemRepository"
---

<objective>
Interface ItemRepository + implementação filesystem (FsItemRepository) + endpoint de validação GET /api/pkm/topics.

Purpose: Implementa ARC-01, ARC-02, ARC-03, ARC-04 e RUN-02. Após este plano, a aplicação tem um modelo canônico read-only para acessar o pkm montado externamente. O contrato ItemRepository isola os consumers (navegação, viewer, busca) da implementação de filesystem — permitindo troca para banco na v3 sem alterar nada além da implementação.

Output: types.ts (contratos TypeScript), item-repository.ts (interface seam), fs-item-repository.ts (implementação), route.ts (endpoint de validação), testes implementados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-secure-read-model-foundation/01-CONTEXT.md
@.planning/phases/01-secure-read-model-foundation/01-RESEARCH.md
@.planning/phases/01-secure-read-model-foundation/01-01-SUMMARY.md
@reference/schemas/frontmatter-item.md
@reference/pkm/pkm-structure.md

<interfaces>
<!-- Contratos do domínio PKM extraídos de reference/schemas/frontmatter-item.md -->

Frontmatter de item (campos obrigatórios):
- estado: "rascunho" | "finalizado"
- modelo: string (ex: "url-extrato", "nota-conceito", "sidecar")
- data_captura: string (YYYY-MM-DD)

Frontmatter condicional:
- url: string — obrigatório em arquivos url_*, ausente nos demais

Campos derivados (inferidos — nunca no frontmatter):
- tipo: inferido do nome do arquivo
  - prefixo "url_" → "url"
  - extensão não-.md (ex: .svg, .png, .pdf) → "binario"
  - nome no formato "nome.extensao.md" (sidecar) → "binario" com sidecarPath
  - outros .md sem url_ → "nota"
- tópico: pasta onde o arquivo está (pkm/[topico]/)

Index files (na raiz do ai-pkm — NÃO dentro de pkm/):
- index/topicos.json: Topic[] — campos: id (string), descricao (string), subtopicos?: Subtopic[]
- index/grupos.json: Group[] — campos: caminho (string), descricao (string), topico (string)

Item ID convention (D-08):
- path relativo à raiz do pkm, separado por /
- Exemplo: "tecnologia/superapp/nota-conceito.md"
- URL-encoded para uso em rotas: "tecnologia%2Fsuperapp%2Fnota-conceito.md"

Localização dos índices (RESEARCH.md A2, A3):
- Os índices ficam em process.cwd()/index/ — raiz do repositório ai-pkm
- Em produção/Docker, garantir que o volume inclui o diretório index/ do ai-pkm
- PKM_PATH aponta para o repositório pkm/ montado externamente
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Definir contratos TypeScript — types.ts e interface ItemRepository</name>
  <files>
    src/lib/pkm/types.ts
    src/lib/pkm/item-repository.ts
  </files>
  <read_first>
    - reference/schemas/frontmatter-item.md (contrato completo de frontmatter — campos obrigatórios, condicionais e derivados)
    - .planning/phases/01-secure-read-model-foundation/01-CONTEXT.md (D-06, D-07, D-08 — contrato do ItemRepository)
    - .planning/phases/01-secure-read-model-foundation/01-RESEARCH.md (Pattern 3 — TypeScript interfaces)
  </read_first>
  <action>
**1. Criar src/lib/pkm/types.ts:**

```typescript
/**
 * Tipos canônicos do domínio PKM — Phase 1
 *
 * Estes tipos definem o modelo semântico compartilhado entre navegação,
 * viewer e busca (ARC-03). A implementação de filesystem usa estes tipos;
 * a implementação de banco da v3 usará os mesmos contratos.
 */

/** Tipo de item inferido do nome do arquivo (nunca do frontmatter) */
export type ItemType = "nota" | "url" | "binario";

/** Estado do item — campo obrigatório no frontmatter */
export type ItemEstado = "rascunho" | "finalizado";

/**
 * Item lógico read-only — unidade semântica do PKM.
 *
 * id: path relativo à raiz do pkm, separado por /
 *     Exemplo: "tecnologia/superapp/nota-conceito.md"
 *     URL-encoded para uso em rotas Next.js.
 *     Estável enquanto o arquivo não for renomeado (aceitável para read-only).
 */
export interface Item {
  /** Path relativo à raiz do pkm — identificador estável (ARC-02) */
  id: string;
  /** Path absoluto no filesystem — para leitura de conteúdo */
  path: string;
  /** Nome do arquivo sem extensão */
  name: string;
  /** Tipo inferido do nome do arquivo (ARC-03) */
  type: ItemType;
  /** Estado do item — do frontmatter */
  estado: ItemEstado;
  /** Tópico — inferido da pasta onde o arquivo está */
  topic: string;
  /** Nome do grupo (pasta _grupo), se aplicável */
  group?: string;
  /** Data de captura — do frontmatter (YYYY-MM-DD) */
  dataCaptura: string;
  /** URL canônica — apenas para type === "url" */
  url?: string;
  /** Path absoluto do sidecar — apenas para binários com sidecar associado */
  sidecarPath?: string;
}

/** Subtópico — nó filho de um tópico */
export interface Subtopic {
  id: string;
  descricao: string;
}

/** Tópico — nó raiz da árvore de navegação */
export interface Topic {
  id: string;
  descricao: string;
  subtopicos?: Subtopic[];
}

/** Grupo — pasta _grupo dentro de um tópico */
export interface Group {
  /** Caminho relativo à raiz do repositório ai-pkm (ex: "pkm/tecnologia/_superapp/") */
  caminho: string;
  descricao: string;
  topico: string;
}
```

**2. Criar src/lib/pkm/item-repository.ts:**

```typescript
/**
 * Interface ItemRepository — seam de abstração do acesso ao PKM (ARC-04)
 *
 * A v2 implementa FsItemRepository (filesystem + índices JSON).
 * A v3 pode substituir por DbItemRepository (banco de dados) sem alterar
 * navegação, viewer nem busca — o contrato desta interface permanece estável.
 *
 * REGRA: Todos os consumers (Server Components, Route Handlers) dependem
 * desta interface, nunca diretamente de FsItemRepository.
 */
import type { Item, Topic, Group } from "./types";

export interface ItemRepository {
  /**
   * Retorna todos os tópicos do índice estrutural.
   * Fast path: lê index/topicos.json (ARC-01)
   */
  listTopics(): Topic[];

  /**
   * Retorna grupos de um tópico específico.
   * Fast path: lê index/grupos.json filtrado por topico (ARC-01)
   */
  listGroups(topic: string): Group[];

  /**
   * Busca um item pelo ID (path relativo URL-decoded).
   * Retorna null se não encontrado. (ARC-02)
   */
  getItem(id: string): Item | null;

  /**
   * Busca itens por nome (substring case-insensitive).
   * Fase 1: implementação mínima por nome de arquivo.
   * Fase futura: busca textual no conteúdo (ARC-04 — seam preparada).
   */
  searchByName(q: string): Item[];
}
```
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>
- src/lib/pkm/types.ts exporta: Item, ItemType, ItemEstado, Topic, Subtopic, Group
- src/lib/pkm/item-repository.ts exporta: ItemRepository com 4 métodos assinados
- npm run typecheck passa sem erros nos arquivos novos
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar FsItemRepository + endpoint GET /api/pkm/topics + testes</name>
  <files>
    src/lib/pkm/fs-item-repository.ts
    src/app/api/pkm/topics/route.ts
    src/__tests__/item-repository.test.ts
  </files>
  <read_first>
    - src/lib/pkm/types.ts (criado na Tarefa 1 — contratos a implementar)
    - src/lib/pkm/item-repository.ts (interface a implementar)
    - src/lib/env.ts (para usar env.PKM_PATH)
    - .planning/phases/01-secure-read-model-foundation/01-RESEARCH.md (Pattern 3 — código do FsItemRepository; Pitfall 4 — PKM_PATH absoluto; Common Pitfalls; Open Questions sobre localização dos índices)
    - reference/schemas/frontmatter-item.md (lógica de inferência de tipo por nome de arquivo)
    - index/topicos.json (estrutura real do índice — para escrever testes corretos)
    - index/grupos.json (estrutura real do índice)
    - src/__tests__/item-repository.test.ts (stubs a implementar)
  </read_first>
  <behavior>
    - listTopics(): retorna array com pelo menos um Topic com campo id e descricao strings não-vazias
    - listGroups("tecnologia"): retorna array de Group[] onde cada item tem topico === "tecnologia"
    - getItem("tecnologia/superapp/test.md"): retorna Item com type === "nota", topic === "tecnologia", id === "tecnologia/superapp/test.md" (mock do filesystem)
    - getItem("../../../etc/passwd"): lança Error com mensagem "Path traversal detectado"
    - FsItemRepository satisfaz a interface ItemRepository (verificado via typecheck)
    - FsItemRepository usa env.PKM_PATH (não path relativo hardcoded)
  </behavior>
  <action>
**1. Criar src/lib/pkm/fs-item-repository.ts:**

```typescript
import fs from "fs";
import path from "path";
import matter from "gray-matter";
import type { ItemRepository, Item, Topic, Group } from "./types";
import type { ItemType, ItemEstado } from "./types";
import { env } from "@/lib/env";

/**
 * Implementação filesystem do ItemRepository — v2 (ARC-01, ARC-04)
 *
 * Fast path: lê index/topicos.json e index/grupos.json da raiz do ai-pkm
 * para estrutura de navegação sem varrer o pkm/ inteiro.
 *
 * getItem(): lê frontmatter do arquivo individual quando necessário.
 *
 * IMPORTANTE: Esta classe só pode ser instanciada em contextos Node.js
 * (Server Components, Route Handlers). NUNCA usar em middleware.ts
 * (Edge Runtime não tem acesso a fs). (RESEARCH.md Pitfall 1)
 */
export class FsItemRepository implements ItemRepository {
  private readonly pkmRoot: string;
  private readonly indexDir: string;

  constructor() {
    // PKM_PATH deve ser sempre absoluto (RESEARCH.md Pitfall 4)
    this.pkmRoot = path.resolve(env.PKM_PATH);
    // Índices ficam na raiz do repositório ai-pkm (RESEARCH.md A2)
    this.indexDir = path.join(process.cwd(), "index");
  }

  listTopics(): Topic[] {
    const indexPath = path.join(this.indexDir, "topicos.json");
    const raw = fs.readFileSync(indexPath, "utf-8");
    return JSON.parse(raw) as Topic[];
  }

  listGroups(topic: string): Group[] {
    const indexPath = path.join(this.indexDir, "grupos.json");
    const raw = fs.readFileSync(indexPath, "utf-8");
    const all = JSON.parse(raw) as Group[];
    return all.filter((g) => g.topico === topic);
  }

  getItem(id: string): Item | null {
    // ID é path relativo URL-decoded (D-08)
    const decoded = decodeURIComponent(id);

    // Segurança: validar que o path resolvido começa com pkmRoot
    // (impede path traversal via ../ — RESEARCH.md Security Domain)
    const absPath = path.resolve(this.pkmRoot, decoded);
    if (!absPath.startsWith(this.pkmRoot + path.sep) && absPath !== this.pkmRoot) {
      throw new Error(`Path traversal detectado: ${id}`);
    }

    if (!fs.existsSync(absPath)) return null;

    const raw = fs.readFileSync(absPath, "utf-8");
    const { data: frontmatter } = matter(raw);

    // Inferir tipo do nome do arquivo (não do frontmatter — reference/schemas/frontmatter-item.md)
    const filename = path.basename(decoded);
    const type = this.inferType(filename, decoded);

    // Inferir tópico da pasta (primeiro segmento do path relativo)
    const segments = decoded.split("/");
    const topic = segments[0] ?? "";
    const group = this.inferGroup(segments);

    const item: Item = {
      id,
      path: absPath,
      name: path.basename(decoded, path.extname(decoded)),
      type,
      estado: (frontmatter.estado as ItemEstado) ?? "rascunho",
      topic,
      group,
      dataCaptura: (frontmatter.data_captura as string) ?? "",
      url: type === "url" ? (frontmatter.url as string | undefined) : undefined,
    };

    // Verificar sidecar para binários
    if (type === "binario") {
      const sidecarPath = absPath + ".md";
      if (fs.existsSync(sidecarPath)) {
        item.sidecarPath = sidecarPath;
      }
    }

    return item;
  }

  searchByName(q: string): Item[] {
    // Fase 1: implementação stub — retorna vazio.
    // Fase futura: busca por nome nos índices (ARC-04 — seam preparada).
    return [];
  }

  private inferType(filename: string, relPath: string): ItemType {
    // Prefixo url_ → tipo url
    if (filename.startsWith("url_")) return "url";
    // Sidecar: nome.extensao.md (ex: foto.png.md)
    const isSidecar = /\.[^.]+\.[^.]+$/.test(relPath);
    if (isSidecar) return "binario";
    // Extensão não-.md → binário
    if (!relPath.endsWith(".md")) return "binario";
    // Demais → nota
    return "nota";
  }

  private inferGroup(segments: string[]): string | undefined {
    // Grupo é pasta que começa com _ (convenção PKM)
    const groupSegment = segments.find((s, i) => i > 0 && s.startsWith("_"));
    return groupSegment?.replace(/^_/, "") ?? undefined;
  }
}
```

**2. Criar src/app/api/pkm/topics/route.ts** (endpoint de validação):

```typescript
import { auth } from "@/lib/auth";
import { FsItemRepository } from "@/lib/pkm/fs-item-repository";
import { NextResponse } from "next/server";

/**
 * GET /api/pkm/topics
 * Endpoint de validação do read model — retorna lista de tópicos.
 * Protegido por auth (middleware universal já bloqueia sem sessão).
 */
export async function GET() {
  const session = await auth();
  if (!session) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  try {
    const repo = new FsItemRepository();
    const topics = repo.listTopics();
    return NextResponse.json({ topics });
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

**3. Implementar testes em src/__tests__/item-repository.test.ts:**

Substituir os stubs test.todo() pela implementação real usando mocks de filesystem (vi.mock de 'fs' e 'gray-matter'):

```typescript
import { describe, test, expect, vi, beforeEach } from "vitest";

// Mock do módulo env para não requerer variáveis reais em teste
vi.mock("@/lib/env", () => ({
  env: {
    PKM_PATH: "/mock/pkm",
    AUTH_USERNAME: "test",
    AUTH_PASSWORD: "testpass123",
    NEXTAUTH_SECRET: "test-secret-with-at-least-32-characters-here",
    NEXTAUTH_URL: "http://localhost:3000",
  },
}));

// Mock do fs
vi.mock("fs");

import fs from "fs";
import { FsItemRepository } from "@/lib/pkm/fs-item-repository";
import type { ItemRepository } from "@/lib/pkm/item-repository";

const mockTopicos = JSON.stringify([
  { id: "tecnologia", descricao: "Tecnologia e engenharia" },
]);
const mockGrupos = JSON.stringify([
  { caminho: "pkm/tecnologia/_superapp/", descricao: "Superapps", topico: "tecnologia" },
]);

describe("FsItemRepository", () => {
  beforeEach(() => {
    vi.mocked(fs.readFileSync).mockImplementation((p: unknown) => {
      if (String(p).endsWith("topicos.json")) return mockTopicos;
      if (String(p).endsWith("grupos.json")) return mockGrupos;
      return "";
    });
    vi.mocked(fs.existsSync).mockReturnValue(true);
  });

  test("ARC-01: listTopics() retorna array de Topic[] a partir do index/topicos.json", () => {
    const repo = new FsItemRepository();
    const topics = repo.listTopics();
    expect(topics).toHaveLength(1);
    expect(topics[0]?.id).toBe("tecnologia");
    expect(typeof topics[0]?.descricao).toBe("string");
  });

  test("ARC-01: listGroups() retorna grupos filtrados por tópico", () => {
    const repo = new FsItemRepository();
    const groups = repo.listGroups("tecnologia");
    expect(groups).toHaveLength(1);
    expect(groups[0]?.topico).toBe("tecnologia");
  });

  test("ARC-02: getItem() resolve ID estável (path relativo URL-decoded)", () => {
    vi.mocked(fs.readFileSync).mockReturnValue("---\nestado: rascunho\nmodelo: nota-conceito\ndata_captura: 2026-01-01\n---");
    const repo = new FsItemRepository();
    const item = repo.getItem("tecnologia/superapp/nota.md");
    expect(item?.id).toBe("tecnologia/superapp/nota.md");
  });

  test("ARC-03: getItem retorna Item com type 'url' para arquivos com prefixo url_", () => {
    vi.mocked(fs.readFileSync).mockReturnValue("---\nestado: rascunho\nmodelo: url-extrato\ndata_captura: 2026-01-01\nurl: https://exemplo.com\n---");
    const repo = new FsItemRepository();
    const item = repo.getItem("tecnologia/url_exemplo.md");
    expect(item?.type).toBe("url");
  });

  test("ARC-04: FsItemRepository implementa interface ItemRepository", () => {
    // Verificação em nível de tipo — se compilar, o contrato está satisfeito
    const repo: ItemRepository = new FsItemRepository();
    expect(typeof repo.listTopics).toBe("function");
    expect(typeof repo.listGroups).toBe("function");
    expect(typeof repo.getItem).toBe("function");
    expect(typeof repo.searchByName).toBe("function");
  });

  test("RUN-02: usa PKM_PATH do env, não path hardcoded", () => {
    const repo = new FsItemRepository();
    // getItem com path traversal deve lançar erro (prova que pkm root é usado como boundary)
    expect(() => repo.getItem("../../../etc/passwd")).toThrow("Path traversal detectado");
  });
});
```
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/ai-pkm && npm run typecheck && npm run test 2>&1 | tail -15</automated>
  </verify>
  <done>
- src/lib/pkm/fs-item-repository.ts: implementa ItemRepository; usa env.PKM_PATH (path.resolve); lança erro em path traversal; inferType correto para nota/url/binario; sidecar detectado
- src/app/api/pkm/topics/route.ts: GET autenticado que chama FsItemRepository().listTopics()
- src/__tests__/item-repository.test.ts: 5 testes passando (não mais .todo()) cobrindo ARC-01/02/03/04 e RUN-02
- npm run test passa com exit 0
- npm run typecheck passa sem erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Item ID (URL param) → filesystem path | O ID recebido pela aplicação é transformado em path do filesystem — boundary crítica para path traversal |
| PKM_PATH env var → filesystem | O path do pkm montado externamente; um valor malicioso pode expor arquivos fora do pkm |
| index/topicos.json e grupos.json → runtime | Arquivos lidos em tempo de execução; conteúdo inesperado pode causar erros de parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-10 | Tampering | FsItemRepository.getItem() — path traversal | mitigate | path.resolve(pkm_root, decoded_id) + validação que absPath.startsWith(pkm_root + sep); qualquer ../ que escape o pkm_root lança Error("Path traversal detectado"); testado no item-repository.test.ts |
| T-1-11 | Information Disclosure | GET /api/pkm/topics — exposição de estrutura do pkm | accept | Endpoint protegido por auth (session check no route handler + middleware universal); sem sessão → 401; conteúdo do pkm é para o próprio operador single-user |
| T-1-12 | Tampering | PKM_PATH — valor malicioso no env | mitigate | path.resolve() normaliza o path; a validação de prefix impede acesso fora do pkm_root mesmo que PKM_PATH seja manipulado; env.ts valida que PKM_PATH é string não-vazia |
| T-1-13 | Denial of Service | FsItemRepository — leitura síncrona de arquivo | accept | readFileSync síncrono é aceitável para read model single-user de baixa concorrência; para v3 com múltiplos usuários, migrar para leitura assíncrona |
| T-1-14 | Information Disclosure | error message em getItem() — path traversal | mitigate | Mensagem de erro inclui o id inválido para fins de debug; como o endpoint retorna apenas {"error": "message"} sem stack trace, a exposição é mínima; o id foi fornecido pelo próprio usuário autenticado |
</threat_model>

<verification>
Checklist de conclusão do PLAN-03:

1. `npm run typecheck` passa sem erros
2. `npm run test` passa com 5 testes implementados (ARC-01, ARC-02, ARC-03, ARC-04, RUN-02)
3. `path.resolve()` + validação de prefix presente em getItem() — grep confirma
4. Teste de path traversal `getItem("../../../etc/passwd")` lança Error com mensagem "Path traversal detectado"
5. GET /api/pkm/topics com sessão válida retorna `{ topics: [...] }` (smoke test manual com PKM_PATH configurado)
6. FsItemRepository está declarado como `implements ItemRepository` — typescript verifica o contrato
7. searchByName retorna [] e não acessa filesystem (fase 1 stub — não implementar busca real)
</verification>

<success_criteria>
- Interface ItemRepository exportada de src/lib/pkm/item-repository.ts com 4 métodos assinados
- FsItemRepository implements ItemRepository — typecheck confirma o contrato
- listTopics() lê index/topicos.json da raiz do ai-pkm (não de PKM_PATH)
- getItem() rejeita paths com ../ que escapem PKM_PATH com Error("Path traversal detectado")
- Todos os 5 testes em item-repository.test.ts passando
- GET /api/pkm/topics acessível com sessão (retorna topics) e bloqueado sem sessão (401)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-secure-read-model-foundation/01-03-SUMMARY.md` com:
- Contratos TypeScript definidos (interfaces, tipos exportados)
- Lógica de inferência de tipo implementada
- Segurança: como path traversal é prevenido (código exato da validação)
- Localização dos índices JSON em runtime (process.cwd() + "index/")
- Quaisquer desvios e motivos
</output>
