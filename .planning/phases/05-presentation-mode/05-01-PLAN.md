---
phase: 05-presentation-mode
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/viewer-header.test.tsx
  - src/__tests__/viewer-client-shell.test.tsx
  - src/__tests__/presentation-mode.test.tsx
  - src/__tests__/laser-pointer-overlay.test.tsx
  - src/__tests__/viewer-theme.test.tsx
autonomous: true
requirements: [PRS-01, PRS-02, PRS-03, PRS-04, PRS-05, PRS-06, PRS-07]

must_haves:
  truths:
    - "A fase 5 nao avanca sem contratos automatizados para presentation mode, hit area de controles, laser e presets do viewer"
    - "O modo apresentacao precisa ser validado como comportamento interno da app, nao como mero fullscreen nativo"
    - "O laser deve ser protegido por testes de rastro temporal e escopo dentro/fora do presentation mode"
    - "Os presets devem ser validados como tema do viewer apenas, sem vazamento para a shell inteira"
  artifacts:
    - path: "src/__tests__/viewer-header.test.tsx"
      provides: "Contrato do botao de apresentacao e do seletor de tema"
    - path: "src/__tests__/viewer-client-shell.test.tsx"
      provides: "Contrato de transicao entre shell normal e presentation mode"
    - path: "src/__tests__/presentation-mode.test.tsx"
      provides: "Contrato do palco puro, hit area e controles auto-ocultaveis"
    - path: "src/__tests__/laser-pointer-overlay.test.tsx"
      provides: "Contrato do laser temporal e do escopo transversal ao viewer"
    - path: "src/__tests__/viewer-theme.test.tsx"
      provides: "Contrato dos presets no root do viewer, sem vazamento para a shell"
  key_links:
    - from: "src/__tests__/viewer-client-shell.test.tsx"
      to: "src/components/viewer/viewer-client-shell.tsx"
      via: "estado presentation mode e bloqueio do InfoPanel"
    - from: "src/__tests__/viewer-header.test.tsx"
      to: "src/components/viewer/viewer-header.tsx"
      via: "gatilho real do modo apresentacao e seletor de tema"
    - from: "src/__tests__/presentation-mode.test.tsx"
      to: "src/components/viewer/presentation-overlay.tsx"
      via: "hit area inferior esquerda e controles discretos"
    - from: "src/__tests__/laser-pointer-overlay.test.tsx"
      to: "src/components/viewer/laser-pointer-overlay.tsx"
      via: "rastro dissipativo orientado por tempo"
    - from: "src/__tests__/viewer-theme.test.tsx"
      to: "src/components/viewer/viewer-theme.ts"
      via: "presets restritos ao viewer root e persistencia local segura"
---

<objective>
Congelar os contratos da fase 5 antes da implementacao. Esta wave protege a interpretacao correta de presentation mode, laser e temas, que sao requisitos de UX especificos e faceis de distorcer sem testes.

Purpose: Garantir que a fase 5 tenha contratos verificaveis para comportamento do palco, controles, ponteiro e presets.
Output: Ate cinco arquivos de teste descrevendo o comportamento esperado da fase 5.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-presentation-mode/05-CONTEXT.md
@.planning/phases/05-presentation-mode/05-RESEARCH.md
@DESIGN.md
@src/components/viewer/viewer-header.tsx
@src/components/viewer/viewer-client-shell.tsx
@src/components/viewer/viewer-page.tsx
@src/components/viewer/markdown-viewer.tsx
@src/components/viewer/image-viewer.tsx
@src/components/viewer/pdf-viewer.tsx
@src/__tests__/viewer-header.test.tsx

<interfaces>
From src/components/viewer/viewer-client-shell.tsx:
```typescript
interface ViewerClientShellProps {
  topic: string;
  group?: string;
  estado: "rascunho" | "finalizado";
  itemId: string;
  frontmatter: RawFrontmatter;
  children: React.ReactNode;
  sidecarContent?: string | null;
}
```

From src/components/viewer/viewer-header.tsx:
```typescript
interface ViewerHeaderProps {
  topic: string;
  group?: string;
  estado: "rascunho" | "finalizado";
  itemId: string;
  panelOpen: boolean;
  onTogglePanel: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expandir o contrato do ViewerHeader para presentation mode e tema</name>
  <files>src/__tests__/viewer-header.test.tsx</files>
  <behavior>
    - Test 1: o botao de apresentacao deixa de ser disabled e passa a chamar um callback explicito.
    - Test 2: o slot de tema deixa de ser placeholder e expõe um controle de preset fora do modo apresentacao.
    - Test 3: quando o presentation mode estiver ativo, o botao/painel respeita a indisponibilidade do `InfoPanel`.
  </behavior>
  <action>Atualizar `viewer-header.test.tsx` para substituir o contrato legado de botao desabilitado por assertions do gatilho real de presentation mode e do seletor de tema. O teste deve deixar explicito que o modo apresentacao entra pelo header e que a troca de tema fica fora do modo, no mesmo header.</action>
  <verify>
    <automated>npx vitest run src/__tests__/viewer-header.test.tsx</automated>
  </verify>
  <done>O header fica protegido contra regressao de placeholder e contra vazamento de responsabilidades entre presentation mode, tema e painel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escrever testes do shell de apresentacao, da hit area e dos presets do viewer</name>
  <files>src/__tests__/viewer-client-shell.test.tsx, src/__tests__/presentation-mode.test.tsx, src/__tests__/viewer-theme.test.tsx</files>
  <behavior>
    - Test 1: entrar em presentation mode oculta header normal, bloqueia `InfoPanel` e preserva o conteudo principal.
    - Test 2: `Esc` sai do modo apresentacao.
    - Test 3: os controles nao reaparecem com movimento global; apenas a regiao inferior esquerda os revela.
    - Test 4: os controles ficam discretos e auto-ocultaveis apos inatividade.
    - Test 5: o comportamento vale para markdown, imagem e PDF, nao apenas markdown.
    - Test 6: o preset ativo e aplicado apenas no root do viewer, sem modificar a shell global.
    - Test 7: markdown, imagem, PDF e fallback recebem o mesmo contexto de tema no viewer root, mesmo que a validacao seja por mocks leves.
    - Test 8: a persistencia local do preset usa fallback seguro quando `localStorage` nao estiver disponivel.
    - Test 9: o seletor de tema so atua fora do presentation mode.
  </behavior>
  <action>Criar `viewer-client-shell.test.tsx`, `presentation-mode.test.tsx` e `viewer-theme.test.tsx` com mocks minimos dos viewers para travar a semantica do palco puro, do bloqueio do painel, da hit area e do escopo dos presets. Cobrir explicitamente que o `InfoPanel` nao abre no modo apresentacao, que a shell volta ao estado normal ao sair e que o tema fica restrito ao root do viewer sem vazar para `AppShell`.</action>
  <verify>
    <automated>npx vitest run src/__tests__/viewer-client-shell.test.tsx src/__tests__/presentation-mode.test.tsx src/__tests__/viewer-theme.test.tsx</automated>
  </verify>
  <done>Os requisitos PRS-01, PRS-02, PRS-03, PRS-04, PRS-06 e PRS-07 passam a ter contratos automatizados claros ainda na Wave 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Escrever testes dedicados do laser transversal ao viewer</name>
  <files>src/__tests__/laser-pointer-overlay.test.tsx</files>
  <behavior>
    - Test 1: o laser pode ser ligado e desligado sem entrar em presentation mode.
    - Test 2: o overlay registra pontos temporais e elimina segmentos antigos por idade/opacidade.
    - Test 3: o overlay ignora atualizacao quando o documento fica oculto ou quando o laser esta desligado.
    - Test 4: o laser nao captura eventos de forma que bloqueie interacao normal com o conteudo quando estiver desligado.
  </behavior>
  <action>Criar `laser-pointer-overlay.test.tsx` com timers controlados para validar o comportamento temporal do rastro. O contrato deve bloquear implementacoes simplistas de cursor estatico sem memoria visual.</action>
  <verify>
    <automated>npx vitest run src/__tests__/laser-pointer-overlay.test.tsx</automated>
  </verify>
  <done>PRS-05 fica protegido por contrato antes da codificacao do overlay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header/UI controls -> shell state | O gatilho de apresentacao nao pode deixar o shell em estado incoerente |
| Presentation hit area -> controls | O mecanismo de revelacao nao pode virar hover global ruidoso |
| Laser overlay -> viewer content | O overlay nao pode bloquear a leitura nem simular funcionalidade sem rastro real |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-05-01 | high | Spoofing | presentation mode | mitigate-blocking | Testes RED exigem modo interno real, nao fullscreen-only |
| T-05-02 | high | Tampering | controls reveal | mitigate-blocking | Testes RED exigem hit area dedicada e rejeitam reveal por movimento global |
| T-05-03 | medium | Denial of Service | laser overlay | mitigate | Testes RED exigem desligamento limpo e pausa em estado oculto |
| T-05-04 | medium | Tampering | theme presets | mitigate | Testes RED exigem escopo limitado ao viewer root |
</threat_model>

<verification>
- `npx vitest run src/__tests__/viewer-header.test.tsx`
- `npx vitest run src/__tests__/viewer-client-shell.test.tsx src/__tests__/presentation-mode.test.tsx src/__tests__/viewer-theme.test.tsx`
- `npx vitest run src/__tests__/laser-pointer-overlay.test.tsx`
</verification>

<success_criteria>
- Todos os requisitos da fase 5 possuem contratos automatizados diretos antes da implementacao.
- Nenhum comportamento central do modo apresentacao fica dependente de interpretacao subjetiva do executor.
- O laser e os temas deixam de ser placeholders conceituais e passam a ter fronteiras tecnicas testaveis.
</success_criteria>

<output>
After completion, create `.planning/phases/05-presentation-mode/05-01-SUMMARY.md`
</output>
