---
phase: 05-presentation-mode
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - src/components/viewer/laser-pointer-overlay.tsx
  - src/components/viewer/viewer-client-shell.tsx
  - src/components/viewer/presentation-controls.tsx
requirements: [PRS-04, PRS-05]
autonomous: false

must_haves:
  truths:
    - "O laser e camada transversal ao viewer inteiro, nao detalhe do markdown"
    - "O laser funciona dentro e fora do presentation mode"
    - "O rastro precisa ter persistencia curta e dissipacao progressiva; cursor estatico nao atende"
    - "A implementacao deve permanecer leve e evitar re-render de toda a arvore do viewer a cada movimento"
  artifacts:
    - path: "src/components/viewer/laser-pointer-overlay.tsx"
      provides: "Overlay temporal do ponteiro laser"
    - path: "src/components/viewer/viewer-client-shell.tsx"
      provides: "Injecao do overlay sobre qualquer viewer"
    - path: "src/components/viewer/presentation-controls.tsx"
      provides: "Toggle do laser no cluster do modo apresentacao"
  key_links:
    - from: "src/components/viewer/presentation-controls.tsx"
      to: "src/components/viewer/viewer-client-shell.tsx"
      via: "toggle do estado `isLaserEnabled`"
    - from: "src/components/viewer/viewer-client-shell.tsx"
      to: "src/components/viewer/laser-pointer-overlay.tsx"
      via: "injecao do overlay sobre markdown, imagem, PDF e fallback"
---

<objective>
Implementar o ponteiro laser da fase 5 com comportamento temporal consistente e escopo transversal ao viewer, buscando fidelidade pratica ao Excalidraw sem integrar o editor inteiro.

Purpose: Fechar PRS-05 e o toggle de PRS-04 sem inflar a arquitetura.
Output: Laser pointer com rastro curto, dissipacao progressiva e uso dentro/fora do modo apresentacao.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-presentation-mode/05-CONTEXT.md
@.planning/phases/05-presentation-mode/05-RESEARCH.md
@DESIGN.md
@src/components/viewer/viewer-client-shell.tsx
@src/components/viewer/presentation-controls.tsx
@src/components/viewer/markdown-viewer.tsx
@src/components/viewer/image-viewer.tsx
@src/components/viewer/pdf-viewer.tsx
@src/__tests__/laser-pointer-overlay.test.tsx
@src/__tests__/presentation-mode.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construir overlay temporal do laser</name>
  <files>src/components/viewer/laser-pointer-overlay.tsx</files>
  <behavior>
    - O overlay registra movimento por Pointer Events.
    - Os pontos do rastro expiram por idade, com opacidade/comprimento decrescentes.
    - O loop de animacao usa `requestAnimationFrame` e pausa quando nao ha trilha ou quando o documento fica oculto.
    - O overlay nao captura cliques de forma a bloquear o viewer quando o laser estiver desligado.
  </behavior>
  <action>Validar primeiro se `@excalidraw/laser-pointer@1.3.2` encaixa no viewer sem arrastar arquitetura excessiva; se sim, encapsular o pacote em `LaserPointerOverlay`. Se nao, implementar `LaserPointerOverlay` localmente com SVG/canvas/divs absolutas e animacao por `requestAnimationFrame`, mantendo o mesmo contrato de rastro temporal. Em ambos os casos, nao integrar o editor Excalidraw inteiro nem criar dependencia que transforme o viewer em whiteboard.</action>
  <verify>
    <automated>npx vitest run src/__tests__/laser-pointer-overlay.test.tsx</automated>
  </verify>
  <done>Existe um overlay dedicado do laser, com rastro real e dissipacao temporal consistente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrar o laser ao shell e aos controles</name>
  <files>src/components/viewer/viewer-client-shell.tsx, src/components/viewer/presentation-controls.tsx</files>
  <behavior>
    - O toggle do laser aparece nos controles do modo apresentacao.
    - O laser pode continuar ativo fora do presentation mode, conforme requisito.
    - O overlay cobre markdown, imagem, PDF e fallback sem codigo especializado por tipo.
  </behavior>
  <action>Adicionar `isLaserEnabled` ao estado do shell, injetar o overlay num ponto comum sobre o viewer e conectar o toggle dos controles. Garantir que sair do presentation mode nao destrua obrigatoriamente o estado do laser, porque PRS-05 exige funcionamento tambem fora do modo.</action>
  <verify>
    <automated>npx vitest run src/__tests__/laser-pointer-overlay.test.tsx src/__tests__/presentation-mode.test.tsx && npm run typecheck</automated>
  </verify>
  <done>O laser vira capacidade transversal do viewer e nao comportamento isolado do palco.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validacao manual de fidelidade pratica do laser</name>
  <what-built>Ponteiro laser com rastro curto e toggle dentro/fora do modo apresentacao.</what-built>
  <action>Comparar o comportamento percebido do laser implementado com a referencia desejada do Excalidraw: persistencia curta, dissipacao progressiva e sensacao de fluidez. Validar em markdown e imagem, porque o contraste e a densidade visual mudam.</action>
  <how-to-verify>
    1. Ativar o laser fora do modo apresentacao e mover o ponteiro sobre markdown e imagem.
    2. Entrar em presentation mode e repetir o teste.
    3. Observar se o rastro some progressivamente e se nao vira linha dura, cursor simples ou efeito atrasado.
    4. Confirmar que desligar o laser limpa a camada de forma previsivel.
  </how-to-verify>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <done>O comportamento real do laser fica suficientemente proximo da referencia para nao exigir replanejamento de arquitetura.</done>
  <resume-signal>Responder com `approved` ou listar os desvios observados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer events -> overlay state | O volume de eventos nao pode contaminar toda a arvore React |
| Overlay -> content interaction | O laser nao pode bloquear leitura/clicks de forma indevida |
| Visibility state -> animation loop | O loop precisa parar quando a aba nao estiver visivel |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-05-08 | high | Denial of Service | laser overlay | mitigate-blocking | Isolar overlay e usar RAF em vez de mutacao ampla por evento |
| T-05-09 | medium | Tampering | user interaction | mitigate | Garantir `pointer-events` apropriado quando desligado |
| T-05-10 | medium | Information Disclosure | laser persistence | mitigate | Limpar trilha ao desligar e ao desmontar overlay |
</threat_model>

<verification>
- `npx vitest run src/__tests__/laser-pointer-overlay.test.tsx`
- `npx vitest run src/__tests__/presentation-mode.test.tsx`
- `npm run typecheck`
</verification>

<success_criteria>
- O laser exibe rastro temporal real, nao cursor estatico.
- O viewer continua funcional em markdown, imagem, PDF e fallback.
- O desempenho permanece compativel com uso normal do viewer.
</success_criteria>

<output>
After completion, create `.planning/phases/05-presentation-mode/05-03-SUMMARY.md`
</output>
