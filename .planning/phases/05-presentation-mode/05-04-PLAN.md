---
phase: 05-presentation-mode
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - src/components/viewer/viewer-theme.ts
  - src/components/viewer/viewer-header.tsx
  - src/components/viewer/viewer-client-shell.tsx
  - src/components/viewer/markdown-viewer.tsx
  - src/components/viewer/image-viewer.tsx
  - src/components/viewer/pdf-viewer.tsx
  - src/components/viewer/unsupported-viewer.tsx
requirements: [PRS-06, PRS-07]
autonomous: false

must_haves:
  truths:
    - "Os temas afetam apenas o viewer e o presentation mode, nunca a shell inteira"
    - "Os presets ChatGPT, GitHub e Excalidraw precisam ter identidade perceptivel sem virar skins radicais"
    - "A troca de tema acontece pelo header fora do presentation mode"
    - "A fase so fecha com verificacao manual em markdown, imagem e PDF"
  artifacts:
    - path: "src/components/viewer/viewer-theme.ts"
      provides: "Contrato dos presets e tokens derivados"
    - path: "src/components/viewer/viewer-client-shell.tsx"
      provides: "Aplicacao do preset no root do viewer e persistencia local"
    - path: "src/components/viewer/markdown-viewer.tsx"
      provides: "Ajustes de superficie editorial por tema"
    - path: "src/components/viewer/image-viewer.tsx"
      provides: "Compatibilidade visual do asset com os presets"
    - path: "src/components/viewer/pdf-viewer.tsx"
      provides: "Compatibilidade visual do container PDF com os presets"
  key_links:
    - from: "src/components/viewer/viewer-header.tsx"
      to: "src/components/viewer/viewer-client-shell.tsx"
      via: "seletor de preset e persistencia do tema ativo"
    - from: "src/components/viewer/viewer-client-shell.tsx"
      to: "src/components/viewer/viewer-theme.ts"
      via: "aplicacao do preset ao root do viewer"
    - from: "src/components/viewer/viewer-theme.ts"
      to: "src/components/viewer/markdown-viewer.tsx"
      via: "propagacao de tokens e variants para markdown"
    - from: "src/components/viewer/viewer-theme.ts"
      to: "src/components/viewer/image-viewer.tsx"
      via: "propagacao de superficie e contraste para imagem"
    - from: "src/components/viewer/viewer-theme.ts"
      to: "src/components/viewer/pdf-viewer.tsx"
      via: "propagacao de superficie e contraste para PDF"
    - from: "src/components/viewer/viewer-theme.ts"
      to: "src/components/viewer/unsupported-viewer.tsx"
      via: "compatibilidade do fallback com os presets"
---

<objective>
Fechar a fase 5 com presets de viewer inspirados em ChatGPT, GitHub e Excalidraw, aplicados apenas ao conteudo, com persistencia local simples e verificacao final da experiencia.

Purpose: Fechar PRS-06 e PRS-07 sem vazar tema para a shell inteira.
Output: Viewer com tres presets coerentes, acionados pelo header e consistentes em markdown, imagem e PDF.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-presentation-mode/05-CONTEXT.md
@.planning/phases/05-presentation-mode/05-RESEARCH.md
@DESIGN.md
@reference/ui/screens/04-presentation-mode/code.html
@src/components/viewer/viewer-header.tsx
@src/components/viewer/viewer-client-shell.tsx
@src/components/viewer/markdown-viewer.tsx
@src/components/viewer/image-viewer.tsx
@src/components/viewer/pdf-viewer.tsx
@src/components/viewer/unsupported-viewer.tsx
@src/__tests__/viewer-header.test.tsx
@src/__tests__/viewer-client-shell.test.tsx
@src/__tests__/viewer-theme.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir presets do viewer e aplicar no root do conteudo</name>
  <files>src/components/viewer/viewer-theme.ts, src/components/viewer/viewer-client-shell.tsx</files>
  <behavior>
    - Existem tres presets nomeados: `chatgpt`, `github`, `excalidraw`.
    - O preset ativo aparece no root do viewer por classe ou data-attribute.
    - A persistencia local e resiliente: se `localStorage` falhar, o viewer continua funcional com default seguro.
  </behavior>
  <action>Criar um modulo `viewer-theme.ts` para descrever os presets e integrá-los ao shell. Persistir apenas a escolha de tema, sem criar configuracao global da app. O default deve ser o que melhor preservar a experiencia atual do viewer.</action>
  <verify>
    <automated>npx vitest run src/__tests__/viewer-header.test.tsx src/__tests__/viewer-client-shell.test.tsx src/__tests__/viewer-theme.test.tsx</automated>
  </verify>
  <done>O shell conhece os presets e aplica o tema apenas ao viewer root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adaptar markdown, imagem, PDF e fallback para os presets</name>
  <files>src/components/viewer/markdown-viewer.tsx, src/components/viewer/image-viewer.tsx, src/components/viewer/pdf-viewer.tsx, src/components/viewer/unsupported-viewer.tsx, src/components/viewer/viewer-header.tsx</files>
  <behavior>
    - `chatgpt` enfatiza leitura limpa e neutra.
    - `github` aproxima composicao e contrastes de documentacao tecnica.
    - `excalidraw` puxa atmosfera mais leve e diagramatica sem perder legibilidade.
    - A shell externa nao muda de tema.
    - O seletor de tema fica no header fora do modo apresentacao.
  </behavior>
  <action>Aplicar os presets nos viewers existentes com o minimo de refactor estrutural. Usar tokens e superficies do design system em vez de valores literais dispersos. Nao copiar HTML do Stitch; adaptar a intencao visual ao stack atual. Garantir que imagem, PDF e fallback continuem coerentes sob cada preset.</action>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <done>Os tres presets existem, sao distinguiveis e respeitam o escopo limitado ao viewer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fechamento visual e funcional da fase 5</name>
  <what-built>Presentation mode funcional, laser transversal ao viewer e presets ChatGPT/GitHub/Excalidraw aplicados ao conteudo.</what-built>
  <action>Validar a fase completa no navegador real com itens markdown, imagem e PDF. Confirmar que o presentation mode, o laser e os presets coexistem sem vazar para a shell e sem quebrar responsividade minima.</action>
  <how-to-verify>
    1. Abrir um markdown e alternar entre os tres presets, dentro e fora do presentation mode.
    2. Repetir com imagem e PDF para confirmar que os containers continuam coerentes.
    3. Ativar o laser em cada preset e verificar contraste/visibilidade.
    4. Confirmar que a navegacao volta ao estado normal ao sair do mode e que o tema persiste ao recarregar a pagina.
    5. Em viewport menor, confirmar que a shell normal continua utilizavel e que o modo apresentacao nao introduz quebra grosseira.
  </how-to-verify>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <done>A fase 5 fica pronta para verificacao oficial, com comportamentos e acabamento coerentes entre tipos de item.</done>
  <resume-signal>Responder com `approved` ou listar os problemas restantes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme selector -> viewer root | A troca de tema nao pode afetar a shell global |
| Theme persistence -> browser storage | Falhas de storage nao podem quebrar leitura |
| Presets -> viewer types | Markdown, imagem e PDF precisam responder de forma coerente ao mesmo preset |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-05-11 | high | Tampering | theme scope | mitigate-blocking | Limitar tema ao root do viewer, nunca ao layout global |
| T-05-12 | medium | Denial of Service | localStorage | mitigate | Fazer fallback resiliente quando storage nao estiver disponivel |
| T-05-13 | medium | Spoofing | preset identity | mitigate | Diferenciar os tres presets sem reestruturar a UI nem mascarar o conteudo |
</threat_model>

<verification>
- `npm test`
- `npm run typecheck`
</verification>

<success_criteria>
- Os tres presets do viewer existem e sao perceptivelmente distintos.
- O presentation mode, o laser e os presets coexistem sem regressao relevante em markdown, imagem e PDF.
- O tema nao vaza para a shell inteira e pode persistir localmente sem obrigatoriedade de backend.
</success_criteria>

<output>
After completion, create `.planning/phases/05-presentation-mode/05-04-SUMMARY.md`
</output>
