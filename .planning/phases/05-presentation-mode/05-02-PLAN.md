---
phase: 05-presentation-mode
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - src/components/viewer/viewer-header.tsx
  - src/components/viewer/viewer-client-shell.tsx
  - src/components/viewer/presentation-overlay.tsx
  - src/components/viewer/presentation-controls.tsx
requirements: [PRS-01, PRS-02, PRS-03, PRS-04]
autonomous: false

must_haves:
  truths:
    - "Presentation mode e uma superficie interna do viewer, nao uma tela separada nem fullscreen do browser"
    - "No modo apresentacao, o `InfoPanel` fica indisponivel e a shell de manutencao desaparece do foco visual"
    - "Os controles sao revelados por hit area inferior esquerda, discretos e auto-ocultaveis"
    - "Sair do modo por `Esc` e por botao dedicado e obrigatorio"
  artifacts:
    - path: "src/components/viewer/viewer-client-shell.tsx"
      provides: "Estado do modo apresentacao e orquestracao do palco puro"
    - path: "src/components/viewer/viewer-header.tsx"
      provides: "Gatilho real de presentation mode e seletor de tema"
    - path: "src/components/viewer/presentation-overlay.tsx"
      provides: "Superficie de apresentacao sobre o viewer"
    - path: "src/components/viewer/presentation-controls.tsx"
      provides: "Cluster inferior esquerdo discreto e auto-ocultavel"
  key_links:
    - from: "src/components/viewer/viewer-header.tsx"
      to: "src/components/viewer/viewer-client-shell.tsx"
      via: "callback de entrada/saida do presentation mode e bloqueio do painel"
    - from: "src/components/viewer/viewer-client-shell.tsx"
      to: "src/components/viewer/presentation-overlay.tsx"
      via: "injecao do palco puro sobre o conteudo atual"
    - from: "src/components/viewer/presentation-overlay.tsx"
      to: "src/components/viewer/presentation-controls.tsx"
      via: "hit area inferior esquerda e cluster auto-ocultavel"
---

<objective>
Entregar a infraestrutura visivel do modo apresentacao: entrada e saida pela app, palco puro, bloqueio do `InfoPanel` e controles discretos no canto inferior esquerdo.

Purpose: Fechar PRS-01, PRS-02, PRS-03 e PRS-04 sobre a arquitetura existente do viewer.
Output: Presentation mode funcional, consistente e integrado ao `ViewerClientShell`.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-presentation-mode/05-CONTEXT.md
@.planning/phases/05-presentation-mode/05-RESEARCH.md
@DESIGN.md
@reference/ui/screens/04-presentation-mode/code.html
@src/components/viewer/viewer-header.tsx
@src/components/viewer/viewer-client-shell.tsx
@src/components/viewer/viewer-page.tsx
@src/components/viewer/markdown-viewer.tsx
@src/components/viewer/image-viewer.tsx
@src/components/viewer/pdf-viewer.tsx
@src/__tests__/viewer-header.test.tsx
@src/__tests__/viewer-client-shell.test.tsx
@src/__tests__/presentation-mode.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transformar o header em ponto real de entrada de presentation mode</name>
  <files>src/components/viewer/viewer-header.tsx</files>
  <behavior>
    - O botao de apresentacao deixa de ser placeholder e entra no fluxo real.
    - O slot de tema vira controle real de preset, mas so fora do modo apresentacao.
    - O toggle do `InfoPanel` respeita a indisponibilidade do painel quando o modo apresentacao esta ativo.
  </behavior>
  <action>Refatorar `ViewerHeader` para receber `isPresentationMode`, `onEnterPresentationMode`, `activeTheme` e `onThemeChange`, mantendo a composicao editorial existente. Nao redesenhar o header inteiro; apenas transformar os slots ja reservados em controles funcionais coerentes com o design atual.</action>
  <verify>
    <automated>npx vitest run src/__tests__/viewer-header.test.tsx</automated>
  </verify>
  <done>O header passa a ser o ponto canônico de entrada em presentation mode e troca de tema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orquestrar o presentation mode no ViewerClientShell</name>
  <files>src/components/viewer/viewer-client-shell.tsx, src/components/viewer/presentation-overlay.tsx, src/components/viewer/presentation-controls.tsx</files>
  <behavior>
    - Entrar em presentation mode transforma o viewer em palco puro sem trocar de rota nem perder o item atual.
    - O `InfoPanel` fecha ao entrar no modo e nao pode abrir enquanto o modo estiver ativo.
    - A hit area no canto inferior esquerdo revela os controles; movimento global nao faz isso.
    - Os controles desaparecem novamente apos inatividade.
    - `Esc` e botao dedicado saem do modo.
  </behavior>
  <action>Concentrar o estado cliente em `ViewerClientShell`, criar `PresentationOverlay` como camada de composicao sobre o conteudo atual e `PresentationControls` como cluster visual separado. Preservar compatibilidade com markdown, imagem, PDF e fallback, evitando ifs especificos por viewer. Se fizer sentido para a UX, permitir fullscreen nativo apenas como acao adicional futura ou detalhe interno, nunca como definicao do modo.</action>
  <verify>
    <automated>npx vitest run src/__tests__/viewer-client-shell.test.tsx src/__tests__/presentation-mode.test.tsx && npm run typecheck</automated>
  </verify>
  <done>O presentation mode funciona como camada interna da app, com controles e saida previsiveis, sem refactor amplo do viewer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validacao manual da experiencia de palco puro</name>
  <what-built>Modo apresentacao interno, controles discretos com hit area inferior esquerda e saida por Esc/botao dedicado.</what-built>
  <action>Validar no navegador se a experiencia realmente lembra um palco puro, sem chrome acidental, sem reintroduzir o `InfoPanel` e sem hover global ruidoso. Testar com item markdown, imagem e PDF.</action>
  <how-to-verify>
    1. Abrir um item markdown, entrar em presentation mode e confirmar que o foco visual vai para o conteudo, nao para o chrome.
    2. Tentar mover o mouse por varias areas da tela e confirmar que os controles nao reaparecem; apenas a area inferior esquerda deve revelá-los.
    3. Repetir com uma imagem e um PDF e confirmar consistencia.
    4. Confirmar que `Esc` e o botao de sair funcionam nos tres casos.
  </how-to-verify>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <done>A superficie de apresentacao se comporta de forma consistente e sem regressao visual obvia.</done>
  <resume-signal>Responder com `approved` ou listar os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header -> shell state | O gatilho precisa alternar presentation mode sem deixar painel/scroll em estado incoerente |
| Hit area -> controls | O reveal deve ser deliberado e restrito ao canto inferior esquerdo |
| Presentation shell -> content viewers | A camada de apresentacao nao pode quebrar markdown, imagem ou PDF |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-05-05 | high | Tampering | viewer-client-shell | mitigate-blocking | Fechar e bloquear `InfoPanel` enquanto o presentation mode estiver ativo |
| T-05-06 | high | Spoofing | controls UX | mitigate-blocking | Impor hit area dedicada e auto-hide por contrato |
| T-05-07 | medium | Denial of Service | presentation overlay | mitigate | Nao duplicar viewers nem trocar de rota; reaproveitar o conteudo ja renderizado |
</threat_model>

<verification>
- `npx vitest run src/__tests__/viewer-header.test.tsx`
- `npx vitest run src/__tests__/viewer-client-shell.test.tsx src/__tests__/presentation-mode.test.tsx`
- `npm run typecheck`
</verification>

<success_criteria>
- O modo apresentacao entra e sai pela app, sem depender da Fullscreen API como definicao primaria.
- O `InfoPanel` deixa de competir com a superficie principal.
- Os controles seguem o padrao travado: discretos, auto-ocultaveis e revelados pela hit area.
</success_criteria>

<output>
After completion, create `.planning/phases/05-presentation-mode/05-02-SUMMARY.md`
</output>
