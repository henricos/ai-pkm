---
phase: 04-asset-viewer-and-item-context
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/viewer-page.test.tsx
  - src/__tests__/item-repository.test.ts
  - src/__tests__/info-panel.test.tsx
  - src/__tests__/preview-route.test.ts
autonomous: true
requirements: [VIEW-04, VIEW-05, VIEW-07, CTX-05]

must_haves:
  truths:
    - "A execução da fase 4 só avança se imagem e PDF estiverem protegidos por contratos automatizados específicos, não por fallback genérico"
    - "A separação entre preview inline autenticado e download attachment fica bloqueada por testes antes de qualquer código de produção"
    - "O sidecar do binário só entra na fase se a leitura segura do `.md` adjacente estiver protegida por testes, sem parsear o binário como UTF-8"
    - "O branch markdown da fase 3 permanece protegido contra regressão enquanto os novos branches de asset são introduzidos"
  artifacts:
    - path: "src/__tests__/viewer-page.test.tsx"
      provides: "Contratos RED/GREEN para imagem, PDF, fallback e preservação do branch markdown"
    - path: "src/__tests__/item-repository.test.ts"
      provides: "Contrato de leitura segura de sidecar adjacente"
    - path: "src/__tests__/info-panel.test.tsx"
      provides: "Contrato de renderização editorial do sidecar no painel"
    - path: "src/__tests__/preview-route.test.ts"
      provides: "Contrato da rota inline autenticada com nosniff e content-disposition inline"
  key_links:
    - from: "src/__tests__/viewer-page.test.tsx"
      to: "src/components/viewer/viewer-page.tsx"
      via: "import e assertions de branch por itemKind"
    - from: "src/__tests__/item-repository.test.ts"
      to: "src/lib/pkm/fs-item-repository.ts"
      via: "getBinaryContext sem ler o binário como texto"
    - from: "src/__tests__/info-panel.test.tsx"
      to: "src/components/viewer/info-panel.tsx"
      via: "slot sidecar-content-phase4 deixa de ser vazio"
    - from: "src/__tests__/preview-route.test.ts"
      to: "src/app/api/pkm/preview/[...path]/route.ts"
      via: "auth + resolveItemPath + headers inline"
---

<objective>
Congelar a estratégia de validação da fase 4 em Wave 0 antes de qualquer implementação nova. Este plano cobre os gaps explicitamente marcados em `04-VALIDATION.md` e evita que a execução invente comportamento durante a codificação.

Purpose: Garantir Nyquist compliance da fase 4, com foco em imagem, PDF, rota inline autenticada e sidecar como contexto complementar.
Output: Quatro arquivos de teste descrevendo os comportamentos exigidos por VIEW-04, VIEW-05, VIEW-07 e CTX-05.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-asset-viewer-and-item-context/04-CONTEXT.md
@.planning/phases/04-asset-viewer-and-item-context/04-RESEARCH.md
@.planning/phases/04-asset-viewer-and-item-context/04-VALIDATION.md
@src/__tests__/viewer-page.test.tsx
@src/__tests__/item-repository.test.ts
@src/__tests__/info-panel.test.tsx
@src/__tests__/raw-route.test.ts

<interfaces>
From src/components/viewer/viewer-page.tsx:
```typescript
interface ViewerPageProps {
  item: {
    id: string;
    label: string;
    scope: string;
    estado: "rascunho" | "finalizado";
    itemKind: NavigationItemKind;
  };
}
```

From src/components/viewer/info-panel.tsx:
```typescript
interface InfoPanelProps {
  panelOpen: boolean;
  onClose: () => void;
  frontmatter: RawFrontmatter;
  topic: string;
  group?: string;
}
```

From src/lib/pkm/item-repository.ts:
```typescript
export interface ItemRepository {
  getItem(id: string): Item | null;
  getItemContent(id: string): string;
  getItemFrontmatter(id: string): RawFrontmatter | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escrever testes de viewer para imagem, PDF e fallback editorial</name>
  <files>src/__tests__/viewer-page.test.tsx</files>
  <behavior>
    - Test 1: `itemKind=image` renderiza viewer dedicado com controles mínimos de zoom/reset, e `unsupported-format` não aparece, per D-01, D-03 e D-03b.
    - Test 2: `itemKind=pdf` renderiza preview inline com URL de preview separada da de download, e `unsupported-format` não aparece, per D-04, D-06 e D-06b.
    - Test 3: `itemKind=binary` continua caindo em fallback editorial com cópia mais legível e sem quebrar o shell, per D-10 e D-11.
    - Test 4: `itemKind=excalidraw` continua em fallback, deixando o preview read-only explicitamente fora do caminho crítico, per D-13.
    - Test 5: `itemKind=markdown` continua usando `MarkdownViewer`, protegendo regressão da fase 3.
  </behavior>
  <action>Atualizar `src/__tests__/viewer-page.test.tsx` para trocar as assertions legadas de unsupported-format em `image` e `pdf` por assertions dos novos branches. Mockar `ViewerClientShell`, `MarkdownViewer`, `ImageViewer`, `PdfViewer` e `UnsupportedViewer` pelos paths reais que os planos seguintes criarão. Verificar também que `ViewerPage` não chama `getItemContent()` para itens binários, evitando regressão de leitura UTF-8 indevida. Referenciar explicitamente VIEW-04, VIEW-05 e VIEW-07 nas descrições dos testes.</action>
  <verify>
    <automated>npx vitest run src/__tests__/viewer-page.test.tsx</automated>
  </verify>
  <done>O arquivo de teste descreve claramente os branches finais de imagem, PDF, fallback e markdown, e falha ou passa apenas em função desses contratos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escrever testes de sidecar no repositório e no InfoPanel</name>
  <files>src/__tests__/item-repository.test.ts, src/__tests__/info-panel.test.tsx</files>
  <behavior>
    - Test 1: `getBinaryContext(id)` retorna `sidecarContent` e `sidecarFrontmatter` do `.md` adjacente, sem ler o binário como texto, per D-07 e D-08.
    - Test 2: ausência de sidecar retorna contexto nulo, mantendo item lógico estável sem erro.
    - Test 3: `InfoPanel` renderiza o corpo Markdown do sidecar no slot final `sidecar-content-phase4`, com hierarquia editorial menor que o conteúdo principal, per D-08 e D-09.
    - Test 4: o sidecar aparece apenas no painel lateral; a navegação segue escondendo o sidecar como item separado, sem exigir mudanças no `navigation-service`.
  </behavior>
  <action>Expandir `src/__tests__/item-repository.test.ts` com casos novos para `getBinaryContext()`, incluindo um mock onde `fs.readFileSync` do binário lança se chamado com encoding `utf-8`, para capturar o risco de parse incorreto. Expandir `src/__tests__/info-panel.test.tsx` para exigir render de sidecar no final do painel usando Markdown rico, sem `rehype-raw`, sem YAML cru e sem duplicar header. Manter o teste de navegação como verificação existente; não criar nova implementação na navegação.</action>
  <verify>
    <automated>npx vitest run src/__tests__/item-repository.test.ts src/__tests__/info-panel.test.tsx src/__tests__/navigation-service.test.ts</automated>
  </verify>
  <done>Os testes travam o contrato de sidecar como contexto do item binário e provam que a navegação continua sem reintroduzir sidecars como itens próprios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Escrever teste dedicado da rota de preview inline autenticada</name>
  <files>src/__tests__/preview-route.test.ts</files>
  <behavior>
    - Test 1: rota `/api/pkm/preview/[...path]` retorna 401 sem sessão, bloqueando bypass de auth, per T-04-01 e T-04-02.
    - Test 2: rota autenticada retorna `Content-Disposition: inline`, `X-Content-Type-Options: nosniff` e `Content-Type` coerente com `.pdf` e imagens, per D-04 e D-06b.
    - Test 3: path traversal retorna 400 e não toca o filesystem real.
    - Test 4: arquivo inexistente retorna 404 sem vazar path absoluto.
  </behavior>
  <action>Criar `src/__tests__/preview-route.test.ts` como arquivo novo, espelhando o padrão de mocks de `raw-route.test.ts`, mas apontado para `@/app/api/pkm/preview/[...path]/route`. O teste deve deixar explícita a separação entre preview inline e download attachment, e verificar que `resolveItemPath()` é reaproveitado como boundary de segurança. Este é o contrato que bloqueia qualquer implementação que tente reaproveitar a rota `/api/pkm/raw` como preview.</action>
  <verify>
    <automated>npx vitest run src/__tests__/preview-route.test.ts</automated>
  </verify>
  <done>Existe um teste dedicado para a rota de preview inline, cobrindo auth, traversal, headers e separação semântica entre preview e download.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests → server handlers | Os testes definem contratos de segurança para rotas que servem arquivos do PKM |
| Tests → repository | Os testes impedem leitura textual acidental de binários e sidecars fora do boundary previsto |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-04-01 | high | Tampering | preview route | mitigate-blocking | Criar teste RED exigindo `resolveItemPath()` e 400 para traversal antes da implementação |
| T-04-02 | high | Elevation of Privilege | preview route | mitigate-blocking | Criar teste RED exigindo `auth()` antes de qualquer leitura de filesystem |
| T-04-03 | high | Information Disclosure | preview/raw separation | mitigate-blocking | Criar teste RED exigindo `inline` na rota de preview e manter `attachment` fora deste plano |
| T-04-04 | medium | Tampering | sidecar rendering | mitigate | Criar teste RED que exige Markdown rico sem `rehype-raw` e sem YAML cru |
| T-04-05 | medium | Denial of Service | viewer-page branch | mitigate | Criar teste RED que falha se `getItemContent()` for chamado em binários |
</threat_model>

<verification>
Executar os quatro testes rápidos da Wave 0:

- `npx vitest run src/__tests__/viewer-page.test.tsx`
- `npx vitest run src/__tests__/item-repository.test.ts src/__tests__/info-panel.test.tsx`
- `npx vitest run src/__tests__/preview-route.test.ts`
- `npx vitest run src/__tests__/navigation-service.test.ts`
</verification>

<success_criteria>
- Todos os gaps marcados em `04-VALIDATION.md` passam a existir como testes automatizados explícitos.
- Nenhum comportamento crítico da fase 4 depende de interpretação implícita do executor.
- As ameaças de alta severidade ficam bloqueadas por contrato antes de qualquer código de produção.
</success_criteria>

<output>
After completion, create `.planning/phases/04-asset-viewer-and-item-context/04-01-SUMMARY.md`
</output>
