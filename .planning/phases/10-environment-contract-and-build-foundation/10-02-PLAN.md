---
phase: 10-environment-contract-and-build-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/lib/env.ts
  - src/__tests__/env.test.ts
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
must_haves:
  truths:
    - "A aplicação recusa subir com mensagem clara quando `APP_BASE_PATH` está ausente."
    - "A aplicação recusa subir com mensagem clara quando `NEXTAUTH_URL` está ausente."
    - "A aplicação recusa subir quando `NEXTAUTH_URL` e `APP_BASE_PATH` divergem no pathname, mostrando o par correto esperado."
  artifacts:
    - path: "src/lib/env.ts"
      provides: "validação fail-fast do contrato app/auth"
  key_links:
    - from: "src/lib/env.ts"
      to: "src/lib/base-path.ts"
      via: "normalização compartilhada do prefixo"
      pattern: "normalizeBasePath|withBasePath"
    - from: "src/lib/env.ts"
      to: "startup server-side"
      via: "parseEnv com process.exit(1)"
      pattern: "process\\.exit\\(1\\)"
---

<objective>
Fechar o contrato fail-fast de ambiente para app e auth sem ainda reescrever os consumers de rota.

Purpose: garantir que ENV-01, ENV-02 e ENV-03 sejam validados no ponto central já existente, preservando a estratégia de falha cedo e a mensagem consolidada em pt-BR.
Output: `src/lib/env.ts` atualizado para exigir `APP_BASE_PATH`, validar `NEXTAUTH_URL` e recusar divergência de pathname.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-environment-contract-and-build-foundation/10-RESEARCH.md
@.planning/phases/10-environment-contract-and-build-foundation/10-01-PLAN.md
@src/lib/env.ts
@src/lib/base-path.ts

<interfaces>
From src/lib/env.ts:
```ts
const envSchema = z.object({ ... }).superRefine((data, ctx) => { ... })
function parseEnv() { ... process.exit(1) }
export const env = parseEnv()
```

From the foundation created in 10-01:
```ts
export function normalizeBasePath(input: string): string
export function withBasePath(pathname: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender o schema de env com `APP_BASE_PATH` obrigatório</name>
  <files>src/lib/env.ts, src/__tests__/env.test.ts</files>
  <action>Atualizar `src/lib/env.ts` para incluir `APP_BASE_PATH` no `envSchema` com a mesma ergonomia de mensagens em pt-BR já usada para variáveis obrigatórias. Reutilizar a semântica de `src/lib/base-path.ts` para validar formato e evitar que o contrato do prefixo derive duas regras diferentes. Adicionar ou ajustar `src/__tests__/env.test.ts` para cobrir ausência de `APP_BASE_PATH`, ausência de `NEXTAUTH_URL`, divergência de pathname e caso válido com `APP_BASE_PATH=/pkm` e `NEXTAUTH_URL=https://host/pkm`. Preserve a estratégia atual de `safeParse`, agregação de issues e `process.exit(1)`; não mover a validação para `next.config.ts`, não espalhar checagens por `auth.ts` ou componentes, e não introduzir fallback silencioso para ausência da variável.</action>
  <verify>
    <automated>npm run typecheck && npm test -- env && rg -n "APP_BASE_PATH|safeParse|process\\.exit\\(1\\)" src/lib/env.ts src/__tests__/env.test.ts</automated>
  </verify>
  <done>`APP_BASE_PATH` passa a integrar o contrato central de ambiente com falha cedo e mensagem coerente com o padrão já adotado no projeto.</done>
</task>

<task type="auto">
  <name>Task 2: Validar a sincronia obrigatória entre `APP_BASE_PATH` e `NEXTAUTH_URL`</name>
  <files>src/lib/env.ts</files>
  <action>Adicionar no `superRefine` a validação de ENV-03 comparando o pathname de `NEXTAUTH_URL` com o `APP_BASE_PATH` normalizado, usando `new URL(...).pathname` e emitindo mensagem clara com exemplo explícito do par correto, como `APP_BASE_PATH=/pkm` junto de `NEXTAUTH_URL=https://host/pkm`. Nesta phase, o pathname aceito e travado pelo roadmap e exatamente o `APP_BASE_PATH` do app; a plan nao altera `basePath` do Auth.js, nao valida `/api/auth` e nao muda a semantica de `pages.signIn`. Preserve a exigência de `NEXTAUTH_URL` obrigatório de ENV-02 e mantenha a validação de produção de `INDEX_PATH` intacta. Não antecipar aqui qualquer ajuste de `pages.signIn`, redirects, callback fallbacks ou viewers; esta plan fecha apenas o contrato runtime app/auth.</action>
  <verify>
    <automated>npm run typecheck && npm test -- env && rg -n "NEXTAUTH_URL|new URL|APP_BASE_PATH=/pkm|https://host/pkm|/api/auth" src/lib/env.ts src/__tests__/env.test.ts</automated>
  </verify>
  <done>O startup passa a recusar configurações sem `NEXTAUTH_URL` ou com pathname divergente do `APP_BASE_PATH`, com orientação explícita para correção e sem abrir margem para reinterpretar o contrato desta phase como `/api/auth`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process environment → `src/lib/env.ts` | Input externo não confiável entra no contrato de runtime |
| `NEXTAUTH_URL` → auth/session callbacks | Uma URL mal configurada quebra autenticação e redirects de sessão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | T | `src/lib/env.ts` | mitigate | Validar `APP_BASE_PATH` e `NEXTAUTH_URL` no startup com schema Zod único, sem defaults implícitos. |
| T-10-05 | D | `src/lib/env.ts` | mitigate | Falhar cedo com mensagem clara quando o pathname divergir, evitando app parcialmente funcional com auth quebrado. |
| T-10-06 | I | `src/lib/env.ts` | mitigate | Exibir exemplo correto de configuração sem imprimir segredos nem aceitar URL malformada. |
</threat_model>

<verification>
Validar que `src/lib/env.ts` continua sendo o único ponto de fail-fast de ambiente e que o contrato agora cobre ausência e divergência de `APP_BASE_PATH`/`NEXTAUTH_URL`.
</verification>

<success_criteria>
- `APP_BASE_PATH` é obrigatório no schema central de ambiente.
- `NEXTAUTH_URL` continua obrigatório e passa a ser validado em sincronia com o prefixo.
- O contrato é fechado em `src/lib/env.ts` sem espalhar lógica para consumers da aplicação.
</success_criteria>

<output>
After completion, create `.planning/phases/10-environment-contract-and-build-foundation/10-02-SUMMARY.md`
</output>
