---
phase: 10-environment-contract-and-build-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - next.config.ts
  - Dockerfile
  - .github/workflows/release-ghcr.yml
  - src/__tests__/next-config.test.ts
  - src/__tests__/release-workflow.test.ts
autonomous: true
requirements:
  - CFG-01
  - CFG-02
must_haves:
  truths:
    - "`next.config.ts` lê `APP_BASE_PATH` como fonte explícita do `basePath` do Next.js."
    - "O builder do Docker recebe `APP_BASE_PATH` antes do `next build`, em vez de depender de conhecimento implícito."
    - "O workflow de release mostra `APP_BASE_PATH=/pkm` no `docker build` de forma visível e rastreável no código."
  artifacts:
    - path: "next.config.ts"
      provides: "basePath do framework ligado ao contrato de build"
    - path: "Dockerfile"
      provides: "propagação do build arg até o estágio builder"
    - path: ".github/workflows/release-ghcr.yml"
      provides: "workflow com `APP_BASE_PATH=/pkm` baked no build"
  key_links:
    - from: ".github/workflows/release-ghcr.yml"
      to: "Dockerfile"
      via: "build-arg APP_BASE_PATH"
      pattern: "APP_BASE_PATH=/pkm|ARG APP_BASE_PATH"
    - from: "Dockerfile"
      to: "next.config.ts"
      via: "ENV APP_BASE_PATH disponível ao `next build`"
      pattern: "ENV APP_BASE_PATH|basePath"
---

<objective>
Fechar a cadeia build-time do prefixo até o framework, sem consumir ainda a nova fundação nos redirects e hrefs do app.

Purpose: materializar CFG-01 e CFG-02 com uma propagação explícita workflow → Dockerfile → Next.js, preservando a decisão da milestone de que o `basePath` é baked no build e exige nova release para mudar.
Output: `next.config.ts`, `Dockerfile` e workflow de release atualizados para tratar `APP_BASE_PATH` como input obrigatório da build.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-environment-contract-and-build-foundation/10-RESEARCH.md
@.planning/phases/10-environment-contract-and-build-foundation/10-01-PLAN.md
@next.config.ts
@Dockerfile
@.github/workflows/release-ghcr.yml

<interfaces>
From next.config.ts:
```ts
const nextConfig: NextConfig = {
  devIndicators: false,
  output: "standalone",
  env: { ... },
}
```

From Dockerfile:
```dockerfile
ARG APP_VERSION
ARG NEXT_PUBLIC_GIT_HASH
RUN npm run build
```

From .github/workflows/release-ghcr.yml:
```yaml
build-args: |
  APP_VERSION=${{ env.RELEASE_VERSION }}
  NEXT_PUBLIC_GIT_HASH=${{ env.SHORT_SHA }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conectar `APP_BASE_PATH` ao build do framework</name>
  <files>next.config.ts, Dockerfile, src/__tests__/next-config.test.ts, src/__tests__/release-workflow.test.ts</files>
  <action>Atualizar `next.config.ts` para ler `APP_BASE_PATH` como fonte explícita de `basePath`, sem hardcode local do valor e sem introduzir fallback silencioso para `/pkm`. No `Dockerfile`, declarar `ARG APP_BASE_PATH` e promovê-lo ao estágio `builder` de forma que o `next build` enxergue esse valor junto dos outros metadados já injetados. Adicionar `src/__tests__/next-config.test.ts` para provar que `basePath` sai de `APP_BASE_PATH` e ajustar `src/__tests__/release-workflow.test.ts` para exigir `APP_BASE_PATH=/pkm` em `build-args`. Preserve os build args/versionamento existentes e não reescreva runtime env do stage `runner` além do necessário para refletir a cadeia de build-time. Não tocar em componentes, páginas, `auth.ts` ou helpers de navegação nesta plan.</action>
  <verify>
    <automated>npm run typecheck && npm test -- next-config release-workflow && rg -n "basePath:|APP_BASE_PATH" next.config.ts Dockerfile .github/workflows/release-ghcr.yml src/__tests__/next-config.test.ts src/__tests__/release-workflow.test.ts</automated>
  </verify>
  <done>`next.config.ts` e `Dockerfile` passam a formar uma cadeia explícita que disponibiliza `APP_BASE_PATH` ao `next build` e o converte em `basePath` do Next.js, com cobertura automatizada da config e do workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Bakear `APP_BASE_PATH=/pkm` no workflow de release</name>
  <files>.github/workflows/release-ghcr.yml</files>
  <action>Atualizar o step `Build and publish image` em `.github/workflows/release-ghcr.yml` para passar `APP_BASE_PATH=/pkm` dentro de `build-args`, mantendo a decisão travada da milestone de tornar o valor visível no código do workflow. Preserve os tags/labels OCI, o gate de ancestry em `main` e a validação de paridade com `package.json`. Não mover esse valor para secrets ou variables do GitHub Actions e não introduzir configurabilidade runtime adicional; a plan deve reforçar a opção baked, não enfraquecê-la.</action>
  <verify>
    <automated>rg -n "APP_BASE_PATH=/pkm|docker/build-push-action@v6|APP_VERSION=\\$\\{\\{ env.RELEASE_VERSION \\}\\}|NEXT_PUBLIC_GIT_HASH=\\$\\{\\{ env.SHORT_SHA \\}\\}" .github/workflows/release-ghcr.yml</automated>
  </verify>
  <done>O workflow de release exibe `APP_BASE_PATH=/pkm` como parte do build oficial da imagem, preservando a rastreabilidade do contrato baked no repositório.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → Docker build | O valor baked do prefixo atravessa a pipeline de publicação e precisa chegar intacto ao build |
| Docker builder → Next.js config | O framework depende de receber o prefixo no build para gerar rotas corretas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | T | `.github/workflows/release-ghcr.yml` | mitigate | Hardcodar `APP_BASE_PATH=/pkm` no workflow conforme decisão da milestone, evitando drift com configuração implícita fora do repositório. |
| T-10-08 | D | `Dockerfile` | mitigate | Declarar e propagar `ARG APP_BASE_PATH` até o estágio `builder`, impedindo build sem o prefixo esperado. |
| T-10-09 | R | `next.config.ts` | mitigate | Referenciar `APP_BASE_PATH` explicitamente como `basePath`, tornando a origem do valor auditável no código. |
</threat_model>

<verification>
Validar a cadeia workflow → Dockerfile → `next.config.ts` por inspeção de arquivo e typecheck, garantindo que o prefixo baked existe sem tocar nos consumers da aplicação.
</verification>

<success_criteria>
- `next.config.ts` usa `APP_BASE_PATH` como `basePath`.
- O `Dockerfile` recebe e expõe `APP_BASE_PATH` ao estágio `builder`.
- O workflow de release inclui `APP_BASE_PATH=/pkm` nos `build-args`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-environment-contract-and-build-foundation/10-03-SUMMARY.md`
</output>
