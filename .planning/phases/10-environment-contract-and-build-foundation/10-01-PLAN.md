---
phase: 10-environment-contract-and-build-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/base-path.ts
  - src/__tests__/with-base-path.test.ts
autonomous: true
requirements:
  - CFG-03
must_haves:
  truths:
    - "Existe um módulo único que define a semântica do base path do app, sem concatenação ad hoc espalhada."
    - "`withBasePath()` transforma paths absolutos internos como `/login` e `/library/x` em URLs prefixadas coerentes com `APP_BASE_PATH`."
    - "A fundação nasce sem reescrever redirects, hrefs, callbacks ou páginas da aplicação antes da Phase 11."
  artifacts:
    - path: "src/lib/base-path.ts"
      provides: "contrato central de normalização e composição do prefixo"
  key_links:
    - from: "src/lib/base-path.ts"
      to: "Phase 11 consumers"
      via: "API estável `withBasePath()`"
      pattern: "export function withBasePath"
    - from: "src/lib/base-path.ts"
      to: "Phase 10 env/build contract"
      via: "normalização compartilhada do prefixo"
      pattern: "normalizeBasePath|APP_BASE_PATH"
---

<objective>
Definir a superfície central do base path antes de qualquer rewiring de consumers da aplicação.

Purpose: entregar o contrato único que a Phase 10 e a Phase 11 vão consumir, evitando que redirects, hrefs e callbacks continuem inventando concatenação própria.
Output: `src/lib/base-path.ts` com API estável para normalização e composição de paths prefixados.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-environment-contract-and-build-foundation/10-RESEARCH.md
@src/lib/env.ts

<interfaces>
From src/lib/env.ts:
```ts
export const env = parseEnv();
```

The new module created here must expose a stable API that later plans can consume directly, without revisiting path semantics.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o módulo canônico de base path</name>
  <files>src/lib/base-path.ts, src/__tests__/with-base-path.test.ts</files>
  <action>Criar `src/lib/base-path.ts` como única fonte de verdade do prefixo do app para esta milestone. O arquivo deve exportar, no mínimo, `normalizeBasePath(input)`, `withBasePath(pathname)` e qualquer helper puro adicional estritamente necessário para reutilizar a mesma semântica em env/build. A semântica precisa cobrir o caso canônico `/pkm`, lidar explicitamente com barra final de forma determinística e rejeitar entradas inválidas com erro claro em pt-BR. Adicionar `src/__tests__/with-base-path.test.ts` cobrindo normalização de `/pkm`, normalização de `/pkm/`, composição de `/`, composição de paths internos como `/login` e rejeição de entradas inválidas. Não importar componentes de UI, não tocar em redirects/hrefs/callbacks existentes e não reescrever consumers da aplicação nesta plan; o objetivo é apenas estabelecer a fundação de CFG-03 para uso posterior.</action>
  <verify>
    <automated>npm run typecheck && npm test -- with-base-path && rg -n "export function normalizeBasePath|export function withBasePath|throw new Error" src/lib/base-path.ts src/__tests__/with-base-path.test.ts</automated>
  </verify>
  <done>O repositório passa a ter um módulo puro e central com a API de prefixo necessária para env/build/consumers, provado por teste unitário próprio e sem qualquer adoção prematura nos pontos de navegação da aplicação.</done>
</task>

<task type="auto">
  <name>Task 2: Travar a fronteira da fundação para a Phase 11</name>
  <files>src/lib/base-path.ts</files>
  <action>Adicionar comentários sucintos no próprio módulo deixando claro onde `withBasePath()` deve ser usado (`redirect()`, callback URLs e composição server-side) e onde não deve ser usado por padrão (`next/link` e consumers já auto-prefixados pelo framework). O objetivo é reduzir drift quando a Phase 11 consumir a API. Não criar wrappers alternativos, não duplicar helpers em `route-helpers.ts` e não alterar `src/lib/auth.ts`, `src/app/(shell)/layout.tsx`, `src/app/(auth)/login/page.tsx`, `src/components/login-form.tsx` nem viewers nesta plan.</action>
  <verify>
    <automated>rg -n "redirect|callback|next/link|auto-prefix" src/lib/base-path.ts</automated>
  </verify>
  <done>O módulo deixa explícita sua responsabilidade e preserva a fronteira da milestone: contrato pronto, consumers ainda intactos para a Phase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/build input → helper module | O valor do prefixo chega de configuração externa e precisa virar contrato interno sem ambiguidade |
| helper module → future redirects/URLs | Um helper mal definido propagaria URLs quebradas para auth, navegação e viewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `src/lib/base-path.ts` | mitigate | Centralizar normalização e rejeitar formatos inválidos em um único módulo, em vez de permitir concatenação manual dispersa. |
| T-10-02 | D | `src/lib/base-path.ts` | mitigate | Lançar erro claro para inputs inválidos logo na fundação, evitando loops ou URLs impossíveis só descobertos em runtime. |
| T-10-03 | R | `src/lib/base-path.ts` | mitigate | Documentar no código os casos de uso esperados e não esperados para reduzir adoção incorreta na Phase 11. |
</threat_model>

<verification>
Validar que `src/lib/base-path.ts` existe, exporta a API contratada e não houve rewrites em consumers de rota fora do boundary desta plan.
</verification>

<success_criteria>
- Existe um módulo único para normalização e composição do base path.
- `withBasePath()` está pronto para uso em redirects/callbacks server-side.
- Nenhum redirect, href ou callback da aplicação foi reescrito prematuramente.
</success_criteria>

<output>
After completion, create `.planning/phases/10-environment-contract-and-build-foundation/10-01-SUMMARY.md`
</output>
