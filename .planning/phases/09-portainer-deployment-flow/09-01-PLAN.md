---
phase: 09-portainer-deployment-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/deploy-compose-ghcr.md
autonomous: true
requirements:
  - DEP-01
  - DEP-03
must_haves:
  truths:
    - "O operador encontra no repositório um fluxo direto de instalação e update por `docker compose` usando `ghcr.io/henricos/ai-pkm:latest`."
    - "Os pré-requisitos de `pkm` e `index` no host aparecem antes do primeiro `docker compose up`."
    - "As instruções terminam em acesso à aplicação até a tela de login, sem smoke test formal, rollback ou direções futuras."
  artifacts:
    - path: "README.md"
      provides: "quickstart público e direto de runtime container"
    - path: "docs/deploy-compose-ghcr.md"
      provides: "guia canônico de instalação, configuração, start e update por compose"
  key_links:
    - from: "README.md"
      to: "docs/deploy-compose-ghcr.md"
      via: "link explícito para o guia canônico de deploy"
      pattern: "deploy-compose-ghcr\\.md"
    - from: "docs/deploy-compose-ghcr.md"
      to: "ghcr.io/henricos/ai-pkm:latest"
      via: "snippet de compose documentado"
      pattern: "ghcr\\.io/henricos/ai-pkm:latest"
    - from: "docs/deploy-compose-ghcr.md"
      to: "pkm/index externos"
      via: "pré-requisitos e bind mounts declarados"
      pattern: "PKM_HOST_PATH|INDEX_HOST_PATH|/data/pkm|/data/index"
---

<objective>
Fechar a superfície pública e canônica de deploy da Phase 09 sem romper o contrato de packaging da Phase 7.

Purpose: entregar o fluxo objetivo que cobre instalação, compose config, start/update e acesso inicial, com `docker compose` como narrativa oficial e `latest` como imagem operacional per D-01, D-03, D-04 e D-11.
Output: `README.md` simplificado para quickstart e um guia dedicado `docs/deploy-compose-ghcr.md`.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-portainer-deployment-flow/09-CONTEXT.md
@.planning/phases/09-portainer-deployment-flow/09-RESEARCH.md
@.planning/phases/07-container-packaging-foundation/07-03-SUMMARY.md
@.planning/phases/08-semver-release-pipeline/08-03-SUMMARY.md
@README.md
@compose.yaml
@.env.compose.example
@docs/docker-validation.md
@docs/release-semver-ghcr.md
@docs/dev-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o guia canônico de deploy/update por compose</name>
  <files>docs/deploy-compose-ghcr.md</files>
  <action>Escrever um guia novo em pt-BR que trate `docker compose` como superfície oficial per D-03 e use `ghcr.io/henricos/ai-pkm:latest` como referência operacional per D-01. O documento deve abrir com pré-requisitos explícitos de host para `pkm` e `index` per D-06, D-07 e D-08; incluir um exemplo direto de `compose.yaml` sem citar Portainer per D-04; orientar criação do arquivo de env, start inicial (`docker compose up -d`) e update (`docker compose pull` + `docker compose up -d`) sem `git pull` no runtime per D-09; e encerrar em “abra a aplicação e chegue à tela de login” per D-11. Não incluir smoke test, rollback, checklist pós-deploy nem especulação futura per D-12 e D-13. Preserve o contrato da Phase 7: trate `compose.yaml` e `.env.compose.example` apenas como referências, não como alvos obrigatórios de edição.</action>
  <verify>
    <automated>rg -n 'ghcr.io/henricos/ai-pkm:latest|docker compose pull|docker compose up -d|PKM_HOST_PATH|INDEX_HOST_PATH|/data/pkm|/data/index|tela de login' docs/deploy-compose-ghcr.md</automated>
  </verify>
  <done>Existe um guia único e completo de deploy/update por compose cobrindo imagem pública, configuração externa e acesso inicial até login, sem conteúdo fora do boundary da fase.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever o README como quickstart público do runtime container</name>
  <files>README.md</files>
  <action>Enxugar a parte operacional do `README.md` para um formato de quickstart de imagem/container per D-10. O README deve ensinar um padrão direto de `compose.yaml`, apontar para o guia canônico criado na Task 1, manter a separação entre dev local e runtime empacotado, e não mencionar Portainer per D-04 e D-05. Ajuste ou remova o texto antigo que ancora o leitor em `docs/docker-validation.md` como rota principal de deploy, mantendo esse doc apenas como validação local da Phase 7. Referencie o guia de release só como etapa anterior ao update, sem duplicar o checklist completo.</action>
  <verify>
    <automated>rg -n 'deploy-compose-ghcr\\.md|docker-validation\\.md|release-semver-ghcr\\.md|docker compose' README.md && ! rg -n 'Portainer' README.md && ! rg -n 'smoke test|rollback|banco de dados|migracao.*banco|futuro banco' README.md</automated>
  </verify>
  <done>O README passa a funcionar como entrada pública curta para instalar/subir/atualizar a app por compose, sem drift com a documentação detalhada e sem citar Portainer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → compose/env | Paths, credenciais e URL base entram como input não confiável na configuração do runtime |
| imagem pública → host com mounts | A imagem publicada precisa consumir apenas configuração externa e binds corretos, sem induzir operador a embutir dados privados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | docs/deploy-compose-ghcr.md | mitigate | Documentar somente binds externos de `pkm` e `index` com paths explícitos e `read_only`, evitando instruções que alterem o contrato de runtime da Phase 7. |
| T-09-02 | I | README.md | mitigate | Remover do quickstart qualquer narrativa que sugira `git pull` no runtime ou mistura entre guia público e validação local, preservando o fluxo por imagem publicada. |
| T-09-03 | E | docs/deploy-compose-ghcr.md | mitigate | Exigir `NEXTAUTH_URL`, credenciais e `NEXTAUTH_SECRET` como configuração externa obrigatória, reduzindo risco de bootstrap inseguro por defaults implícitos. |
</threat_model>

<verification>
Validar que o guia novo contém todos os invariantes travados da fase, que o README aponta para ele como superfície pública principal e que o termo `Portainer` não aparece no `README.md`.
</verification>

<success_criteria>
- O operador encontra no `README.md` um quickstart curto para runtime container e um link para o guia canônico.
- `docs/deploy-compose-ghcr.md` usa `ghcr.io/henricos/ai-pkm:latest`, `docker compose` e pré-requisitos explícitos de `pkm` + `index`.
- Nenhum dos dois arquivos introduz smoke test, rollback ou roadmap futuro.
</success_criteria>

<output>
After completion, create `.planning/phases/09-portainer-deployment-flow/09-01-SUMMARY.md`
</output>
