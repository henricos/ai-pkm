---
phase: 09-portainer-deployment-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - docs/portainer-stack-update.md
  - docs/release-semver-ghcr.md
  - docs/docker-validation.md
autonomous: true
requirements:
  - DEP-02
  - DEP-03
must_haves:
  truths:
    - "O operador consegue mapear a saída da release para o update do servidor atual sem reinventar o compose."
    - "O redeploy no Portainer é descrito apenas como reaplicação do mesmo compose e das mesmas variáveis externas, preservando os mounts de `pkm` e `index`."
    - "`docs/docker-validation.md` continua claramente sendo guia de validação local da imagem, não guia de deploy."
  artifacts:
    - path: "docs/portainer-stack-update.md"
      provides: "ponte operacional curta entre compose canônico e reaplicação no Portainer"
    - path: "docs/release-semver-ghcr.md"
      provides: "handoff explícito da release publicada para o guia de deploy/update"
    - path: "docs/docker-validation.md"
      provides: "fronteira preservada entre validação local e deploy operacional"
  key_links:
    - from: "docs/release-semver-ghcr.md"
      to: "docs/deploy-compose-ghcr.md"
      via: "handoff após publish da imagem"
      pattern: "deploy-compose-ghcr\\.md"
    - from: "docs/portainer-stack-update.md"
      to: "docs/deploy-compose-ghcr.md"
      via: "reuso do mesmo compose e mesmas env vars"
      pattern: "deploy-compose-ghcr\\.md|PKM_HOST_PATH|INDEX_HOST_PATH"
    - from: "docs/docker-validation.md"
      to: "validação local"
      via: "escopo explicitamente limitado à imagem local"
      pattern: "validar localmente|Phase 7|não o fluxo de desenvolvimento"
---

<objective>
Fechar a reconciliação documental da operação real do servidor atual sem poluir a superfície pública principal.

Purpose: garantir que DEP-02 e DEP-03 fiquem completos com uma ponte curta para Portainer e com os docs legados corretamente reposicionados, preservando as decisões D-05 e D-12.
Output: um guia curto de reaplicação no Portainer e cross-links ajustados nos docs já existentes.
</objective>

<execution_context>
@/home/henrico/.codex/get-shit-done/workflows/execute-plan.md
@/home/henrico/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-portainer-deployment-flow/09-CONTEXT.md
@.planning/phases/09-portainer-deployment-flow/09-RESEARCH.md
@.planning/phases/09-portainer-deployment-flow/09-01-PLAN.md
@docs/release-semver-ghcr.md
@docs/docker-validation.md
@docs/dev-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar a reaplicação do compose no Portainer sem criar segunda fonte de verdade</name>
  <files>docs/portainer-stack-update.md</files>
  <action>Escrever um guia curto e operacional em pt-BR para o ambiente atual via Portainer, deixando explícito que ele reaplica o mesmo compose e as mesmas variáveis do guia canônico da Plan 09-01 per D-05. O texto deve focar em editar ou reaplicar o stack usando `ghcr.io/henricos/ai-pkm:latest`, preservando binds externos de `pkm` e `index` per DEP-02, e concluir no refresh/redeploy da aplicação sem inventar YAML alternativo. Não transformar esse arquivo em documentação pública principal; ele é um apêndice operacional do ambiente atual.</action>
  <verify>
    <automated>rg -n 'ghcr.io/henricos/ai-pkm:latest|Portainer|PKM_HOST_PATH|INDEX_HOST_PATH|deploy-compose-ghcr\\.md' docs/portainer-stack-update.md</automated>
  </verify>
  <done>Existe um guia curto de operação no Portainer que depende do compose canônico e documenta preservação de configuração externa e mounts.</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar o handoff entre release, deploy e validação local</name>
  <files>docs/release-semver-ghcr.md, docs/docker-validation.md</files>
  <action>Atualizar `docs/release-semver-ghcr.md` para encerrar a Phase 8 apontando para o guia de deploy/update da Phase 9 após a publicação da imagem, sem duplicar comandos operacionais fora do escopo. Atualizar `docs/docker-validation.md` apenas o suficiente para preservar seu posicionamento como validação local da imagem da Phase 7, removendo qualquer leitura ambígua de que ele seja o guia de deploy do servidor atual. Não adicionar smoke test, rollback, checklist pós-deploy ou especulação futura. Não tocar em `compose.yaml` nem `.env.compose.example` a menos que a própria documentação revele quebra factual incontornável; se não houver quebra, manter ambos intactos per decisão travada.</action>
  <verify>
    <automated>rg -n 'deploy-compose-ghcr\\.md|portainer-stack-update\\.md' docs/release-semver-ghcr.md && rg -n 'validar localmente|Phase 7|imagem local|não o fluxo de desenvolvimento' docs/docker-validation.md && ! rg -n 'guia de deploy do servidor|Portainer' docs/docker-validation.md</automated>
  </verify>
  <done>O handoff entre release, deploy e validação local fica explícito, sem sobreposição editorial e sem abrir uma segunda narrativa de configuração.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release publicada → operador do servidor | O operador precisa sair do publish e cair no guia certo para atualizar o ambiente atual |
| Portainer stack editor → runtime | Uma interface separada pode induzir drift entre YAML, env vars e mounts se a documentação divergir do compose canônico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | T | docs/portainer-stack-update.md | mitigate | Obrigar o guia do Portainer a reutilizar o compose canônico e as mesmas env vars, sem introduzir YAML paralelo nem variáveis alternativas. |
| T-09-05 | R | docs/release-semver-ghcr.md | mitigate | Adicionar handoff explícito para o guia de deploy da Phase 9, reduzindo risco de operador parar no publish e não executar o update correto. |
| T-09-06 | I | docs/docker-validation.md | mitigate | Reforçar o escopo “validação local da imagem” para evitar que segredos e paths de produção sejam copiados para o guia errado. |
</threat_model>

<verification>
Validar que o guia de Portainer referencia o compose canônico em vez de substituí-lo, que o guia de release aponta para o deploy guide e que `docs/docker-validation.md` continua explicitamente local.
</verification>

<success_criteria>
- Existe uma ponte documental curta entre publish no GHCR e update do servidor atual.
- O redeploy no Portainer é descrito como reaplicação do mesmo compose e das mesmas variáveis externas.
- `docs/docker-validation.md` permanece focado em build/validação local e não vira guia de deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/09-portainer-deployment-flow/09-02-SUMMARY.md`
</output>
