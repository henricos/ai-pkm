---
phase: 12-tests-and-operational-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/dev-setup.md
  - README.md
  - compose.yaml
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "docs/dev-setup.md menciona APP_BASE_PATH com exemplo concreto APP_BASE_PATH=/pkm"
    - "docs/dev-setup.md instrui a configurar NEXTAUTH_URL=http://localhost:3000/pkm (com /pkm)"
    - "docs/dev-setup.md explica que localhost:3000/ retorna 404 e que o acesso correto é localhost:3000/pkm"
    - "docs/dev-setup.md tem seção de troubleshooting para erro de sincronia APP_BASE_PATH vs NEXTAUTH_URL"
    - "README.md documenta os 3 lugares de configuração: .env (dev), release-ghcr.yml (build), compose.yaml (runtime)"
    - "README.md explica que mudar o path exige editar o workflow e abrir nova release"
    - "README.md e compose.yaml incluem APP_BASE_PATH no exemplo de compose"
    - "NEXTAUTH_URL no exemplo do README termina em /pkm"
  artifacts:
    - path: "docs/dev-setup.md"
      provides: "Guia de setup local atualizado com APP_BASE_PATH e NEXTAUTH_URL correto"
      contains: "APP_BASE_PATH"
    - path: "README.md"
      provides: "README com contrato dos 3 lugares e nota de nova release"
      contains: "3 lugares"
    - path: "compose.yaml"
      provides: "compose.yaml de validação local com APP_BASE_PATH incluído"
      contains: "APP_BASE_PATH"
  key_links:
    - from: "docs/dev-setup.md"
      to: ".env.example"
      via: "exemplos de variáveis devem estar em sincronia com .env.example"
      pattern: "APP_BASE_PATH=/pkm"
    - from: "README.md"
      to: ".github/workflows/release-ghcr.yml"
      via: "documentar que APP_BASE_PATH é hardcoded no workflow na linha do --build-arg"
      pattern: "release-ghcr.yml"
---

<objective>
Cobrir os requisitos DOC-01 e DOC-02 do milestone v2.2.

DOC-01: Atualizar `docs/dev-setup.md` para documentar `APP_BASE_PATH` como variável
obrigatória, corrigir o exemplo de `NEXTAUTH_URL` para incluir o prefixo `/pkm`,
atualizar a seção de verificação do fluxo para usar `localhost:3000/pkm`, adicionar nota
explícita de que a raiz retorna 404, e adicionar item de troubleshooting para erros de
sincronia.

DOC-02: Atualizar `README.md` para incluir `APP_BASE_PATH` no compose de exemplo,
corrigir `NEXTAUTH_URL` para terminar em `/pkm`, e adicionar uma seção "Contrato dos 3
lugares de configuração" explicando `.env`, `release-ghcr.yml` e `compose.yaml` com nota
de que mudar o path exige nova release. Atualizar `compose.yaml` do repositório para
incluir `APP_BASE_PATH`.

Purpose: Eliminar dependência de conhecimento implícito sobre o contrato de `basePath`.
Qualquer operador novo deve conseguir configurar o ambiente só com esses documentos.

Output: docs/dev-setup.md e README.md atualizados; compose.yaml corrigido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tests-and-operational-documentation/12-RESEARCH.md

<interfaces>
<!-- Referências verificadas no codebase para manter documentação em sincronia -->

De .env.example (estado atual — referência canônica para dev):
```
PKM_PATH=/absolute/path/to/your/pkm
AUTH_USERNAME=seu_usuario
AUTH_PASSWORD=senha_segura_minimo_8_chars
NEXTAUTH_SECRET=string_aleatoria_minimo_32_chars
NEXTAUTH_URL=http://localhost:3000/pkm
APP_BASE_PATH=/pkm
```

De .github/workflows/release-ghcr.yml linha 69 (estado atual verificado):
```yaml
--build-arg APP_BASE_PATH=/pkm
```
(O valor /pkm é hardcoded no workflow — não é uma variável de ambiente do runner)

De compose.yaml (estado atual — FALTA APP_BASE_PATH):
```yaml
environment:
  APP_ROOT_PATH: /app
  PKM_PATH: /data/pkm
  INDEX_PATH: /data/index
  AUTH_USERNAME: ${AUTH_USERNAME}
  AUTH_PASSWORD: ${AUTH_PASSWORD}
  NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
  NEXTAUTH_URL: ${NEXTAUTH_URL}
  # APP_BASE_PATH está ausente — precisa ser adicionado
```

Os 3 lugares de configuração (contrato verificado):
| Lugar          | Arquivo                                   | Tipo                    | Valor atual         |
|----------------|-------------------------------------------|-------------------------|---------------------|
| Desenvolvimento| .env / .env.local                         | Runtime, variável       | APP_BASE_PATH=/pkm  |
| Build da imagem| .github/workflows/release-ghcr.yml L69   | Build-arg, hardcoded    | APP_BASE_PATH=/pkm  |
| Runtime container | compose.yaml (runtime externo)         | Runtime, variável       | ausente — adicionar |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Atualizar docs/dev-setup.md com APP_BASE_PATH e URLs corretas (DOC-01)</name>
  <files>docs/dev-setup.md</files>
  <read_first>
    - docs/dev-setup.md (estado atual — leia inteiro antes de editar)
    - .env.example (referência canônica de variáveis obrigatórias)
    - .planning/REQUIREMENTS.md (definição exata de DOC-01)
    - .planning/phases/12-tests-and-operational-documentation/12-RESEARCH.md (seção "Estado Atual da Documentação — DOC-01" e "Pitfall 4")
  </read_first>
  <action>
Editar `docs/dev-setup.md` aplicando as seguintes mudanças. O arquivo tem estrutura de
seções numeradas — manter todas as seções existentes, apenas atualizar conteúdo onde
indicado.

**1. Adicionar seção para APP_BASE_PATH** (após a seção de NEXTAUTH_URL, antes do exemplo preenchido):

```markdown
#### `APP_BASE_PATH`

Prefixo de rota da aplicação. Em desenvolvimento local, deve ser `/pkm`.

```env
APP_BASE_PATH=/pkm
```

> **Importante:** `APP_BASE_PATH` e `NEXTAUTH_URL` devem estar sincronizados.
> O pathname de `NEXTAUTH_URL` deve terminar com o mesmo valor de `APP_BASE_PATH`.
> Se divergirem, a aplicação recusa iniciar com mensagem explicando o problema.
```

**2. Corrigir seção `NEXTAUTH_URL`** — substituir o texto e o exemplo:

Texto atual a substituir:
```
URL base da aplicação. Para desenvolvimento local:

```env
NEXTAUTH_URL=http://localhost:3000
```
```

Novo texto:
```markdown
URL base da aplicação, incluindo o prefixo de rota. Para desenvolvimento local:

```env
NEXTAUTH_URL=http://localhost:3000/pkm
```

> **Nota:** O pathname de `NEXTAUTH_URL` deve terminar com o mesmo valor de
> `APP_BASE_PATH`. Com `APP_BASE_PATH=/pkm`, use `NEXTAUTH_URL=http://localhost:3000/pkm`.
> Esta aplicação usa NextAuth v5 (Auth.js). O nome `NEXTAUTH_URL` é mantido por
> compatibilidade — Auth.js v5 também aceita `AUTH_URL`.
```

**3. Corrigir o exemplo de `.env.local` preenchido** (seção "Exemplo de `.env.local` preenchido"):

Substituir o bloco de exemplo atual por:
```env
PKM_PATH=/home/user/pkm
# Opcional em dev se você roda a app da raiz do ai-pkm
INDEX_PATH=/home/user/ai-pkm/index
AUTH_USERNAME=curator
AUTH_PASSWORD=minha_senha_local_segura
NEXTAUTH_SECRET=Xk3mP9rQ2vL7nT4wJ8eA1bC6dF0hI5jK
NEXTAUTH_URL=http://localhost:3000/pkm
APP_BASE_PATH=/pkm
```

**4. Atualizar a seção "4. Executar em modo dev"** — adicionar nota sobre o path de acesso:

Após `O servidor sobe em \`http://localhost:3000\`.`, adicionar:

```markdown
> **Atenção:** O acesso correto é `http://localhost:3000/pkm` (com o prefixo).
> A raiz `http://localhost:3000/` retorna 404 — isso é comportamento esperado com
> `APP_BASE_PATH=/pkm`.
```

**5. Atualizar a seção "5. Verificação do fluxo"** — corrigir URLs:

Substituir os 4 passos por:
```markdown
1. Abra `http://localhost:3000/pkm` no navegador
2. Sem sessão ativa, deve redirecionar automaticamente para `/pkm/login`
3. Na tela de login, insira as credenciais configuradas no `.env.local`
4. Após login bem-sucedido, você verá a home autenticada com a lista de tópicos do PKM
```

**6. Adicionar item de troubleshooting** (na seção "7. Troubleshooting"):

Adicionar após o item de "NEXTAUTH_SECRET é obrigatório":
```markdown
### Erro de sincronia: `APP_BASE_PATH=/pkm` mas `NEXTAUTH_URL` não termina em `/pkm`

A aplicação valida que o pathname de `NEXTAUTH_URL` coincide com `APP_BASE_PATH`.
Se você ver uma mensagem como `NEXTAUTH_URL deve terminar com APP_BASE_PATH`, corrija
o `.env.local` para garantir sincronia:

```env
# CORRETO — pathname de NEXTAUTH_URL deve terminar em /pkm
APP_BASE_PATH=/pkm
NEXTAUTH_URL=http://localhost:3000/pkm

# ERRADO — vai causar falha no startup
APP_BASE_PATH=/pkm
NEXTAUTH_URL=http://localhost:3000
```
```

**7. Atualizar o rodapé** — substituir:
`*Última atualização: Phase 1 — Secure Read Model Foundation*`
por:
`*Última atualização: Phase 12 — Tests and Operational Documentation (v2.2)*`
  </action>
  <verify>
    <automated>grep -n "APP_BASE_PATH" /home/henrico/github/henricos/ai-pkm/docs/dev-setup.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "APP_BASE_PATH" docs/dev-setup.md` retorna 5 ou mais (seção própria + exemplo + notas + troubleshooting)
    - `grep "localhost:3000/pkm" docs/dev-setup.md` retorna pelo menos 2 linhas
    - `grep "retorna 404" docs/dev-setup.md` retorna pelo menos 1 linha
    - `grep "sincronia" docs/dev-setup.md` retorna pelo menos 1 linha (seção troubleshooting)
    - `grep "localhost:3000\"" docs/dev-setup.md` retorna 0 linhas (URL sem prefixo /pkm eliminada dos exemplos)
    - A seção "Verificação do fluxo" usa localhost:3000/pkm como primeiro passo
  </acceptance_criteria>
  <done>docs/dev-setup.md documenta APP_BASE_PATH, corrige NEXTAUTH_URL para incluir /pkm, explica que a raiz retorna 404, e tem troubleshooting para erros de sincronia.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Atualizar README.md e compose.yaml com contrato dos 3 lugares (DOC-02)</name>
  <files>README.md, compose.yaml</files>
  <read_first>
    - README.md (estado atual — leia inteiro antes de editar)
    - compose.yaml (estado atual — leia antes de editar)
    - .github/workflows/release-ghcr.yml (para confirmar linha exata do --build-arg APP_BASE_PATH)
    - .planning/REQUIREMENTS.md (definição exata de DOC-02)
    - .planning/phases/12-tests-and-operational-documentation/12-RESEARCH.md (seção "Estado Atual da Documentação — DOC-02" e "Os 3 lugares de configuração")
  </read_first>
  <action>
**Editar `compose.yaml`:**

Adicionar `APP_BASE_PATH: /pkm` na seção `environment`, após `APP_ROOT_PATH`:

```yaml
environment:
  APP_ROOT_PATH: /app
  APP_BASE_PATH: /pkm
  PKM_PATH: /data/pkm
  INDEX_PATH: /data/index
  AUTH_USERNAME: ${AUTH_USERNAME}
  AUTH_PASSWORD: ${AUTH_PASSWORD}
  NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
  NEXTAUTH_URL: ${NEXTAUTH_URL}
```

**Editar `README.md`:**

**1. Atualizar os pré-requisitos do Quickstart** — substituir a linha sobre credenciais:
De:
```
- Credenciais reais para `AUTH_USERNAME`, `AUTH_PASSWORD`, `NEXTAUTH_SECRET` e `NEXTAUTH_URL`
```
Para:
```
- Credenciais reais para `AUTH_USERNAME`, `AUTH_PASSWORD`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL` e `APP_BASE_PATH`
```

**2. Atualizar o exemplo de compose.yaml no README** — substituir o bloco `compose.yaml` do Quickstart:

```yaml
services:
  ai-pkm:
    image: ghcr.io/henricos/ai-pkm:latest
    container_name: ai-pkm
    environment:
      APP_ROOT_PATH: /app
      APP_BASE_PATH: /pkm
      PKM_PATH: /data/pkm
      INDEX_PATH: /data/index
      AUTH_USERNAME: curator
      AUTH_PASSWORD: uma-senha-segura
      NEXTAUTH_SECRET: troque-por-uma-string-aleatoria-com-pelo-menos-32-caracteres
      NEXTAUTH_URL: http://SEU-HOST:3030/pkm
    volumes:
      - /absolute/path/to/pkm:/data/pkm:ro
      - /absolute/path/to/ai-pkm/index:/data/index:ro
    ports:
      - "3030:3000"
    restart: unless-stopped
```

**3. Atualizar a lista de placeholders** — após o bloco yaml, substituir:
De:
```
- `NEXTAUTH_URL`: URL pública real do runtime
```
Para:
```
- `APP_BASE_PATH`: prefixo de rota — use `/pkm` (padrão) ou outro prefixo, mas veja nota abaixo
- `NEXTAUTH_URL`: URL pública real do runtime incluindo o prefixo (ex: `https://meuhost.com/pkm`)
```

**4. Adicionar seção "Contrato dos 3 lugares de configuração"** — inserir APÓS o bloco do Quickstart
(após a linha `Depois, abra \`http://SEU-HOST:3030\`...`), ANTES da seção `## Desenvolvimento`:

```markdown
## Contrato dos 3 lugares de configuração

O `basePath` da aplicação (`APP_BASE_PATH`) é configurado em **3 lugares distintos**,
cada um com papel diferente:

| Lugar | Arquivo | Tipo | Valor padrão |
|-------|---------|------|--------------|
| Desenvolvimento local | `.env` / `.env.local` | Variável de ambiente runtime | `APP_BASE_PATH=/pkm` |
| Build da imagem Docker | `.github/workflows/release-ghcr.yml` (build-arg) | Baked no build, hardcoded | `APP_BASE_PATH=/pkm` |
| Runtime do container | `compose.yaml` (environment) | Variável de ambiente runtime | `APP_BASE_PATH=/pkm` |

> **Importante:** O valor de `APP_BASE_PATH` no workflow (`release-ghcr.yml`) é
> **hardcoded no build** — ele fica baked dentro da imagem Docker no momento do build.
> Isso significa que **mudar o path exige editar o workflow e abrir uma nova release**.
> Não é possível mudar o prefixo apenas trocando a variável no `compose.yaml` do runtime.

> **Sincronia obrigatória:** O pathname de `NEXTAUTH_URL` deve terminar com o mesmo
> valor de `APP_BASE_PATH`. Com `APP_BASE_PATH=/pkm`, use
> `NEXTAUTH_URL=https://meuhost.com/pkm`. A aplicação recusa iniciar se esses valores
> divergirem.
```

**5. Atualizar a URL de confirmação final do Quickstart** — substituir:
`Depois, abra \`http://SEU-HOST:3030\` e confirme que a aplicação chega à tela de login.`
por:
`Depois, abra \`http://SEU-HOST:3030/pkm\` e confirme que a aplicação chega à tela de login. A raiz \`http://SEU-HOST:3030/\` retorna 404 — isso é esperado.`
  </action>
  <verify>
    <automated>grep -n "APP_BASE_PATH" /home/henrico/github/henricos/ai-pkm/README.md && grep -n "APP_BASE_PATH" /home/henrico/github/henricos/ai-pkm/compose.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "APP_BASE_PATH" README.md` retorna 5 ou mais
    - `grep "3 lugares" README.md` retorna pelo menos 1 linha
    - `grep "nova release" README.md` retorna pelo menos 1 linha (nota sobre mudar path)
    - `grep "hardcoded" README.md` retorna pelo menos 1 linha
    - `grep "APP_BASE_PATH" compose.yaml` retorna pelo menos 1 linha
    - `grep "SEU-HOST:3030\"" README.md` retorna 0 linhas (URL sem /pkm eliminada dos exemplos de acesso)
    - O exemplo de compose.yaml no README inclui `NEXTAUTH_URL: http://SEU-HOST:3030/pkm`
    - A seção "Contrato dos 3 lugares" existe no README com tabela de 3 linhas
  </acceptance_criteria>
  <done>README.md documenta os 3 lugares de configuração com exemplos e nota de nova release; compose.yaml do repositório inclui APP_BASE_PATH=/pkm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentação → operador | O operador lê os docs e configura credenciais reais; docs incorretos levam a configuração inválida |
| compose.yaml → ambiente de validação local | compose.yaml sem APP_BASE_PATH causaria falha silenciosa ao validar imagem localmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | docs/dev-setup.md | mitigate | Documentação não deve expor valores reais de NEXTAUTH_SECRET ou credenciais; os exemplos usam placeholders como `Xk3mP9rQ2vL7nT4wJ8eA1bC6dF0hI5jK` (string fictícia, sem entropia real) e avisos explícitos para trocar |
| T-12-06 | Elevation of Privilege | docs/dev-setup.md — Aviso de segurança | accept | A seção de aviso já documenta que a comparação de credenciais é por string simples e inadequada para exposição pública; manter esse aviso no documento atualizado |
| T-12-07 | Tampering | compose.yaml | mitigate | O `APP_BASE_PATH=/pkm` no compose.yaml é um valor estático para validação local; o operador de runtime real tem sua própria cópia do compose conforme documentado no README Quickstart |
| T-12-08 | Denial of Service | Configuração incorreta de NEXTAUTH_URL | mitigate | A documentação corrigida previne configuração de NEXTAUTH_URL sem prefixo /pkm, que causaria falha no startup (DoS acidental por configuração) |
</threat_model>

<verification>
Verificações por requisito:

```bash
# DOC-01: dev-setup.md menciona APP_BASE_PATH e localhost:3000/pkm
grep -c "APP_BASE_PATH" docs/dev-setup.md
grep "localhost:3000/pkm" docs/dev-setup.md
grep "retorna 404" docs/dev-setup.md

# DOC-02: README documenta os 3 lugares e compose.yaml tem APP_BASE_PATH
grep "3 lugares" README.md
grep "nova release" README.md
grep "APP_BASE_PATH" compose.yaml

# Verificação geral de consistência
grep "localhost:3000\"" docs/dev-setup.md  # deve retornar 0 linhas (URL sem prefixo eliminada)
grep "SEU-HOST:3030\"" README.md           # deve retornar 0 linhas (URL sem prefixo eliminada)
```
</verification>

<success_criteria>
1. `docs/dev-setup.md` menciona `APP_BASE_PATH` em pelo menos 5 linhas (seção própria, exemplo, notas, troubleshooting)
2. `docs/dev-setup.md` usa `localhost:3000/pkm` na seção de verificação do fluxo
3. `docs/dev-setup.md` explica que a raiz retorna 404
4. `docs/dev-setup.md` tem item de troubleshooting para erros de sincronia
5. `README.md` tem seção "Contrato dos 3 lugares de configuração" com tabela
6. `README.md` menciona que mudar o path exige nova release
7. `README.md` exemplo de compose inclui `APP_BASE_PATH` e `NEXTAUTH_URL` terminando em `/pkm`
8. `compose.yaml` inclui `APP_BASE_PATH: /pkm` na seção environment
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/12-tests-and-operational-documentation/12-02-SUMMARY.md`
seguindo o template em `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
